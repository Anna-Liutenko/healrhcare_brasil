# 🎨 ПЛАН: Вёрстка прототипа и inline-редактирование

**Дата:** 4 октября 2025  
**Цель:** Превратить HTML прототип в редактируемые блоки CMS  
**Статус:** 📋 Требует выполнения

---

## 🎯 ТВОЯ КОНЦЕПЦИЯ

### Что ты хочешь:

**1. Заверстать все страницы из прототипа как готовые блоки**
- Взять HTML из `Prototype. Healthcare in Brazil.html`
- Разбить каждую секцию на блок
- Сделать эти блоки редактируемыми inline

**2. Inline-редактирование в средней панели**
- Кликнуть на текст → редактировать сразу
- Кликнуть на картинку → заменить
- Кликнуть на кнопку → изменить текст/ссылку
- Всё визуально, без переключения на вкладки

**3. Правая панель — только технические настройки**
- Название страницы
- Slug (URL)
- SEO meta-теги
- Статус (draft/published)
- Tracking codes

**4. Готовые страницы**
- Сразу завёрстанные шаблоны
- Можно взять готовую страницу как основу
- Отредактировать текст/картинки inline
- Сохранить как новую страницу

---

## 📊 ЧТО УЖЕ ЕСТЬ

### ✅ В `templates.js` уже есть 7 готовых шаблонов:

1. **Homepage** (Главная страница)
   - `main-screen` - Hero с фоновой картинкой
   - `service-cards` - Чем я могу помочь
   - `about-section` - Обо мне

2. **Guides** (Страница гайдов)
   - `page-header` - Заголовок страницы
   - `article-cards` (3 колонки) - Карточки гайдов

3. **Blog** (Блог)
   - `page-header`
   - `article-cards` (2 колонки) - Статьи

4. **All Materials** (Все материалы)
   - Комбинация блоков

5. **Bot Assistant** (Бот-помощник)
   - Информация о боте

6. **About Me** (Обо мне)
   - Детальная информация

7. **Contact** (Контакты)
   - Форма обратной связи

### ✅ В `blocks.js` есть определения блоков:

- `main-screen` - Hero секция
- `text-block` - Текстовый блок с Quill
- `service-cards` - Карточки услуг
- `article-cards` - Карточки статей
- `about-section` - Секция "Обо мне"
- `page-header` - Заголовок страницы
- `cta-section` - Call to Action
- `faq-block` - FAQ

### ⚠️ Чего НЕТ:

- ❌ **Inline-редактирование текста** (клик на текст → редактировать)
- ❌ **Inline-замена картинок** (клик на картинку → загрузить новую)
- ❌ **Inline-редактирование кнопок** (клик на кнопку → изменить текст/ссылку)
- ❌ **Полная вёрстка всех секций из прототипа**

---

## 🔍 АНАЛИЗ ПРОТОТИПА

Твой HTML прототип содержит:

### Секции из прототипа (которые нужно превратить в блоки):

1. **Hero с фоновой картинкой** ✅ Уже есть (`main-screen`)
2. **Service Cards** ✅ Уже есть (`service-cards`)
3. **About Me секция** ✅ Уже есть (`about-section`)
4. **Article Cards** ✅ Уже есть (`article-cards`)
5. **Page Header** ✅ Уже есть (`page-header`)
6. **CTA Section** ✅ Уже есть (`cta-section`)
7. **FAQ Block** ✅ Уже есть (`faq-block`)

### Дополнительные секции (которых нет в blocks.js):

8. **Navigation Menu** ❌ Отсутствует
9. **Footer** ❌ Отсутствует
10. **Testimonials** ❌ Отсутствует (если есть в прототипе)
11. **Gallery/Media Grid** ❌ Отсутствует
12. **Contact Form** ❌ Отсутствует (детальная форма)

---

## 📋 ТЕКУЩИЙ СТАТУС В ПЛАНЕ

### Этап 5: Инлайн-редактирование ❌ НЕ ВЫПОЛНЕН

Из `CMS_DEVELOPMENT_PLAN.md`:

```
### Этап 5: Инлайн-редактирование и медиа
- [ ] Inline-редактирование текста в блоках
- [ ] Замена картинок в блоках
- [ ] Редактирование SVG-кода иконок
- [ ] Удаление контейнеров внутри блоков
- [ ] Интеграция медиа-библиотеки в редактор
```

**Проблема:** Этот этап есть в плане, но:
- Не детализирован (как именно реализовать)
- Нет технического описания
- Нет приоритетности

---

## 🚀 ОБНОВЛЁННЫЙ ПЛАН

### ЭТАП 5 (ДЕТАЛИЗИРОВАННЫЙ): Вёрстка прототипа + Inline-редактирование

#### 5.1. Доверстать недостающие блоки из прототипа (2-3 дня)

**Цель:** Все секции из HTML прототипа должны быть блоками

**Задачи:**

1. **Проанализировать прототип:**
   - [ ] Открыть `Prototype. Healthcare in Brazil.html`
   - [ ] Выписать все уникальные секции
   - [ ] Сравнить с существующими блоками в `blocks.js`

2. **Создать недостающие блоки:**
   - [ ] **Navigation Menu Block** (если требуется редактирование меню inline)
     - Горизонтальное меню
     - Логотип
     - Мобильное меню
   
   - [ ] **Footer Block**
     - Колонки с ссылками
     - Соц. сети
     - Copyright
   
   - [ ] **Testimonials Block** (если есть отзывы)
     - Карточки отзывов
     - Аватарки
     - Имена и роли
   
   - [ ] **Gallery Block** (если нужна галерея)
     - Grid изображений
     - Lightbox при клике
   
   - [ ] **Contact Form Block** (детальная форма)
     - Поля формы
     - Валидация
     - Отправка на email

3. **Обновить `blocks.js`:**
   - [ ] Добавить новые типы блоков
   - [ ] Добавить preview для каждого
   - [ ] Добавить настройки по умолчанию

4. **Создать вёрстку для каждого блока:**
   - [ ] HTML структура
   - [ ] CSS стили (из прототипа)
   - [ ] Адаптивность

#### 5.2. Inline-редактирование текста (3-4 дня)

**Цель:** Клик на любой текст → редактирование прямо в средней панели

**Технологии:**
- **ContentEditable API** - для inline-редактирования
- **Quill.js** - для богатого текста (если нужно форматирование)
- **Debounce** - для автосохранения

**Задачи:**

1. **Простой текст (заголовки, параграфы):**
   - [ ] Добавить `contenteditable="true"` на элементы
   - [ ] Отслеживать изменения через `input` event
   - [ ] Сохранять в `block.data` при изменении
   - [ ] Добавить индикатор "редактируется"
   - [ ] Debounce для автосохранения (3 секунды)

2. **Форматированный текст (с Quill):**
   - [ ] Клик на текстовый блок → активировать Quill
   - [ ] Toolbar появляется рядом с текстом
   - [ ] Изменения сохраняются в `block.data.content`
   - [ ] Клик вне блока → сохранить и деактивировать Quill

3. **Особые случаи:**
   - [ ] Inline-редактирование кнопок (текст кнопки, ссылка)
   - [ ] Inline-редактирование списков
   - [ ] Inline-редактирование подписей к изображениям

**Пример кода:**

```javascript
// В editor.js
setupInlineEditing() {
    // Находим все редактируемые элементы
    const editableElements = document.querySelectorAll('[data-editable]');
    
    editableElements.forEach(el => {
        el.contentEditable = true;
        el.classList.add('inline-editable');
        
        // Автосохранение при изменении (с debounce)
        el.addEventListener('input', this.debounce((e) => {
            const blockId = el.closest('[data-block-id]').dataset.blockId;
            const fieldPath = el.dataset.editable; // например, "data.title"
            const newValue = el.textContent;
            
            this.updateBlockField(blockId, fieldPath, newValue);
            this.autoSave();
        }, 3000));
        
        // Подсветка при фокусе
        el.addEventListener('focus', () => {
            el.classList.add('editing');
        });
        
        el.addEventListener('blur', () => {
            el.classList.remove('editing');
        });
    });
}

updateBlockField(blockId, fieldPath, value) {
    const block = this.blocks.find(b => b.id === blockId);
    if (!block) return;
    
    // Поддержка вложенных путей (data.title, data.paragraphs[0])
    const keys = fieldPath.split('.');
    let obj = block;
    for (let i = 0; i < keys.length - 1; i++) {
        obj = obj[keys[i]];
    }
    obj[keys[keys.length - 1]] = value;
}

debounce(func, wait) {
    let timeout;
    return function(...args) {
        clearTimeout(timeout);
        timeout = setTimeout(() => func.apply(this, args), wait);
    };
}
```

**CSS стили:**

```css
/* Inline-редактируемые элементы */
[data-editable] {
    position: relative;
    transition: all 0.2s ease;
}

[data-editable]:hover {
    outline: 2px dashed rgba(0, 141, 141, 0.3);
    outline-offset: 4px;
    cursor: text;
}

[data-editable].editing {
    outline: 2px solid rgba(0, 141, 141, 0.8);
    background-color: rgba(0, 141, 141, 0.05);
}

[data-editable]:focus {
    outline: 2px solid var(--color-action);
    background-color: rgba(0, 141, 141, 0.1);
}
```

#### 5.3. Inline-замена картинок (2-3 дня)

**Цель:** Клик на картинку → загрузить новую

**Задачи:**

1. **Базовая функциональность:**
   - [ ] Клик на `<img>` → открыть File Picker
   - [ ] Выбор файла → загрузка на сервер
   - [ ] После загрузки → заменить `src` картинки
   - [ ] Сохранить URL в `block.data.image`

2. **Интеграция с медиа-библиотекой:**
   - [ ] Клик на картинку → показать модальное окно
   - [ ] Две вкладки: "Загрузить новую" / "Выбрать из библиотеки"
   - [ ] Drag & Drop для загрузки
   - [ ] Preview загруженной картинки
   - [ ] Кнопка "Применить" → заменить картинку

3. **UX улучшения:**
   - [ ] Hover на картинке → иконка "📷 Изменить"
   - [ ] Прогресс-бар при загрузке
   - [ ] Предпросмотр перед заменой
   - [ ] Возможность отменить (undo)

**Пример кода:**

```javascript
// В editor.js
setupImageEditing() {
    const images = document.querySelectorAll('[data-image-editable]');
    
    images.forEach(img => {
        // Обернуть в контейнер с кнопкой
        const wrapper = document.createElement('div');
        wrapper.className = 'image-edit-wrapper';
        img.parentNode.insertBefore(wrapper, img);
        wrapper.appendChild(img);
        
        const editBtn = document.createElement('button');
        editBtn.className = 'image-edit-btn';
        editBtn.innerHTML = '📷 Изменить';
        editBtn.onclick = (e) => {
            e.preventDefault();
            this.openImagePicker(img);
        };
        wrapper.appendChild(editBtn);
    });
}

async openImagePicker(imgElement) {
    // Показать модальное окно медиа-библиотеки
    this.showMediaLibraryModal({
        onSelect: async (mediaFile) => {
            // Заменить картинку
            imgElement.src = mediaFile.url;
            
            // Обновить данные блока
            const blockId = imgElement.closest('[data-block-id]').dataset.blockId;
            const fieldPath = imgElement.dataset.imageEditable;
            this.updateBlockField(blockId, fieldPath, mediaFile.url);
            
            // Автосохранение
            await this.autoSave();
            
            this.showNotification('Изображение обновлено', 'success');
        }
    });
}
```

**HTML шаблон блока:**

```html
<!-- В blocks.js, секция render -->
<div class="block-content">
    <div class="image-edit-wrapper">
        <img 
            :src="blockData.image" 
            :alt="blockData.title"
            data-image-editable="data.image"
            :data-block-id="blockId"
        />
        <button class="image-edit-btn">📷 Изменить</button>
    </div>
    <h2 
        data-editable="data.title"
        :data-block-id="blockId"
    >{{ blockData.title }}</h2>
</div>
```

#### 5.4. Inline-редактирование кнопок (1 день)

**Цель:** Клик на кнопку → изменить текст и ссылку

**Задачи:**

1. **Редактирование текста кнопки:**
   - [ ] Клик на кнопку → текст становится редактируемым
   - [ ] Сохранение при потере фокуса
   - [ ] Обновление `block.data.buttonText`

2. **Редактирование ссылки:**
   - [ ] Double-click на кнопке → модальное окно
   - [ ] Поле для ввода URL
   - [ ] Checkbox "Открывать в новой вкладке"
   - [ ] Сохранение в `block.data.buttonLink`

3. **Стилизация:**
   - [ ] Выбор стиля кнопки (primary/secondary/outline)
   - [ ] Выравнивание (left/center/right)

#### 5.5. Доработка шаблонов страниц (1-2 дня)

**Цель:** Превратить `templates.js` в полноценные готовые страницы

**Задачи:**

1. **Обновить templates.js:**
   - [ ] Добавить все недостающие блоки из прототипа
   - [ ] Проверить, что каждый шаблон соответствует прототипу
   - [ ] Добавить реальный контент (из прототипа)

2. **Интеграция шаблонов в редактор:**
   - [ ] Кнопка "Загрузить шаблон" в редакторе
   - [ ] Модальное окно с превью всех шаблонов
   - [ ] Клик на шаблон → загрузить блоки в редактор
   - [ ] Возможность редактировать загруженный шаблон

3. **Сохранение как шаблон:**
   - [ ] Кнопка "Сохранить как шаблон"
   - [ ] Модальное окно: название, описание, превью
   - [ ] Сохранение в БД (таблица `page_templates`)
   - [ ] Возможность использовать позже

#### 5.6. Медиа-библиотека (уже в плане, но детализировать)

**Уже запланировано в Этапе 4, но нужно интегрировать с inline-редактированием**

---

## 🎨 UX/UI КОНЦЕПЦИЯ

### Средняя панель (Canvas):

**Режим 1: Просмотр**
- Блоки отображаются как на реальном сайте
- Hover на элементе → подсветка + иконка редактирования
- Можно перетаскивать блоки для изменения порядка

**Режим 2: Редактирование**
- Клик на текст → курсор, можно печатать
- Клик на картинку → модальное окно загрузки
- Клик на кнопку → редактирование текста/ссылки
- Все изменения автосохраняются (debounce 3 сек)

### Правая панель (Settings):

**Вкладка "Страница":**
- Название (генерирует slug)
- Slug (можно изменить вручную)
- Тип (regular/article/guide/collection)
- Статус (draft/published)

**Вкладка "SEO":**
- Meta Title
- Meta Description
- Meta Keywords
- Open Graph теги
- Twitter Cards

**Вкладка "Tracking":**
- Google Analytics ID
- Facebook Pixel
- Другие коды
- Страничные коды (только для этой страницы)

**Вкладка "Настройки":**
- Дата публикации
- Автор
- Категории/Теги
- Featured Image

---

## 📊 ПРИОРИТИЗАЦИЯ

### КРИТИЧНО (должно быть в MVP):

1. ✅ **Inline-редактирование текста** (3-4 дня)
2. ✅ **Inline-замена картинок** (2-3 дня)
3. ✅ **Медиа-библиотека** (уже в плане)

### ВАЖНО (желательно в первой версии):

4. ⚠️ **Inline-редактирование кнопок** (1 день)
5. ⚠️ **Доверстать блоки из прототипа** (2-3 дня)
6. ⚠️ **Шаблоны страниц** (1-2 дня)

### МОЖНО ПОТОМ (nice to have):

7. ❌ **Редактирование SVG иконок**
8. ❌ **Удаление контейнеров внутри блоков**
9. ❌ **История изменений (undo/redo)**

---

## 🚀 РЕКОМЕНДУЕМЫЙ ПОРЯДОК ВЫПОЛНЕНИЯ

### Неделя 1: Inline-редактирование текста

**Дни 1-2:**
- [ ] Реализовать базовое inline-редактирование (contenteditable)
- [ ] Добавить автосохранение (debounce)
- [ ] Стилизовать (outline при hover/focus)

**Дни 3-4:**
- [ ] Интегрировать Quill для форматированного текста
- [ ] Добавить floating toolbar
- [ ] Протестировать на всех блоках

### Неделя 2: Inline-замена картинок

**Дни 1-2:**
- [ ] Создать медиа-библиотеку (backend + frontend)
- [ ] Drag & drop загрузка
- [ ] Превью

**День 3:**
- [ ] Модальное окно выбора картинки
- [ ] Интеграция с inline-редактированием

### Неделя 3: Доверстать блоки + шаблоны

**Дни 1-2:**
- [ ] Проанализировать прототип
- [ ] Создать недостающие блоки
- [ ] Обновить blocks.js

**День 3:**
- [ ] Обновить templates.js
- [ ] Добавить "Загрузить шаблон" в редактор

---

## 📝 ОБНОВЛЕНИЕ В CMS_DEVELOPMENT_PLAN.md

Я рекомендую обновить **Этап 5** в плане:

### Было:
```
### Этап 5: Инлайн-редактирование и медиа
- [ ] Inline-редактирование текста в блоках
- [ ] Замена картинок в блоках
- [ ] Редактирование SVG-кода иконок
- [ ] Удаление контейнеров внутри блоков
- [ ] Интеграция медиа-библиотеки в редактор
```

### Должно быть:
```
### Этап 5: Вёрстка прототипа + Inline-редактирование

**5.1. Доверстать блоки из прототипа (2-3 дня)**
- [ ] Проанализировать прототип (все уникальные секции)
- [ ] Создать недостающие блоки (Footer, Navigation, Testimonials, Gallery, Contact Form)
- [ ] Обновить blocks.js с новыми типами
- [ ] Стилизовать согласно прототипу

**5.2. Inline-редактирование текста (3-4 дня)**
- [ ] Простой текст: contenteditable API + автосохранение
- [ ] Форматированный текст: интеграция Quill.js
- [ ] Inline-редактирование кнопок (текст + ссылка)
- [ ] UX: подсветка при hover/focus, индикатор редактирования

**5.3. Inline-замена картинок (2-3 дня)**
- [ ] Базовая функциональность: клик на img → File Picker
- [ ] Интеграция с медиа-библиотекой
- [ ] UX: hover overlay с иконкой, прогресс загрузки
- [ ] Drag & Drop для загрузки

**5.4. Шаблоны страниц (1-2 дня)**
- [ ] Обновить templates.js (все секции из прототипа)
- [ ] Кнопка "Загрузить шаблон" в редакторе
- [ ] Модальное окно с превью шаблонов
- [ ] Возможность сохранять свои шаблоны

**5.5. Медиа-библиотека (см. Этап 4)**
- Интеграция с inline-редактированием
```

---

## 💡 ВЫВОДЫ

### Что у тебя уже есть:
✅ Готовые шаблоны в `templates.js`  
✅ Определения блоков в `blocks.js`  
✅ Базовая структура редактора  

### Чего не хватает для твоей концепции:
❌ Inline-редактирование текста (клик → редактировать)  
❌ Inline-замена картинок (клик → загрузить)  
❌ Некоторые блоки из прототипа (Footer, Navigation, etc.)  

### Главное:
**Этап 5 НЕ ВЫПОЛНЕН, но он КРИТИЧЕН для твоей концепции работы!**

Без inline-редактирования ты вынуждена переключаться в правую панель для каждого изменения, что неудобно.

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ

**Сделать Этап 5 ПРИОРИТЕТОМ #1**

Потому что:
1. Это ТВОЯ главная фишка (inline-редактирование)
2. Без этого CMS неудобно использовать
3. Все остальное (меню, настройки, пользователи) — вторично

**План:**
1. **Сейчас:** Inline-редактирование текста (3-4 дня)
2. **Потом:** Inline-замена картинок (2-3 дня)
3. **Затем:** Доверстать блоки из прототипа (2-3 дня)

**Итого: 2-3 недели → полностью готовая CMS с твоей концепцией!**

---

**Хочешь начать с inline-редактирования текста?** Я могу создать базовую реализацию прямо сейчас! 🚀

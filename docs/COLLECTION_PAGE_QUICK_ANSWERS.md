# 🎯 Быстрые ответы: Страница-коллекция "Все материалы"

**Дата:** 19 октября 2025

---

## ❓ Вопрос 1: План создания страницы-коллекции и работа слоёв

### ✅ Ваша архитектура УЖЕ поддерживает коллекции!

**Что уже есть:**
- ✅ `PageType::Collection` в Entity
- ✅ `Page.collectionConfig` (JSON) в БД
- ✅ Шаблон `all-materials.html` существует
- ✅ Рендеринг блоков `article-cards` работает

### 📐 Как работают слои:

```
🔹 DOMAIN (Entity)
├── PageType::Collection — тип страницы "коллекция"
├── Page::collectionConfig — JSON с настройками сборки
└── Page::getCardImage() — получение картинки для карточки (НОВОЕ)

🔹 APPLICATION (Use Cases)
├── GetCollectionItems — собрать список статей/гайдов (НОВОЕ)
└── UpdateCollectionCardImage — обновить картинку карточки (НОВОЕ)

🔹 INFRASTRUCTURE (Repository)
├── MySQLPageRepository::findByTypeAndStatus() — фильтр по типу (НОВОЕ)
└── MySQLBlockRepository — для извлечения картинок из блоков

🔹 PRESENTATION (Controller)
├── PublicPageController::renderCollectionPage() — рендеринг коллекции (НОВОЕ)
└── CollectionController — API для управления (НОВОЕ)

🔹 UI (Frontend)
├── editor.js — loadCollectionItems(), updateCardImage() (НОВОЕ)
└── editor.html — UI для редактирования картинок (НОВОЕ)
```

### 🔄 Алгоритм работы:

```
User создаёт Article/Guide
   ↓
Сохраняет → БД (type: article/guide)
   ↓
Публикует → status: published
   ↓
GET /all-materials → PublicPageController
   ↓
GetCollectionItems Use Case:
   1. Читает collectionConfig (какие типы собирать)
   2. Фильтрует: WHERE type IN ('article','guide') AND status='published'
   3. Сортирует по publishedAt DESC
   4. Для каждой страницы формирует карточку:
      - title ← page.title
      - snippet ← page.seoDescription
      - image ← АЛГОРИТМ (см. вопрос 2)
      - url ← /page.slug
   5. Группирует по секциям (Гайды / Статьи)
   ↓
Рендерит HTML с article-cards блоками
   ↓
Страница "Все материалы" отображается динамически
```

---

## ❓ Вопрос 2: Алгоритм установки картинок на карточках

### 🎨 Приоритет картинок (от высшего к низшему):

```php
// Page::getCardImage(?array $blocks = null): string

1. ✅ Кастомная картинка (приоритет!)
   collectionConfig.cardImages[pageId] = '/uploads/custom.jpg'
   
2. ⏬ Первый блок main-screen/hero с картинкой
   block.type === 'main-screen' && block.data.image.url
   
3. ⏬ Первый блок article-cards с картинкой
   block.type === 'article-cards' && block.data.cards[0].image.url
   
4. ⏬ Первый загруженный MediaFile для этой страницы
   SELECT url FROM media WHERE page_id = ? LIMIT 1
   (опционально, требует доп. связи в БД)
   
5. ⏬ Fallback картинка по умолчанию
   /uploads/default-card.jpg
```

### 🛠️ Реализация в коде:

```php
// backend/src/Domain/Entity/Page.php

public function getCardImage(?array $blocks = null): string
{
    // 1. Кастомная картинка из collectionConfig
    if ($this->collectionConfig && 
        isset($this->collectionConfig['cardImages'][$this->id])) {
        return $this->collectionConfig['cardImages'][$this->id];
    }
    
    // 2-3. Извлечь из блоков (если переданы)
    if ($blocks) {
        foreach ($blocks as $block) {
            $data = $block->getData();
            
            // Main-screen / hero с картинкой
            if (in_array($block->getType(), ['main-screen', 'hero']) && 
                isset($data['image']['url'])) {
                return $data['image']['url'];
            }
            
            // Article-cards с картинками
            if ($block->getType() === 'article-cards' && 
                isset($data['cards'][0]['image']['url'])) {
                return $data['cards'][0]['image']['url'];
            }
        }
    }
    
    // 4. Fallback
    return '/uploads/default-card.jpg';
}
```

### ✏️ Как пользователь меняет картинку:

```
1. User открывает страницу "Все материалы" в редакторе
2. Видит превью всех карточек статей
3. Нажимает "Изменить картинку" на карточке статьи X
4. Выбирает картинку из галереи или загружает новую
   ↓
PATCH /api/pages/all-materials-id/card-image
{
  "targetPageId": "статья-X-uuid",
  "imageUrl": "/uploads/new-image.jpg"
}
   ↓
UpdateCollectionCardImage Use Case:
   1. Загружает Page (all-materials)
   2. Обновляет collectionConfig.cardImages[X] = new-image.jpg
   3. Сохраняет Page
   ↓
Коллекция перерендерится с новой картинкой
```

### 🔒 Почему это безопасно:

1. ✅ **Не трогает исходную страницу** — картинка карточки хранится в `collectionConfig` страницы-коллекции, а не в самой статье
2. ✅ **Одна статья — разные картинки** — одна статья может иметь разные картинки в разных коллекциях
3. ✅ **Откат на default** — если удалить кастомную картинку, система вернётся к извлечению из блоков
4. ✅ **Не ломает другие страницы** — изменение видно только на странице "Все материалы"

---

## ❓ Вопрос 3: Разделение Статей и Гайдов

### ✅ Да, понимаю! Решение через `collectionConfig.sections`:

### Вариант 1: Секции на одной странице "Все материалы"

```json
// collectionConfig для страницы "Все материалы"
{
  "sourceTypes": ["article", "guide"],
  "sections": [
    {
      "title": "Гайды",
      "sourceTypes": ["guide"]
    },
    {
      "title": "Статьи из блога",
      "sourceTypes": ["article"]
    }
  ]
}
```

**Результат:**
```html
<section>
  <h3>Гайды</h3>
  <div class="articles-grid">
    <!-- Карточки только guide -->
  </div>
</section>

<section>
  <h3>Статьи из блога</h3>
  <div class="articles-grid">
    <!-- Карточки только article -->
  </div>
</section>
```

### Вариант 2: Отдельные коллекции

Создать 3 страницы:

1. **Страница "Гайды"** (slug: `guides`)
   ```json
   {
     "type": "collection",
     "collectionConfig": {
       "sourceTypes": ["guide"]
     }
   }
   ```

2. **Страница "Блог"** (slug: `blog`)
   ```json
   {
     "type": "collection",
     "collectionConfig": {
       "sourceTypes": ["article"]
     }
   }
   ```

3. **Страница "Все материалы"** (slug: `all-materials`)
   ```json
   {
     "type": "collection",
     "collectionConfig": {
       "sourceTypes": ["article", "guide"],
       "sections": [...]
     }
   }
   ```

### 🎯 Рекомендация:

**Используйте Вариант 1** (секции на одной странице) + создайте отдельные коллекции для `guides` и `blog`.

**Преимущества:**
- ✅ Одна точка правды — все материалы на одной странице
- ✅ Автоматическое разделение по типу
- ✅ Можно добавлять новые секции без кода
- ✅ URL `/all-materials` показывает ВСЁ, `/guides` — только гайды

---

## 📊 Структура `collectionConfig` (финальная)

```json
{
  "type": "auto-collection",           // Тип коллекции
  "sourceTypes": ["article", "guide"], // Какие типы собирать
  "sortBy": "publishedAt",             // Сортировка
  "sortOrder": "desc",                 // desc | asc
  "limit": null,                       // null = без лимита
  "excludePages": [],                  // ID страниц для исключения
  "sections": [                        // Секции (для раздельного отображения)
    {
      "title": "Гайды",
      "sourceTypes": ["guide"]
    },
    {
      "title": "Статьи из блога",
      "sourceTypes": ["article"]
    }
  ],
  "cardImages": {                      // Кастомные картинки карточек
    "page-uuid-1": "/uploads/custom-card-1.jpg",
    "page-uuid-2": "/uploads/custom-card-2.jpg"
  }
}
```

---

## 🚀 Что делать дальше:

### Минимальная реализация (MVP):

1. ✅ **Backend Use Cases** (2-3 часа)
   - `GetCollectionItems` — сборка коллекции
   - `UpdateCollectionCardImage` — обновление картинки

2. ✅ **Backend Controller** (1 час)
   - `GET /api/pages/:id/collection-items`
   - `PATCH /api/pages/:id/card-image`

3. ✅ **Frontend UI** (2 часа)
   - Редактор картинок карточек в `editor.html`
   - Интеграция с галереей

4. ✅ **PublicPageController** (1 час)
   - Рендеринг коллекции на публичной странице

### Полная реализация (с тестами):

5. ✅ **Unit-тесты** (2 часа)
6. ✅ **E2E тесты** (1 час)
7. ✅ **Документация** (1 час)

---

## 📝 Краткие выводы:

1. ✅ **Ваша архитектура уже готова** — тип `Collection` есть, `collectionConfig` работает
2. ✅ **Алгоритм картинок безопасен** — кастомные картинки → блоки → fallback
3. ✅ **Разделение типов** — через секции в `collectionConfig` или отдельные коллекции
4. ✅ **Ничего не сломается** — изменения изолированы в `collectionConfig` страницы коллекции

**Готово к реализации! 🎉**

---

**Полный план:** См. `COLLECTION_PAGE_IMPLEMENTATION_PLAN.md` (детальная спецификация с кодом)

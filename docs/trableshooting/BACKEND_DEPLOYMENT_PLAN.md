# План развертывания Backend API для визуального редактора

**Дата:** 2025-10-04
**Цель:** Развернуть PHP backend в XAMPP для работы визуального редактора
**Проблема:** Backend существует локально, но недоступен через Apache

---

## 🎯 ЦЕЛЬ

Сделать backend API доступным по адресу:
```
http://localhost/healthcare-cms-backend/public/api/...
```

Чтобы визуальный редактор мог:
- ✅ Авторизоваться
- ✅ Загружать страницы из БД
- ✅ Сохранять страницы в БД
- ✅ Публиковать страницы

---

## 📋 ПЛАН ДЕЙСТВИЙ (пошагово)

### ШАГ 1: Анализ текущей ситуации

**Что проверю:**

1. **Структуру backend папки:**
   ```bash
   # Проверю, какие файлы есть в backend
   ls -la "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend"
   ```

   **Ожидаю найти:**
   - `public/` - точка входа (index.php)
   - `src/` - исходный код (контроллеры, модели)
   - `vendor/` - зависимости Composer
   - `composer.json` - конфигурация проекта
   - `.env` или `config/` - настройки БД

2. **Проверю наличие vendor (установлены ли зависимости):**
   ```bash
   ls "C:\Users\annal\Documents\Мои сайты\...\backend\vendor"
   ```

   **Если vendor НЕТ:**
   - Нужно запустить `composer install`

   **Если vendor ЕСТЬ:**
   - Переходим к копированию

3. **Проверю конфигурацию БД:**
   ```bash
   # Найду файл с настройками БД
   cat backend/.env
   # или
   cat backend/config/database.php
   ```

   **Проверю параметры:**
   - DB_HOST (должен быть localhost)
   - DB_NAME (должен быть healthcare_cms)
   - DB_USER (должен быть root)
   - DB_PASSWORD (пустой для XAMPP)

---

### ШАГ 2: Подготовка backend к развертыванию

**Что сделаю:**

1. **Проверю и установлю зависимости (если нужно):**
   ```bash
   cd "C:\Users\annal\Documents\Мои сайты\...\backend"
   composer install --no-dev --optimize-autoloader
   ```

   **Зачем `--no-dev`:**
   - Не устанавливать dev-зависимости (PHPUnit, etc.)
   - Ускорить установку

   **Зачем `--optimize-autoloader`:**
   - Оптимизировать автозагрузку классов
   - Быстрее работает в production

2. **Проверю файл .htaccess в public/**
   ```bash
   cat backend/public/.htaccess
   ```

   **Должен содержать:**
   ```apache
   <IfModule mod_rewrite.c>
       RewriteEngine On
       RewriteCond %{REQUEST_FILENAME} !-f
       RewriteCond %{REQUEST_FILENAME} !-d
       RewriteRule ^ index.php [QSA,L]
   </IfModule>
   ```

   **Если файла НЕТ:**
   - Создам его вручную

3. **Проверю composer.json - autoload настройки:**
   ```bash
   cat backend/composer.json
   ```

   **Ищу секцию autoload:**
   ```json
   "autoload": {
       "psr-4": {
           "Presentation\\": "src/Presentation/",
           "Application\\": "src/Application/",
           "Domain\\": "src/Domain/",
           "Infrastructure\\": "src/Infrastructure/"
       }
   }
   ```

   **Если что-то не так:**
   - Запущу `composer dump-autoload`

---

### ШАГ 3: Копирование backend в htdocs

**Вариант A: Простое копирование (РЕКОМЕНДУЮ)**

```bash
# Копирую всю папку backend в htdocs
xcopy "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend" "C:\xampp\htdocs\healthcare-cms-backend" /E /I /Y
```

**Параметры xcopy:**
- `/E` - копировать все подпапки, включая пустые
- `/I` - если destination не существует, создать как папку
- `/Y` - не спрашивать подтверждение

**Проверю результат:**
```bash
ls "C:\xampp\htdocs\healthcare-cms-backend\public"
# Должен быть index.php
```

**Вариант B: Симлинк (если есть права администратора)**

```powershell
# Запущу PowerShell от имени администратора
New-Item -ItemType SymbolicLink `
  -Path "C:\xampp\htdocs\healthcare-cms-backend" `
  -Target "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend"
```

**Преимущество симлинка:**
- Изменения в исходной папке сразу видны в htdocs
- Не нужно копировать при каждом изменении

**Недостаток:**
- Требует прав администратора

---

### ШАГ 4: Настройка Apache для работы с backend

**Что проверю:**

1. **mod_rewrite включен в Apache:**
   ```bash
   # Проверю файл httpd.conf
   grep "mod_rewrite" C:\xampp\apache\conf\httpd.conf
   ```

   **Должно быть:**
   ```apache
   LoadModule rewrite_module modules/mod_rewrite.so
   ```

   **Если закомментировано (начинается с #):**
   - Нужно раскомментировать
   - Перезапустить Apache

2. **AllowOverride включен:**
   ```bash
   # Ищу секцию Directory для htdocs
   grep -A 5 "Directory.*htdocs" C:\xampp\apache\conf\httpd.conf
   ```

   **Должно быть:**
   ```apache
   <Directory "C:/xampp/htdocs">
       Options Indexes FollowSymLinks
       AllowOverride All  # ← ВАЖНО!
       Require all granted
   </Directory>
   ```

   **Если `AllowOverride None`:**
   - Изменю на `AllowOverride All`
   - Перезапущу Apache

3. **Перезапущу Apache (если были изменения):**
   ```bash
   # Остановлю Apache
   net stop Apache2.4

   # Запущу Apache
   net start Apache2.4
   ```

---

### ШАГ 5: Настройка конфигурации backend

**Что проверю и настрою:**

1. **Файл .env или config/database.php:**

   **Проверю текущие настройки:**
   ```bash
   cat C:\xampp\htdocs\healthcare-cms-backend\.env
   ```

   **Должно быть:**
   ```env
   DB_HOST=localhost
   DB_PORT=3306
   DB_NAME=healthcare_cms
   DB_USER=root
   DB_PASSWORD=

   JWT_SECRET=your-secret-key-here
   JWT_EXPIRATION=86400
   ```

   **Если файла .env нет:**
   - Создам его на основе .env.example
   - Или проверю config/database.php

2. **Проверю константы в коде:**
   ```bash
   # Найду, где определены настройки БД
   grep -r "DB_HOST" C:\xampp\htdocs\healthcare-cms-backend\src\
   ```

   **Если используются константы напрямую:**
   - Убедюсь, что они правильные

3. **Проверю JWT секрет:**
   - Должен быть установлен для генерации токенов
   - Минимум 32 символа

---

### ШАГ 6: Проверка БД (должна быть готова)

**Что проверю:**

1. **База данных healthcare_cms существует:**
   ```bash
   mysql -uroot -e "SHOW DATABASES LIKE 'healthcare_cms';"
   ```

   **Ожидаемый результат:**
   ```
   +---------------------------+
   | Database (healthcare_cms) |
   +---------------------------+
   | healthcare_cms            |
   +---------------------------+
   ```

   **Если базы НЕТ:**
   - ❌ ПРОБЛЕМА! База должна быть создана ранее
   - Нужно запустить миграции

2. **Таблицы существуют:**
   ```bash
   mysql -uroot healthcare_cms -e "SHOW TABLES;"
   ```

   **Ожидаю увидеть:**
   - users
   - pages
   - blocks
   - settings
   - sessions
   - и другие

3. **Пользователь anna существует:**
   ```bash
   mysql -uroot healthcare_cms -e "SELECT username, role FROM users WHERE username='anna';"
   ```

   **Ожидаемый результат:**
   ```
   +----------+-------+
   | username | role  |
   +----------+-------+
   | anna     | admin |
   +----------+-------+
   ```

---

### ШАГ 7: Тестирование API endpoints

**Последовательность тестов:**

1. **Health check (простейший endpoint):**
   ```bash
   curl http://localhost/healthcare-cms-backend/public/api/health
   ```

   **Ожидаемый результат:**
   ```json
   {"status":"ok","timestamp":"2025-10-04T18:50:00Z"}
   ```

   **Если 404:**
   - ❌ Apache не видит файлы
   - Проверю права доступа к папке
   - Проверю .htaccess

   **Если 500:**
   - ❌ PHP ошибка
   - Проверю логи: `C:\xampp\apache\logs\error.log`

2. **Login endpoint (проверка БД и JWT):**
   ```bash
   curl -X POST http://localhost/healthcare-cms-backend/public/api/auth/login \
     -H "Content-Type: application/json" \
     -d '{"username":"anna","password":"test123"}'
   ```

   **Ожидаемый результат:**
   ```json
   {
     "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
     "user": {
       "id": "uuid-here",
       "username": "anna",
       "email": "anna@example.com",
       "role": "admin"
     }
   }
   ```

   **Если ошибка "Invalid credentials":**
   - ❌ Пароль неверный в БД
   - Проверю хеш пароля

   **Если ошибка "Database connection failed":**
   - ❌ Не подключается к БД
   - Проверю настройки .env

3. **Get current user (проверка авторизации):**
   ```bash
   # Сначала получу токен из предыдущего запроса
   TOKEN="eyJ0eXAiOiJKV1QiLCJhbGc..."

   curl http://localhost/healthcare-cms-backend/public/api/auth/me \
     -H "Authorization: Bearer $TOKEN"
   ```

   **Ожидаемый результат:**
   ```json
   {
     "id": "uuid-here",
     "username": "anna",
     "email": "anna@example.com",
     "role": "admin"
   }
   ```

4. **Get pages (проверка CRUD):**
   ```bash
   curl http://localhost/healthcare-cms-backend/public/api/pages \
     -H "Authorization: Bearer $TOKEN"
   ```

   **Ожидаемый результат:**
   ```json
   [
     {
       "id": "uuid",
       "title": "Главная",
       "slug": "home",
       "type": "home",
       "status": "published",
       "created_at": "2025-10-04 12:00:00"
     }
   ]
   ```

---

### ШАГ 8: Тестирование в браузере

**Что проверю:**

1. **Открою визуальный редактор:**
   ```
   http://localhost/visual-editor-standalone/
   ```

2. **Проверю Developer Console:**
   - Открою F12 → Console
   - Не должно быть ошибок 404
   - Должен быть запрос к `/api/auth/me`

3. **Проверю модальное окно входа:**
   - Должно открыться автоматически
   - Поля username и password должны быть видны

4. **Войду в систему:**
   - Логин: `anna`
   - Пароль: `test123`
   - Нажму "🔐 Войти"

   **Ожидаю:**
   - Модальное окно закроется
   - В toolbar появится "👤 anna"
   - Console покажет успешный запрос к `/api/auth/login`

5. **Проверю вкладку "Страница":**
   - Переключусь на вкладку "Страница"
   - Должны быть видны все поля
   - Введу название → slug должен сгенерироваться автоматически

6. **Создам тестовую страницу:**
   - Название: "Тестовая страница"
   - Slug: `testovaija-stranitsa` (автоматически)
   - Добавлю блок (любой)
   - Нажму "💾 Сохранить"

   **Ожидаю:**
   - Notification: "✅ Страница создана"
   - Console: успешный POST к `/api/pages`
   - URL изменится на `?id=uuid`

7. **Проверю в БД:**
   ```bash
   mysql -uroot healthcare_cms -e "SELECT id, title, slug FROM pages ORDER BY created_at DESC LIMIT 1;"
   ```

   **Должен увидеть:**
   ```
   +--------------------------------------+-------------------+-----------------------+
   | id                                   | title             | slug                  |
   +--------------------------------------+-------------------+-----------------------+
   | uuid-here                            | Тестовая страница | testovaija-stranitsa  |
   +--------------------------------------+-------------------+-----------------------+
   ```

8. **Проверю блоки в БД:**
   ```bash
   mysql -uroot healthcare_cms -e "SELECT COUNT(*) FROM blocks WHERE page_id='uuid-here';"
   ```

   **Должен быть минимум 1 блок**

---

### ШАГ 9: Обработка возможных ошибок

**Что могу встретить и как исправлю:**

| Ошибка | Причина | Решение |
|--------|---------|---------|
| **404 Not Found** | Apache не видит файлы | 1. Проверить путь к папке<br>2. Проверить .htaccess<br>3. Включить mod_rewrite |
| **500 Internal Server Error** | PHP ошибка | 1. Проверить `apache/logs/error.log`<br>2. Включить `display_errors` в php.ini<br>3. Проверить vendor/autoload.php |
| **CORS error** | Нет CORS заголовков | 1. Проверить `header('Access-Control-Allow-Origin: *')` в index.php<br>2. Добавить в .htaccess |
| **Database connection failed** | Неверные настройки БД | 1. Проверить .env<br>2. Проверить, запущен ли MySQL<br>3. Проверить имя БД |
| **Invalid credentials** | Неверный пароль | 1. Сбросить пароль в БД<br>2. Проверить хеш в users таблице |
| **JWT error** | Нет секрета или библиотеки | 1. Установить JWT библиотеку<br>2. Задать JWT_SECRET в .env |
| **Autoload error** | Composer не установлен | 1. Запустить `composer install`<br>2. Проверить vendor/autoload.php |
| **Permission denied** | Нет прав на файлы | 1. Проверить права на папку<br>2. Дать права 755 на папки, 644 на файлы |

---

### ШАГ 10: Финальная проверка интеграции

**Полный цикл тестирования:**

1. **Создание страницы:**
   - Открыть редактор
   - Войти
   - Заполнить данные страницы
   - Добавить несколько блоков
   - Сохранить
   - ✅ Проверить в БД

2. **Редактирование страницы:**
   - Загрузить страницу по `?id=uuid`
   - Изменить название
   - Изменить блоки
   - Сохранить
   - ✅ Проверить изменения в БД

3. **Публикация страницы:**
   - Нажать "🚀 Опубликовать"
   - ✅ Статус должен измениться на "published"
   - ✅ Проверить в БД: `status = 'published'`

4. **Выход и повторный вход:**
   - Нажать "🚪 Выход"
   - Модальное окно должно появиться
   - Войти снова
   - ✅ Страница должна остаться та же (по URL)

---

## 🔍 ЧЕКЛИСТ ПРОВЕРКИ

### Перед развертыванием:
- [ ] Backend папка найдена
- [ ] vendor/ существует (или запущен `composer install`)
- [ ] .env или config/database.php настроены
- [ ] БД healthcare_cms существует
- [ ] Таблицы созданы
- [ ] Пользователь anna существует

### После копирования:
- [ ] Файлы скопированы в `C:\xampp\htdocs\healthcare-cms-backend\`
- [ ] public/index.php существует
- [ ] public/.htaccess существует
- [ ] vendor/autoload.php существует

### Настройка Apache:
- [ ] mod_rewrite включен
- [ ] AllowOverride All установлен
- [ ] Apache перезапущен (если были изменения)

### Тестирование API:
- [ ] Health endpoint работает (200 OK)
- [ ] Login возвращает token
- [ ] /api/auth/me работает с токеном
- [ ] /api/pages возвращает список страниц

### Тестирование в браузере:
- [ ] Модальное окно входа открывается
- [ ] Вход работает (модалка закрывается)
- [ ] Показывается имя пользователя
- [ ] Вкладка "Страница" работает
- [ ] Slug генерируется автоматически
- [ ] Сохранение создает запись в БД
- [ ] Публикация меняет статус

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения всех шагов:

✅ **Backend API работает:**
```bash
curl http://localhost/healthcare-cms-backend/public/api/health
# → {"status":"ok"}
```

✅ **Визуальный редактор подключен:**
```
http://localhost/visual-editor-standalone/
# → Модальное окно входа → Вход → Работает редактор
```

✅ **CRUD операции работают:**
- Создание страниц → записи в БД
- Редактирование → обновление в БД
- Публикация → изменение статуса
- Загрузка → чтение из БД

✅ **Нет ошибок в консоли браузера**

---

## 📝 ЛОГИ ДЛЯ ОТЛАДКИ

**Если что-то не работает, проверю логи:**

1. **Apache error log:**
   ```
   C:\xampp\apache\logs\error.log
   ```

2. **Apache access log:**
   ```
   C:\xampp\apache\logs\access.log
   ```

3. **PHP error log:**
   ```
   C:\xampp\php\logs\php_error_log
   ```

4. **MySQL log:**
   ```
   C:\xampp\mysql\data\mysql_error.log
   ```

5. **Browser Console:**
   ```
   F12 → Console → Network
   ```

---

## 🚀 НАЧИНАЕМ!

Готов приступить к выполнению плана.

**Первый шаг:** Проверю структуру backend папки и наличие vendor/

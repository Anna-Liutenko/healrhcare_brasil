# 📊 Анализ истории дебага и улучшения плана разработки

**Дата:** 4 октября 2025
**Основа:** Анализ документа `ИСТОРИЯ_ДЕБАГА_API_ИНТЕГРАЦИИ.md`
**Цель:** Предотвратить повторение ошибок при переиспользовании проекта

---

## 🔍 Анализ проблем по слоям Clean Architecture

### 1. Domain Layer (Entities)

#### Проблемы из дебага:
- ❌ **Несоответствие naming convention:** `customName` (код) vs `custom_name` (API)
- ❌ **Отсутствие явных enum значений:** `type: 'page'` не был валидным значением

#### Что добавить в план:

**Новый раздел: 1.1. Naming Convention & Data Contracts**

```markdown
### 1.1. Naming Convention & Data Contracts

**Правила именования полей:**
- **PHP Backend (Database, API):** snake_case (`custom_name`, `created_at`)
- **JavaScript Frontend:** camelCase (`customName`, `createdAt`)
- **URL slugs:** kebab-case (`my-page-slug`)

**Обязательная документация для каждой Entity:**

Block {
  id: string                    // UUID v4
  type: BlockType               // ENUM: см. ниже
  position: number              // >= 0, integer
  custom_name: string | null    // snake_case для API
  data: object                  // JSON, любые данные
}

// ВАЖНО: Явные ENUM значения
enum BlockType {
  MAIN_SCREEN = 'main-screen',
  TEXT_BLOCK = 'text-block',
  SERVICE_CARDS = 'service-cards',
  ARTICLE_CARDS = 'article-cards',
  ABOUT_SECTION = 'about-section',
  PAGE_HEADER = 'page-header',
  CTA_SECTION = 'cta-section',
  FAQ_BLOCK = 'faq-block'
}

enum PageType {
  REGULAR = 'regular',
  ARTICLE = 'article',
  GUIDE = 'guide',
  COLLECTION = 'collection'
}

enum PageStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived'
}

**Правила валидации:**
- slug: /^[a-z0-9-]+$/ (только lowercase latin, numbers, hyphens)
- title: min 1 char, max 255 chars
- type: должен быть из enum PageType
- createdBy: valid UUID v4
```

**Новый раздел: 1.2. API Data Mappers**

```markdown
### 1.2. API Data Mappers

**Обязательно создать utility функции для конвертации между frontend и backend:**

// utils/mappers.js

/**
 * Конвертация Vue объекта в plain object (убирает Proxy)
 */
export function toPlainObject(obj) {
    return JSON.parse(JSON.stringify(obj));
}

/**
 * Конвертация Block из frontend формата в API формат
 */
export function blockToAPI(block) {
    return {
        type: block.type,
        custom_name: block.customName || null,  // camelCase → snake_case
        position: block.position,
        data: toPlainObject(block.data)         // Vue Proxy → Plain Object
    };
}

/**
 * Конвертация Block из API формата в frontend формат
 */
export function blockFromAPI(apiBlock) {
    return {
        type: apiBlock.type,
        customName: apiBlock.custom_name,       // snake_case → camelCase
        position: apiBlock.position,
        data: apiBlock.data
    };
}

/**
 * Транслитерация кириллицы в латиницу для slug
 */
export function transliterate(text) {
    const map = {
        'а': 'a', 'б': 'b', 'в': 'v', 'г': 'g', 'д': 'd', 'е': 'e', 'ё': 'e',
        'ж': 'zh', 'з': 'z', 'и': 'i', 'й': 'y', 'к': 'k', 'л': 'l', 'м': 'm',
        'н': 'n', 'о': 'o', 'п': 'p', 'р': 'r', 'с': 's', 'т': 't', 'у': 'u',
        'ф': 'f', 'х': 'h', 'ц': 'ts', 'ч': 'ch', 'ш': 'sh', 'щ': 'sch',
        'ъ': '', 'ы': 'y', 'ь': '', 'э': 'e', 'ю': 'yu', 'я': 'ya',
        // Uppercase
        'А': 'A', 'Б': 'B', 'В': 'V', 'Г': 'G', 'Д': 'D', 'Е': 'E', 'Ё': 'E',
        'Ж': 'Zh', 'З': 'Z', 'И': 'I', 'Й': 'Y', 'К': 'K', 'Л': 'L', 'М': 'M',
        'Н': 'N', 'О': 'O', 'П': 'P', 'Р': 'R', 'С': 'S', 'Т': 'T', 'У': 'U',
        'Ф': 'F', 'Х': 'H', 'Ц': 'Ts', 'Ч': 'Ch', 'Ш': 'Sh', 'Щ': 'Sch',
        'Ъ': '', 'Ы': 'Y', 'Ь': '', 'Э': 'E', 'Ю': 'Yu', 'Я': 'Ya'
    };

    return text.split('').map(char => map[char] || char).join('');
}

/**
 * Генерация slug из title
 */
export function generateSlug(title) {
    return transliterate(title)
        .toLowerCase()
        .replace(/[^a-z0-9]+/g, '-')
        .replace(/^-+|-+$/g, '');
}
```

---

### 2. Use Cases Layer (Business Logic)

#### Проблемы из дебага:
- ❌ **CreatePage не работает с блоками** - создавал только страницу, игнорировал blocks
- ❌ **Нет детальных сообщений об ошибках** - backend возвращал общий HTTP 400

#### Что добавить в план:

**Дополнить раздел "2. Use Cases":**

```markdown
#### Pages (ДОПОЛНЕНО)
- `createPage(pageData)` - создать страницу **+ сохранить все blocks**
- `updatePage(id, pageData)` - обновить страницу **+ заменить все blocks**
- `deletePage(id)` - удалить страницу **+ каскадно удалить blocks**
- `getPageBySlug(slug)` - получить страницу **+ все blocks отсортированные по position**
- `getPageById(id)` - получить страницу по ID **+ все blocks**
- `getAllPages()` - список всех страниц **(без blocks для производительности)**
- `publishPage(id)` - опубликовать страницу **+ установить publishedAt**
- `unpublishPage(id)` - снять с публикации **+ сбросить publishedAt**

**ВАЖНО: При работе с blocks:**
- Всегда удалять старые blocks перед созданием новых (в updatePage)
- Использовать транзакции для атомарности операций
- Проверять существование page_id перед созданием block
- Валидировать position (должен быть >= 0 и уникален в рамках страницы)

#### Validation Rules (НОВЫЙ РАЗДЕЛ)

**Каждый Use Case должен валидировать входные данные:**

createPage(pageData):
  REQUIRED:
    - title: string, min 1 char, max 255
    - slug: string, pattern /^[a-z0-9-]+$/, unique
    - type: PageType enum
    - createdBy: valid UUID

  OPTIONAL:
    - blocks: array of Block objects
    - seoTitle: string, max 255
    - seoDescription: string, max 500
    - seoKeywords: string, max 255

  RETURNS:
    - success: { pageId: UUID }

  ERRORS:
    - 400: "Title is required"
    - 400: "Slug must contain only lowercase letters, numbers, and hyphens"
    - 400: "Slug 'xxx' already exists"
    - 400: "Type must be one of: regular, article, guide, collection"
    - 400: "CreatedBy must be a valid UUID"
    - 500: "Database error: [details]"

**КРИТИЧЕСКИ ВАЖНО:**
- Все ошибки валидации должны возвращать ДЕТАЛЬНОЕ сообщение
- Указывать какое именно поле невалидно
- Указывать что именно не так с этим полем
```

**Новый раздел: 2.1. Error Handling Standards**

```markdown
### 2.1. Error Handling Standards

**Структура ответа при ошибке:**

{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR" | "NOT_FOUND" | "UNAUTHORIZED" | "SERVER_ERROR",
    "message": "Человекочитаемое сообщение",
    "details": {
      "field": "slug",
      "value": "новая-страница",
      "constraint": "Must contain only lowercase letters, numbers, and hyphens",
      "pattern": "/^[a-z0-9-]+$/"
    }
  }
}

**Коды ошибок:**
- 400 VALIDATION_ERROR - данные не прошли валидацию
- 401 UNAUTHORIZED - не авторизован
- 403 FORBIDDEN - нет прав доступа
- 404 NOT_FOUND - ресурс не найден
- 409 CONFLICT - конфликт (например, slug уже существует)
- 500 SERVER_ERROR - внутренняя ошибка сервера

**PHP Backend должен логировать:**
- Все ошибки валидации в файл validation.log
- Все SQL ошибки в файл database.log
- Все 500 ошибки в файл errors.log
```

---

### 3. Interface Adapters Layer (Repositories & Controllers)

#### Проблемы из дебага:
- ❌ **PageController::create() не сохранял blocks**
- ❌ **Отсутствие логирования запросов**
- ❌ **Недостаточная обработка ошибок в API client**

#### Что добавить в план:

**Дополнить раздел "3. Interface Adapters":**

```markdown
### 3.1. API Request/Response Logging (НОВЫЙ РАЗДЕЛ)

**Backend (PHP):**

// Middleware: ApiLogger.php
class ApiLogger {
    public static function logRequest() {
        $data = [
            'timestamp' => date('Y-m-d H:i:s'),
            'method' => $_SERVER['REQUEST_METHOD'],
            'uri' => $_SERVER['REQUEST_URI'],
            'headers' => getallheaders(),
            'body' => file_get_contents('php://input'),
            'ip' => $_SERVER['REMOTE_ADDR']
        ];

        file_put_contents(
            __DIR__ . '/../../logs/api-requests.log',
            json_encode($data) . PHP_EOL,
            FILE_APPEND
        );
    }

    public static function logResponse($statusCode, $responseData) {
        $data = [
            'timestamp' => date('Y-m-d H:i:s'),
            'status' => $statusCode,
            'data' => $responseData
        ];

        file_put_contents(
            __DIR__ . '/../../logs/api-responses.log',
            json_encode($data) . PHP_EOL,
            FILE_APPEND
        );
    }
}

**Frontend (JavaScript):**

// api-client.js
async request(endpoint, options = {}) {
    const requestId = Date.now();

    console.log(`[${requestId}] 📤 API Request:`, {
        url: `${API_BASE_URL}${endpoint}`,
        method: config.method || 'GET',
        headers: config.headers,
        body: config.body
    });

    const response = await fetch(url, config);
    const data = await response.json();

    console.log(`[${requestId}] 📥 API Response:`, {
        status: response.status,
        statusText: response.statusText,
        data: data
    });

    if (!response.ok) {
        console.error(`[${requestId}] ❌ API Error:`, {
            status: response.status,
            error: data.error || data.message,
            details: data.details
        });
    }

    return data;
}
```

**Новый раздел: 3.2. Backend Controller Standards**

```markdown
### 3.2. Backend Controller Standards

**Каждый контроллер должен:**

1. Логировать входящий запрос (ApiLogger::logRequest())
2. Валидировать авторизацию (если требуется)
3. Вызывать Use Case
4. Обрабатывать исключения с детальными сообщениями
5. Логировать ответ (ApiLogger::logResponse())

**Пример правильного контроллера:**

// PageController.php
public function create(): void
{
    try {
        // 1. Логирование
        ApiLogger::logRequest();

        // 2. Получение данных
        $data = json_decode(file_get_contents('php://input'), true);

        // 3. Валидация JSON
        if (json_last_error() !== JSON_ERROR_NONE) {
            throw new InvalidArgumentException('Invalid JSON: ' . json_last_error_msg());
        }

        // 4. Инициализация репозиториев
        $pageRepository = new MySQLPageRepository();
        $blockRepository = new MySQLBlockRepository();

        // 5. Создание страницы
        $useCase = new CreatePage($pageRepository);
        $page = $useCase->execute($data);

        // 6. ВАЖНО: Сохранение блоков (если есть)
        if (isset($data['blocks']) && is_array($data['blocks'])) {
            foreach ($data['blocks'] as $index => $blockData) {
                $block = new Block(
                    id: Uuid::uuid4()->toString(),
                    pageId: $page->getId(),
                    type: $blockData['type'] ?? 'text-block',
                    position: $blockData['position'] ?? $index,
                    data: $blockData['data'] ?? [],
                    customName: $blockData['custom_name'] ?? null
                );

                $blockRepository->save($block);
            }
        }

        // 7. Успешный ответ
        $response = [
            'success' => true,
            'pageId' => $page->getId()
        ];

        ApiLogger::logResponse(201, $response);
        $this->jsonResponse($response, 201);

    } catch (InvalidArgumentException $e) {
        // Ошибка валидации
        $error = [
            'success' => false,
            'error' => [
                'code' => 'VALIDATION_ERROR',
                'message' => $e->getMessage()
            ]
        ];

        ApiLogger::logResponse(400, $error);
        $this->jsonResponse($error, 400);

    } catch (\Exception $e) {
        // Внутренняя ошибка
        $error = [
            'success' => false,
            'error' => [
                'code' => 'SERVER_ERROR',
                'message' => 'Internal server error',
                'details' => $e->getMessage()
            ]
        ];

        error_log("PageController::create() error: " . $e->getMessage());
        ApiLogger::logResponse(500, $error);
        $this->jsonResponse($error, 500);
    }
}
```

**Новый раздел: 3.3. Frontend API Client Standards**

```markdown
### 3.3. Frontend API Client Standards

**api-client.js должен:**

1. Логировать все запросы и ответы
2. Обрабатывать все типы ошибок с деталями
3. Конвертировать данные через mappers
4. Показывать прогресс длительных операций

**Обязательная структура:**

class ApiClient {
    constructor() {
        this.token = this.getToken();
        this.currentUser = null;
        this.requestId = 0;
    }

    async request(endpoint, options = {}) {
        const requestId = ++this.requestId;
        const url = `${API_BASE_URL}${endpoint}`;

        const headers = {
            'Content-Type': 'application/json',
            ...options.headers
        };

        if (this.token) {
            headers['Authorization'] = `Bearer ${this.token}`;
        }

        const config = {
            ...options,
            headers
        };

        try {
            // Детальное логирование запроса
            console.log(`[${requestId}] 📤 REQUEST:`, {
                url,
                method: config.method || 'GET',
                headers: config.headers,
                body: config.body
            });

            const response = await fetch(url, config);
            const data = await response.json();

            // Детальное логирование ответа
            console.log(`[${requestId}] 📥 RESPONSE:`, {
                status: response.status,
                statusText: response.statusText,
                data
            });

            // КРИТИЧЕСКИ ВАЖНО: Детальная обработка ошибок
            if (!response.ok) {
                const errorDetails = {
                    status: response.status,
                    statusText: response.statusText,
                    code: data.error?.code || 'UNKNOWN',
                    message: data.error?.message || data.message || 'Unknown error',
                    details: data.error?.details || data.details || null,
                    fullResponse: data
                };

                console.error(`[${requestId}] ❌ ERROR:`, errorDetails);

                // Формируем читаемое сообщение
                let errorMessage = `HTTP ${response.status}: ${errorDetails.message}`;
                if (errorDetails.details) {
                    errorMessage += `\n${JSON.stringify(errorDetails.details, null, 2)}`;
                }

                const error = new Error(errorMessage);
                error.details = errorDetails;
                throw error;
            }

            return data;

        } catch (error) {
            // Ошибка сети или парсинга
            if (!error.details) {
                console.error(`[${requestId}] ❌ NETWORK ERROR:`, error);
            }
            throw error;
        }
    }

    // Методы для работы с pages должны использовать mappers
    async createPage(pageData) {
        const apiData = {
            ...pageData,
            blocks: pageData.blocks?.map(blockToAPI) || []  // Используем mapper!
        };

        return await this.request('/api/pages', {
            method: 'POST',
            body: JSON.stringify(apiData)
        });
    }

    async getPage(pageId) {
        const data = await this.request(`/api/pages/${pageId}`, {
            method: 'GET'
        });

        // Конвертируем blocks из API формата
        if (data.blocks) {
            data.blocks = data.blocks.map(blockFromAPI);
        }

        return data;
    }
}
```

---

### 4. Frameworks & Drivers Layer (UI)

#### Проблемы из дебага:
- ❌ **Login modal вне Vue app** - Vue не мог управлять элементом
- ❌ **Отсутствие инструментов отладки** - приходилось рыться в DevTools
- ❌ **currentUser терялся при перезагрузке** - не было персистентности

#### Что добавить в план:

**Дополнить раздел "4. Frameworks & Drivers":**

```markdown
### 4.1. Vue.js Architecture Standards (НОВЫЙ РАЗДЕЛ)

**Правила для Vue компонентов:**

1. **Все интерактивные элементы должны быть внутри `<div id="app">`**
   - Модальные окна
   - Debug панели
   - Notification toasts
   - Любые элементы с v-if, v-for, {{ }}

2. **Persistence через localStorage:**
   - currentUser должен сохраняться при login и загружаться при mount
   - Проверять актуальность через API после загрузки из localStorage
   - Очищать при logout

3. **Обработка Vue Proxy объектов:**
   - Всегда конвертировать через JSON.parse(JSON.stringify()) перед отправкой в API
   - Использовать utility функцию toPlainObject()

**Обязательная структура data():**

data() {
    return {
        // API & Auth
        apiClient: apiClient,
        isAuthenticated: false,
        currentUser: null,  // Должен персистить в localStorage

        // Debug (ОБЯЗАТЕЛЬНО)
        debugLog: [],
        showDebugPanel: true,

        // UI State
        showLoginModal: false,
        isSaving: false,
        isPublishing: false,
        notification: null,

        // Page Data
        currentPageId: null,
        pageTitle: '',
        pageSlug: '',
        pageStatus: 'draft',
        blocks: []
    };
}

**Обязательные методы:**

async mounted() {
    // 1. Проверка авторизации
    this.isAuthenticated = this.apiClient.isAuthenticated();

    // 2. Загрузка пользователя (с localStorage для быстроты)
    if (this.isAuthenticated) {
        // Быстро загружаем из localStorage
        const savedUser = localStorage.getItem('cms_current_user');
        if (savedUser) {
            this.currentUser = JSON.parse(savedUser);
        }

        // Проверяем актуальность через API
        try {
            const apiUser = await this.apiClient.getCurrentUser();
            this.currentUser = apiUser;
            localStorage.setItem('cms_current_user', JSON.stringify(apiUser));
        } catch (error) {
            // Токен невалиден - очищаем
            this.apiClient.setToken(null);
            this.isAuthenticated = false;
            this.currentUser = null;
            localStorage.removeItem('cms_current_user');
            this.showLoginModal = true;
        }
    } else {
        this.showLoginModal = true;
    }
}

async handleLogin() {
    try {
        const result = await this.apiClient.login(
            this.loginForm.username,
            this.loginForm.password
        );

        this.isAuthenticated = true;
        this.currentUser = result.user;

        // ОБЯЗАТЕЛЬНО сохранить в localStorage
        localStorage.setItem('cms_current_user', JSON.stringify(result.user));

        this.showLoginModal = false;
    } catch (error) {
        this.loginError = error.message;
    }
}

async handleLogout() {
    try {
        await this.apiClient.logout();
    } finally {
        this.isAuthenticated = false;
        this.currentUser = null;

        // ОБЯЗАТЕЛЬНО очистить localStorage
        localStorage.removeItem('cms_current_user');
        localStorage.removeItem('cms_auth_token');
    }
}
```

**Новый раздел: 4.2. Debug Tools (ОБЯЗАТЕЛЬНО)**

```markdown
### 4.2. Debug Tools Standards

**Debug Panel - обязательный компонент для всех админ-страниц:**

1. **Визуальная панель с логами**
   - Цветовая кодировка (info=синий, success=зелёный, warning=жёлтый, error=красный)
   - Временные метки
   - Возможность просмотра JSON данных
   - Кнопка очистки
   - Кнопка скрытия/показа

2. **Метод debugMsg() для логирования:**

debugMsg(message, type = 'info', data = null) {
    const timestamp = new Date().toLocaleTimeString('ru-RU');

    this.debugLog.push({
        time: timestamp,
        message,
        type,  // 'info' | 'success' | 'warning' | 'error'
        data: data ? JSON.stringify(data, null, 2) : null
    });

    // Дублируем в консоль
    const consoleMethod = type === 'error' ? 'error' : type === 'warning' ? 'warn' : 'log';
    console[consoleMethod](`[${timestamp}] ${message}`, data || '');

    // Автопрокрутка
    this.$nextTick(() => {
        const debugContent = document.querySelector('.debug-content');
        if (debugContent) {
            debugContent.scrollTop = debugContent.scrollHeight;
        }
    });
}

3. **Интеграция в критические методы:**

async savePage() {
    this.debugMsg('========== НАЧАЛО СОХРАНЕНИЯ ==========', 'info');

    this.debugMsg('Авторизация проверена', 'success', {
        userId: this.currentUser.id,
        username: this.currentUser.username
    });

    const pageData = { /* ... */ };
    this.debugMsg('Данные страницы подготовлены', 'info', pageData);

    try {
        const response = await this.apiClient.createPage(pageData);
        this.debugMsg('Страница сохранена', 'success', response);
    } catch (error) {
        this.debugMsg('ОШИБКА СОХРАНЕНИЯ', 'error', {
            message: error.message,
            details: error.details
        });
    }
}
```

**Новый раздел: 4.3. Slug Generation Standards**

```markdown
### 4.3. Slug Generation Standards

**Обязательно использовать транслитерацию:**

import { transliterate, generateSlug } from './utils/mappers.js';

// В компоненте
data() {
    return {
        pageTitle: '',
        pageSlug: '',  // Автоматически генерируется из title
    };
},

watch: {
    pageTitle(newTitle) {
        // Автоматически обновляем slug при изменении title
        if (!this.pageSlug || this.pageSlug === generateSlug(this.oldTitle)) {
            this.pageSlug = generateSlug(newTitle);
        }
        this.oldTitle = newTitle;
    }
}

**Валидация slug перед отправкой:**

validateSlug(slug) {
    const pattern = /^[a-z0-9-]+$/;
    if (!pattern.test(slug)) {
        throw new Error('Slug must contain only lowercase letters, numbers, and hyphens');
    }
    return true;
}

async savePage() {
    // Валидация
    this.validateSlug(this.pageSlug);

    // Отправка
    // ...
}
```

---

## 📋 Обновлённый План разработки с учётом дебага

### Этап 0: Подготовка (НОВЫЙ ЭТАП - ПЕРЕД ВСЕМИ)

**0.1. Документация API Contract**
- [ ] Создать файл `API_CONTRACT.md` с полным описанием всех endpoints
- [ ] Указать request/response форматы для каждого endpoint
- [ ] Документировать все enum значения
- [ ] Указать правила валидации для всех полей
- [ ] Примеры успешных и ошибочных запросов

**0.2. Создание Utility библиотек**
- [ ] `utils/mappers.js` - конвертация данных между frontend/backend
  - [ ] toPlainObject() - Vue Proxy → Plain Object
  - [ ] blockToAPI() / blockFromAPI() - camelCase ↔ snake_case
  - [ ] transliterate() - кириллица → латиница
  - [ ] generateSlug() - title → slug с транслитерацией
- [ ] `utils/validators.js` - валидация данных на фронтенде
  - [ ] validateSlug()
  - [ ] validateEmail()
  - [ ] validateUUID()

**0.3. Настройка Debug Tools**
- [ ] Создать компонент Debug Panel
- [ ] Добавить стили для Debug Panel
- [ ] Реализовать debugMsg() метод
- [ ] Настроить backend logging (ApiLogger middleware)

### Этап 2: База данных (ДОПОЛНЕНО)

- [ ] Создать новую БД `healthcare_cms`
- [ ] Таблицы: `pages`, `blocks`, `users`, `sessions`, `menu`, `media`, `settings`
- [ ] **ВАЖНО: Связи между таблицами с ON DELETE CASCADE для blocks**
- [ ] Индексы и оптимизация
- [ ] **Создать файл SEED_DATA.sql с тестовыми данными для проверки**

### Этап 3: PHP Backend (ДОПОЛНЕНО)

**3.1. Структура проекта**
- [ ] Clean Architecture структура
- [ ] **ApiLogger middleware для логирования запросов/ответов**
- [ ] **Error handling с детальными сообщениями**

**3.2. Use Cases**
- [ ] CreatePage **+ сохранение blocks**
- [ ] UpdatePage **+ замена blocks (удалить старые, создать новые)**
- [ ] **Все Use Cases должны возвращать детальные ошибки валидации**

**3.3. Controllers**
- [ ] **Каждый контроллер должен следовать стандарту из раздела 3.2**
- [ ] **Обрабатывать InvalidArgumentException → 400**
- [ ] **Обрабатывать Exception → 500 с логированием**
- [ ] **Логировать все запросы и ответы**

**3.4. Тестирование**
- [ ] **Написать интеграционные тесты для каждого endpoint**
- [ ] **Тест на кириллицу в slug (должен вернуть 400)**
- [ ] **Тест на сохранение blocks вместе с page**
- [ ] **Тест на невалидный type (должен вернуть 400)**

### Этап 4: Frontend - Админка (ДОПОЛНЕНО)

**4.1. Базовая структура**
- [ ] Страница входа (`/admin`)
- [ ] **Login modal ВНУТРИ <div id="app">**
- [ ] **Персистентность currentUser через localStorage**
- [ ] **Проверка токена при mount**

**4.2. API Integration**
- [ ] api-client.js **с детальной обработкой ошибок**
- [ ] **Использование mappers для конвертации данных**
- [ ] **Логирование всех запросов/ответов**

**4.3. Visual Editor**
- [ ] Интеграция редактора с API
- [ ] **Использование toPlainObject() перед отправкой blocks**
- [ ] **Автоматическая генерация slug с транслитерацией**
- [ ] **Debug Panel на странице редактора**
- [ ] **Валидация данных перед отправкой**

**4.4. Debug Tools**
- [ ] **Debug Panel компонент**
- [ ] **debugMsg() во всех критических методах**
- [ ] **Кнопка очистки логов**
- [ ] **Кнопка скрытия/показа панели**

### Этап 8: Тестирование и QA (НОВЫЙ ЭТАП)

**8.1. Unit тесты**
- [ ] Тесты для utils/mappers.js
- [ ] Тесты для utils/validators.js
- [ ] Тесты для generateSlug() с кириллицей

**8.2. Integration тесты**
- [ ] Тест полного цикла: создание страницы → сохранение → загрузка
- [ ] Тест авторизации: login → reload → still authenticated
- [ ] Тест ошибок: невалидный slug → получаем детальную ошибку 400

**8.3. Checklist перед деплоем**
- [ ] Все logs работают (frontend console, backend log files)
- [ ] Debug Panel доступен в админке
- [ ] Ошибки API возвращают детальные сообщения
- [ ] Транслитерация slug работает для всех кириллических символов
- [ ] currentUser персистит при перезагрузке страницы
- [ ] Blocks сохраняются вместе с Page

---

## 📊 Checklist для нового проекта на базе этого

### Перед началом разработки:
- [ ] Создан файл `API_CONTRACT.md` с полным описанием API
- [ ] Все enum значения задокументированы
- [ ] Naming convention согласован (snake_case для API, camelCase для frontend)
- [ ] Создана библиотека utils/mappers.js
- [ ] Создана библиотека utils/validators.js
- [ ] Настроен Debug Panel компонент
- [ ] Настроен ApiLogger middleware на backend

### Во время разработки:
- [ ] Каждый Use Case валидирует входные данные
- [ ] Каждый Use Case возвращает детальные ошибки
- [ ] Каждый Controller логирует запросы и ответы
- [ ] Каждый API метод использует mappers для конвертации данных
- [ ] Все критические методы используют debugMsg()
- [ ] Vue реактивные объекты конвертируются через toPlainObject()

### Перед тестированием:
- [ ] Написаны интеграционные тесты для всех endpoints
- [ ] Написаны тесты на граничные случаи (кириллица, спецсимволы)
- [ ] Протестирована персистентность auth через localStorage
- [ ] Протестировано сохранение связанных данных (Page + Blocks)

### Перед деплоем:
- [ ] Debug Panel отключен или доступен только для super_admin
- [ ] Все логи работают
- [ ] Ошибки API не показывают sensitive данные
- [ ] Создан backup базы данных
- [ ] Проверена работа на production-подобном окружении

---

## 🎯 Ключевые выводы

### Что ОБЯЗАТЕЛЬНО делать в будущих проектах:

1. **API Contract First** - документировать API ДО начала разработки
2. **Детальные ошибки** - каждая ошибка валидации должна указывать поле и причину
3. **Mappers** - всегда использовать utility функции для конвертации данных
4. **Debug Tools** - Debug Panel и логирование с первого дня
5. **Транслитерация** - для многоязычных проектов всегда делать транслитерацию slug
6. **Тесты** - интеграционные тесты для критических flows
7. **Персистентность** - всегда сохранять состояние auth в localStorage
8. **Vue Proxy** - всегда конвертировать перед отправкой в API
9. **Логирование** - на фронтенде И бэкенде
10. **Валидация** - на фронтенде И бэкенде

### Что проверять при code review:

- ✅ Используются ли mappers для конвертации данных?
- ✅ Конвертируются ли Vue Proxy через toPlainObject()?
- ✅ Возвращаются ли детальные ошибки из API?
- ✅ Логируются ли все запросы/ответы?
- ✅ Есть ли интеграционные тесты?
- ✅ Работает ли Debug Panel?
- ✅ Персистит ли currentUser при перезагрузке?
- ✅ Работает ли транслитерация slug?
- ✅ Сохраняются ли связанные данные (Page + Blocks)?
- ✅ Все ли элементы Vue внутри <div id="app">?

---

**Дата создания:** 4 октября 2025, 23:59
**Автор:** Claude + Anna
**Статус:** Готов для использования в будущих проектах

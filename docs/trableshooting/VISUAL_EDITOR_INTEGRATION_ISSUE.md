# Проблема интеграции визуального редактора с backend API

**Дата обновления:** 2025-10-04 18:50
**Критичность:** 🔴 Высокая
**Статус:** ⚠️ Код интегрирован, но backend недоступен

---

## 🔧 ЧТО БЫЛО СДЕЛАНО (2025-10-04)

### ✅ Интеграция завершена в коде:

**1. Файл: `C:\xampp\htdocs\visual-editor-standalone\editor.js`**
- ✅ Добавлен импорт `ApiClient` из `./api-client.js`
- ✅ Добавлены методы аутентификации:
  - `checkAuth()` - проверка токена и получение текущего пользователя
  - `login()` - вход в систему
  - `logout()` - выход из системы
- ✅ Добавлен метод загрузки страниц:
  - `loadPageFromAPI(pageId)` - загрузка страницы с блоками из БД
- ✅ Переписан метод сохранения:
  - `savePage()` - создание/обновление страницы через API (вместо localStorage)
- ✅ Добавлен метод публикации:
  - `publishPage()` - публикация страницы
- ✅ Добавлена генерация slug:
  - `generateSlug(title)` - транслитерация русских символов
  - `onTitleChange()` - автогенерация slug при вводе названия
- ✅ Добавлено состояние:
  - `pageData` - название, slug, тип, статус, SEO метаданные
  - `currentUser` - текущий пользователь
  - `currentPageId` - ID редактируемой страницы
  - `isEditMode` - флаг режима редактирования

**2. Файл: `C:\xampp\htdocs\visual-editor-standalone\index.html`**
- ✅ Добавлена вкладка "Страница" с полями:
  - Название страницы (с автогенерацией slug)
  - Slug (URL)
  - Тип страницы (regular/home/article/guide)
  - Статус (draft/published)
  - SEO Title, Description, Keywords
- ✅ Добавлено модальное окно входа:
  - Поля: username, password
  - Кнопка "Войти"
  - Открывается автоматически при отсутствии токена
- ✅ Обновлен toolbar:
  - Показывает имя пользователя
  - Кнопка "💾 Сохранить" → вызывает `savePage()`
  - Кнопка "🚀 Опубликовать" → вызывает `publishPage()`
  - Кнопка "🚪 Выход" → вызывает `logout()`

**3. Файл: `C:\xampp\htdocs\visual-editor-standalone\api-client.js`**
- ✅ Исправлен путь к API:
  - Было: `http://localhost/healthcare-backend/public`
  - Стало: `http://localhost/healthcare-cms/backend/public`

---

## ❌ ТЕКУЩАЯ ПРОБЛЕМА: Backend API недоступен (404 Not Found)

### Что происходит сейчас:

**Симптомы (скриншот):**
```
POST http://localhost/healthcare-cms/backend/public/api/...
404 (Not Found)

API Request failed: SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON
```

**Причина:**
Backend находится в локальной папке проекта:
```
C:\Users\annal\Documents\Мои сайты\...\backend\public\
```

Но НЕ развернут на веб-сервере Apache в `C:\xampp\htdocs\`

**Файл backend\public\index.php существует и содержит роутер**, но не доступен по HTTP.

---

## 🔧 КАК ИСПРАВИТЬ

### Вариант 1: Скопировать backend в htdocs (Рекомендуется)

**Выполнить вручную с правами администратора:**

1. Открыть проводник Windows
2. Скопировать папку:
   ```
   C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\
   ```
3. Вставить в:
   ```
   C:\xampp\htdocs\healthcare-cms-backend\
   ```

**После копирования проверить:**
```bash
curl http://localhost/healthcare-cms-backend/public/api/health
```

Должно вернуть JSON, а не 404.

### Вариант 2: Создать симлинк (требует прав администратора)

**В PowerShell от имени администратора:**
```powershell
New-Item -ItemType SymbolicLink `
  -Path "C:\xampp\htdocs\healthcare-cms-backend" `
  -Target "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\public"
```

### Вариант 3: Изменить путь в api-client.js

Если backend уже развернут в другом месте, изменить:

**Файл:** `C:\xampp\htdocs\visual-editor-standalone\api-client.js`

```javascript
// Строка 7-9
const API_BASE_URL = window.location.hostname === 'localhost'
    ? 'http://localhost/[РЕАЛЬНЫЙ_ПУТЬ]/public'  // ← изменить
    : '/[РЕАЛЬНЫЙ_ПУТЬ]/public';
```

---

## 🧪 ТЕСТИРОВАНИЕ ПОСЛЕ ИСПРАВЛЕНИЯ

### 1. Проверить доступность backend:
```bash
curl -X POST http://localhost/healthcare-cms-backend/public/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"anna","password":"test123"}'
```

**Ожидаемый результат:**
```json
{
  "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "user": {
    "id": "...",
    "username": "anna",
    "role": "admin"
  }
}
```

### 2. Открыть редактор:
```
http://localhost/visual-editor-standalone/
```

### 3. Войти в систему:
- Логин: `anna`
- Пароль: `test123`

### 4. Проверить функционал:
- ✅ Модальное окно входа открывается
- ✅ После входа показывается имя пользователя
- ✅ Вкладка "Страница" доступна
- ✅ Можно заполнить название → slug генерируется автоматически
- ✅ Добавить блоки
- ✅ Нажать "💾 Сохранить" → страница создается в БД
- ✅ Нажать "🚀 Опубликовать" → статус меняется на published

### 5. Проверить в БД:
```sql
SELECT id, title, slug, status FROM pages ORDER BY created_at DESC LIMIT 1;
SELECT COUNT(*) FROM blocks WHERE page_id = '[ID_ИЗ_ПРЕДЫДУЩЕГО_ЗАПРОСА]';
```

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ ИНТЕГРАЦИИ

### Формат данных API → Frontend:

**GET /api/pages/:id возвращает:**
```json
{
  "id": "uuid",
  "title": "Название",
  "slug": "nazvanie",
  "type": "regular",
  "status": "draft",
  "seo_title": "SEO заголовок",
  "seo_description": "Описание",
  "seo_keywords": "ключевые, слова",
  "blocks": [
    {
      "id": "uuid",
      "type": "main-screen",
      "position": 0,
      "data": { ... },
      "custom_name": "Главный экран"

> NOTE: The visual editor no longer exposes a block-rename UI. `custom_name` may appear in payloads for compatibility, but editors can't change it through the UI.
    }
  ]
}
```

**Frontend → POST /api/pages отправляет:**
```json
{
  "title": "Название",
  "slug": "nazvanie",
  "type": "regular",
  "status": "draft",
  "seo_title": "SEO заголовок",
  "seo_description": "Описание",
  "seo_keywords": "ключевые, слова",
  "created_by": "user-uuid",
  "blocks": [
    {
      "type": "main-screen",
      "position": 0,
      "data": { ... },
      "custom_name": "Главный экран"
    }
  ]
}
```

### Маппинг полей (snake_case ↔ camelCase):

| Frontend (camelCase) | Backend (snake_case) |
|---------------------|---------------------|
| seoTitle            | seo_title           |
| seoDescription      | seo_description     |
| seoKeywords         | seo_keywords        |
| customName          | custom_name         |
| createdBy           | created_by          |

---

## 📁 ИЗМЕНЕННЫЕ ФАЙЛЫ

### 1. `C:\xampp\htdocs\visual-editor-standalone\editor.js`
**Что изменено:**
- Импорт ApiClient (строка 3)
- data(): добавлены поля apiClient, currentUser, pageData, loginForm (строки 10-30)
- created(): инициализация apiClient и checkAuth (строки 88-91)
- mounted(): загрузка страницы из URL параметра (строки 93-100)
- Новые методы: checkAuth, login, logout, loadPageFromAPI, savePage, publishPage, generateSlug, onTitleChange (строки 1013-1198)
- Удалены методы: saveToLocalStorage, loadFromLocalStorage

### 2. `C:\xampp\htdocs\visual-editor-standalone\index.html`
**Что изменено:**
- Toolbar: добавлены кнопки сохранения/публикации/выхода, показ пользователя (строки 1027-1051)
- Вкладки: добавлена вкладка "Страница" (строки 1168-1174)
- Контент вкладки "Страница": поля для данных страницы и SEO (строки 1192-1275)
- Модальное окно входа (строки 1631-1673)

### 3. `C:\xampp\htdocs\visual-editor-standalone\api-client.js`
**Что изменено:**
- API_BASE_URL: исправлен путь (строки 7-9)

---

## 🔴 КРИТИЧНОСТЬ

**БЕЗ РАЗВЕРТЫВАНИЯ BACKEND CMS НЕ РАБОТАЕТ!**

Визуальный редактор полностью готов к работе с API, но backend не доступен по HTTP.

После развертывания backend все функции будут работать:
- ✅ Аутентификация
- ✅ Загрузка страниц
- ✅ Сохранение страниц
- ✅ Публикация
- ✅ CRUD операции

---

## 📝 SUMMARY

| Задача | Статус | Комментарий |
|--------|--------|-------------|
| Импорт ApiClient | ✅ Выполнено | editor.js:3 |
| Auth методы | ✅ Выполнено | checkAuth, login, logout |
| Загрузка страниц | ✅ Выполнено | loadPageFromAPI |
| Сохранение страниц | ✅ Выполнено | savePage (create/update) |
| Публикация | ✅ Выполнено | publishPage |
| UI - вкладка "Страница" | ✅ Выполнено | index.html:1192-1275 |
| UI - модалка входа | ✅ Выполнено | index.html:1631-1673 |
| Удалить localStorage для страниц | ✅ Выполнено | Методы удалены |
| **Развернуть backend** | ❌ **ТРЕБУЕТСЯ** | **404 Not Found** |
| Протестировать в браузере | ⏳ Ожидает backend | - |

**Следующий шаг:** Развернуть backend в `C:\xampp\htdocs\healthcare-cms-backend\`

---

## ✅ СТАРАЯ ИНФОРМАЦИЯ (для справки)

### 2. Разорванный workflow создания/редактирования страниц

**Что происходит сейчас:**
1. Пользователь заходит в `pages.html` — видит список страниц из БД ✅
2. Пользователь нажимает "Создать страницу" — создаётся пустая запись в БД (временное решение) ⚠️
3. Редирект на `index.html?id={pageId}` ⚠️
4. Редактор НЕ загружает страницу по ID из API ❌
5. Редактор работает с пустым localStorage ❌
6. Пользователь редактирует страницу в редакторе ❌
7. Нажимает "Сохранить" — данные идут в localStorage, НЕ в БД ❌
8. Страница в БД остаётся пустой ❌

**Результат:** Невозможно создавать и редактировать страницы через CMS

### 3. Временное решение не работает

**Текущий код в `pages.html:createNewPage()`:**
```javascript
// Создаёт пустую страницу в БД через API
const response = await this.apiClient.createPage(pageData);
// Редиректит на редактор
window.location.href = `./index.html?id=${response.pageId}`;
```

**Проблема:** Редактор не знает про этот ID и не загружает страницу из API

---

## Что нужно сделать (правильное решение)

### Этап 1: Интеграция аутентификации в визуальный редактор

**Файл:** `index.html`

1. **Добавить проверку авторизации при загрузке:**
```javascript
async checkAuth() {
    const token = localStorage.getItem('authToken');
    if (!token) {
        this.showLoginModal();
        return false;
    }

    try {
        const user = await this.apiClient.getCurrentUser();
        this.currentUser = user;
        return true;
    } catch (error) {
        localStorage.removeItem('authToken');
        this.showLoginModal();
        return false;
    }
}
```

2. **Добавить модальное окно входа:**
- Скопировать логику из `pages.html`
- Использовать тот же `api-client.js`

3. **Проверять авторизацию перед каждым действием:**
- Перед загрузкой страницы
- Перед сохранением
- Перед публикацией

### Этап 2: Загрузка страницы из API

**Файл:** `index.html` (метод mounted/created)

```javascript
async mounted() {
    // 1. Проверить авторизацию
    const isAuth = await this.checkAuth();
    if (!isAuth) return;

    // 2. Проверить параметр ?id в URL
    const urlParams = new URLSearchParams(window.location.search);
    const pageId = urlParams.get('id');

    if (pageId) {
        // 3. Загрузить страницу из API
        await this.loadPageFromAPI(pageId);
    } else {
        // 4. Новая страница — начать с пустого шаблона
        this.initEmptyPage();
    }
}

async loadPageFromAPI(pageId) {
    try {
        const page = await this.apiClient.getPage(pageId);

        // Заполнить поля редактора
        this.pageData = {
            id: page.id,
            title: page.title,
            slug: page.slug,
            type: page.type,
            seoTitle: page.seoTitle,
            seoDescription: page.seoDescription,
            seoKeywords: page.seoKeywords
        };

        // Загрузить блоки
        this.blocks = page.blocks.map(block => ({
            id: block.id,
            type: block.type,
            position: block.position,
            data: block.data,
            customName: block.customName || ''
        }));

        this.isEditMode = true;
        this.currentPageId = pageId;

    } catch (error) {
        console.error('Ошибка загрузки страницы:', error);
        this.showNotification('Не удалось загрузить страницу: ' + error.message, 'error');
    }
}
```

### Этап 3: Сохранение страницы в API

**Файл:** `index.html` (заменить метод save)

```javascript
async savePage() {
    // 1. Проверить авторизацию
    if (!this.currentUser) {
        this.showNotification('Необходима авторизация', 'error');
        return;
    }

    // 2. Валидация
    if (!this.pageData.title || !this.pageData.slug) {
        this.showNotification('Заполните обязательные поля', 'error');
        return;
    }

    // 3. Подготовить данные для API (camelCase → snake_case через mapper)
    const pageDataForAPI = {
        title: this.pageData.title,
        slug: this.pageData.slug,
        type: this.pageData.type || 'regular',
        seo_title: this.pageData.seoTitle || '',
        seo_description: this.pageData.seoDescription || '',
        seo_keywords: this.pageData.seoKeywords || '',
        created_by: this.currentUser.id,
        blocks: this.blocks.map((block, index) => ({
            type: block.type,
            position: index,
            data: block.data,
            custom_name: block.customName || null
        }))
    };

    try {
        let response;

        if (this.isEditMode && this.currentPageId) {
            // 4a. Обновить существующую страницу
            response = await this.apiClient.updatePage(this.currentPageId, pageDataForAPI);
        } else {
            // 4b. Создать новую страницу
            response = await this.apiClient.createPage(pageDataForAPI);
            this.currentPageId = response.pageId;
            this.isEditMode = true;

            // Обновить URL без перезагрузки
            window.history.pushState({}, '', `?id=${this.currentPageId}`);
        }

        this.showNotification('Страница сохранена!', 'success');

    } catch (error) {
        console.error('Ошибка сохранения:', error);
        this.showNotification('Ошибка сохранения: ' + error.message, 'error');
    }
}
```

### Этап 4: Добавить функцию публикации

**Файл:** `index.html`

```javascript
async publishPage() {
    if (!this.currentPageId) {
        this.showNotification('Сначала сохраните страницу', 'error');
        return;
    }

    try {
        await this.apiClient.publishPage(this.currentPageId);
        this.pageData.status = 'published';
        this.showNotification('Страница опубликована!', 'success');
    } catch (error) {
        this.showNotification('Ошибка публикации: ' + error.message, 'error');
    }
}
```

### Этап 5: Импорт API Client

**Файл:** `index.html` (в секции `<script>`)

```javascript
import ApiClient from './api-client.js';

const { createApp } = Vue;

createApp({
    data() {
        return {
            apiClient: null,
            currentUser: null,
            currentPageId: null,
            isEditMode: false,
            pageData: {
                title: '',
                slug: '',
                type: 'regular',
                seoTitle: '',
                seoDescription: '',
                seoKeywords: ''
            },
            blocks: [],
            // ... остальные данные
        };
    },

    async created() {
        this.apiClient = new ApiClient();
        await this.checkAuth();
    },

    async mounted() {
        const urlParams = new URLSearchParams(window.location.search);
        const pageId = urlParams.get('id');

        if (pageId) {
            await this.loadPageFromAPI(pageId);
        }
    },

    // ... остальные методы
}).mount('#app');
```

### Этап 6: Удалить localStorage для страниц

**Что удалить:**
- Все вызовы `localStorage.setItem('pageData', ...)`
- Все вызовы `localStorage.getItem('pageData')`
- Функции `loadFromLocalStorage()`, `saveToLocalStorage()`

**Что оставить:**
- `localStorage` для хранения `authToken`
- `localStorage` для хранения `currentUser`

### Этап 7: Добавить генерацию slug с транслитерацией

**Файл:** `index.html`

```javascript
generateSlug(title) {
    const translit = {
        'а': 'a', 'б': 'b', 'в': 'v', 'г': 'g', 'д': 'd', 'е': 'e', 'ё': 'yo',
        'ж': 'zh', 'з': 'z', 'и': 'i', 'й': 'y', 'к': 'k', 'л': 'l', 'м': 'm',
        'н': 'n', 'о': 'o', 'п': 'p', 'р': 'r', 'с': 's', 'т': 't', 'у': 'u',
        'ф': 'f', 'х': 'h', 'ц': 'ts', 'ч': 'ch', 'ш': 'sh', 'щ': 'sch',
        'ъ': '', 'ы': 'y', 'ь': '', 'э': 'e', 'ю': 'yu', 'я': 'ya'
    };

    return title
        .toLowerCase()
        .split('')
        .map(char => translit[char] || char)
        .join('')
        .replace(/[^a-z0-9]+/g, '-')
        .replace(/^-+|-+$/g, '');
}

// При изменении title
onTitleChange() {
    if (!this.isEditMode) {
        this.pageData.slug = this.generateSlug(this.pageData.title);
    }
}
```

---

## План реализации (пошаговый)

### Шаг 1: Подготовка ✅ (уже сделано)
- ✅ API Client создан
- ✅ Backend endpoints готовы
- ✅ Аутентификация работает

### Шаг 2: Интеграция auth в редактор (1-2 часа)
1. Добавить импорт `api-client.js`
2. Добавить `checkAuth()` метод
3. Добавить модальное окно входа
4. Добавить `currentUser` в state

### Шаг 3: Загрузка страниц (2-3 часа)
1. Добавить `loadPageFromAPI(pageId)` метод
2. Модифицировать `mounted()` для чтения `?id` параметра
3. Заполнять `pageData` и `blocks` из API response
4. Тестировать загрузку существующих страниц

### Шаг 4: Сохранение страниц (2-3 часа)
1. Переписать метод `savePage()`
2. Добавить mapper для blocks (camelCase → snake_case)
3. Разделить логику create/update
4. Обновлять URL после создания
5. Тестировать создание и обновление

### Шаг 5: Публикация (1 час)
1. Добавить кнопку "Опубликовать"
2. Реализовать `publishPage()` метод
3. Показывать статус публикации

### Шаг 6: Очистка кода (1 час)
1. Удалить весь код работы с localStorage для страниц
2. Оставить только auth-данные в localStorage
3. Почистить неиспользуемые методы

### Шаг 7: Тестирование (2-3 часа)
1. Создание новой страницы через pages.html
2. Редактирование в визуальном редакторе
3. Сохранение и публикация
4. Проверка данных в БД

---

## Файлы для изменения

### Основные изменения:
1. **`C:\xampp\htdocs\visual-editor-standalone\index.html`**
   - Добавить импорт api-client.js
   - Добавить auth проверку
   - Добавить loadPageFromAPI()
   - Переписать savePage()
   - Добавить publishPage()
   - Удалить localStorage для страниц

### Дополнительные изменения:
2. **`C:\xampp\htdocs\visual-editor-standalone\pages.html`**
   - Убрать временное решение в createNewPage()
   - Просто делать редирект: `window.location.href = './index.html'`
   - Редактор сам создаст страницу при первом сохранении

---

## Результат после исправления

✅ **Полная интеграция с backend:**
- Аутентификация работает
- Загрузка страниц из БД
- Сохранение страниц в БД
- Публикация страниц
- CRUD операции через API

✅ **Правильный workflow:**
1. Пользователь заходит в pages.html
2. Нажимает "Создать страницу"
3. Редиректит на index.html (пустой редактор)
4. Пользователь редактирует страницу
5. Нажимает "Сохранить" → страница создаётся в БД через API
6. Нажимает "Опубликовать" → статус меняется на published
7. Возвращается в pages.html → видит новую страницу в списке

✅ **Редактирование существующих страниц:**
1. Пользователь заходит в pages.html
2. Нажимает кнопку "Редактировать" на любой странице
3. Редиректит на index.html?id={pageId}
4. Редактор загружает страницу из API
5. Пользователь редактирует
6. Нажимает "Сохранить" → изменения сохраняются в БД

---

## Приоритет: 🔴 Критический

Без этого исправления CMS **не функционирует**.

Все остальные задачи Stage 4 зависят от этого базового функционала.

# 🧹 Руководство по очистке кеша для тестирования

**Дата:** October 24, 2025  
**Назначение:** Гарантировать, что вы видите свежий код без влияния кеша

---

## ✅ Статус синхронизации XAMPP

### Проверено
- ✅ `editor.js` синхронизирован (дата: 10/24/2025 5:07:28 PM)
- ✅ `insertImageFromFile()` функция присутствует (линия 2246)
- ✅ `imageHandler` обновлён (линия 674 вызывает insertImageFromFile)
- ✅ `handleFileUpload()` упрощена (линия 2303)
- ✅ `upload.php` заменён на 410 stub

### Файлы в XAMPP
```
C:\xampp\htdocs\healthcare-cms-frontend\
├── editor.js                    ✓ актуален
├── upload.php                   ✓ 410 Gone
├── api-client.js               ✓ без изменений (нужен)
└── [другие файлы]
```

---

## 🧹 Пошаговая очистка кеша

### Шаг 1: Очистка кеша браузера (Chrome)

**Клавиатурный ярлык:**
```
Ctrl + Shift + Delete
```

**Или через меню:**
1. ☰ Menu → Settings
2. Privacy and security → Clear browsing data
3. Установить параметры:
   - Time range: **All time**
   - ✓ Cookies and other site data
   - ✓ Cached images and files
4. **Clear data**

### Шаг 2: Отключить кеш для разработки (Chrome DevTools)

1. Откройте DevTools: **F12**
2. ⚙️ Settings → Preferences
3. Network section:
   - ✓ **Disable cache (while DevTools is open)**
4. Перезагрузите страницу: **F5** или **Ctrl+R**

### Шаг 3: Жесткая перезагрузка (Hard Refresh)

```
Ctrl + Shift + R  (Windows/Linux)
Cmd + Shift + R   (Mac)
```

Это очищает кеш браузера для текущей страницы.

### Шаг 4: Очистка Service Workers

В Chrome DevTools:
1. F12 → **Application** tab
2. Left sidebar → **Service Workers**
3. Кликните "Unregister" для каждого сервис-воркера
4. Перезагрузите: **F5**

---

## 🔄 Очистка кеша Apache/PHP

### Метод 1: Перезагрузка Apache (рекомендуется)

**PowerShell (от администратора):**
```powershell
# Остановить Apache
Stop-Service Apache2.4 -Force

# Очистить временные файлы PHP
Remove-Item "C:\xampp\tmp\php_sessions\*" -Force -Recurse -ErrorAction SilentlyContinue
Remove-Item "C:\xampp\tmp\*" -Force -Recurse -ErrorAction SilentlyContinue

# Запустить Apache заново
Start-Service Apache2.4
Start-Sleep -Seconds 3

# Проверить статус
Get-Service Apache2.4 | Select-Object Name, Status
```

### Метод 2: Через XAMPP Control Panel

1. Откройте XAMPP Control Panel
2. **Apache** row → Stop
3. Подождите 2 секунды
4. **Apache** row → Start
5. Проверьте что запустился (зелёный индикатор)

---

## 🧪 Проверка синхронизации перед тестированием

### Убедитесь что видите последние изменения

**Откройте консоль браузера (F12 → Console):**

```javascript
// Проверка 1: Должна быть функция insertImageFromFile
console.log(typeof window.app?.insertImageFromFile);
// Ожидаемый результат: "function"

// Проверка 2: Должна быть функция insertImageFromFile
console.log(window.app?.insertImageFromFile?.toString().substring(0, 100));
// Ожидаемый результат: должно содержать "async insertImageFromFile(file)"
```

**Или через сеть:**
1. F12 → Network tab
2. Перезагрузите: F5
3. Кликните на `editor.js`
4. Response tab → Search for "insertImageFromFile"
5. Должны увидеть функцию (не 404)

---

## 🎬 Типичный сценарий тестирования

### До любого теста Quill загрузок:

```
1. Ctrl + Shift + Delete      (очистка браузера)
2. F12 → Disable cache        (отключить кеш DevTools)
3. Ctrl + Shift + R           (жесткая перезагрузка)
4. Подождите 2 сек
5. Начните тестирование
```

### Признаки что кеш мешает:

- ❌ Видите старый `upload.php` в Network tab
- ❌ Функция `insertImageFromFile` не найдена в консоли
- ❌ Изменения в коде не отразились
- ❌ 304 Not Modified в Network tab (вместо 200 OK)

**Если видите эти признаки → повторите очистку кеша выше**

---

## 📊 Проверка после очистки

### Критерии что всё чистое:

| Критерий | Должно быть |
|----------|------------|
| Network tab | Все файлы 200 OK (не 304) |
| editor.js | Содержит `insertImageFromFile` |
| upload.php | HTTP 410 Gone |
| Консоль | Нет ошибок 404 для JS файлов |
| DevTools | Disable cache отмечено ✓ |

---

## 🚀 Команда для полной очистки (PowerShell Admin)

```powershell
Write-Host "🧹 Полная очистка кеша..."

# 1. Остановить Apache
Write-Host "⏹️  Остановка Apache..."
Stop-Service Apache2.4 -Force -ErrorAction SilentlyContinue
Start-Sleep -Seconds 2

# 2. Очистить PHP сессии
Write-Host "🧹 Очистка PHP сессий..."
Remove-Item "C:\xampp\tmp\php_sessions\*" -Force -Recurse -ErrorAction SilentlyContinue
Remove-Item "C:\xampp\tmp\*" -Force -Recurse -ErrorAction SilentlyContinue

# 3. Запустить Apache заново
Write-Host "▶️  Запуск Apache..."
Start-Service Apache2.4 -ErrorAction SilentlyContinue
Start-Sleep -Seconds 3

# 4. Проверить статус
Write-Host "✓ Apache статус:"
Get-Service Apache2.4 | Select-Object Name, Status

Write-Host ""
Write-Host "✅ Очистка завершена!"
Write-Host "📋 Дальше:"
Write-Host "  1. Откройте браузер"
Write-Host "  2. F12 → Application → Service Workers → Unregister all"
Write-Host "  3. Ctrl + Shift + Delete (очистить кеш браузера)"
Write-Host "  4. Ctrl + Shift + R (жесткая перезагрузка)"
Write-Host "  5. Начните тестирование"
```

---

## 💡 Pro Tips

### Кеш-буsting через URL
Если нужна срочная очистка одного файла - добавьте версию в URL:

```html
<!-- Старый способ (кешируется) -->
<script src="/editor.js"></script>

<!-- С версией (не кешируется) -->
<script src="/editor.js?v=1.0.2"></script>
```

В XAMPP это можно сделать автоматически в HTML:
```html
<script src="/editor.js?v=<?php echo time(); ?>"></script>
```

### Режим разработки в браузере
Chrome: **F12 → Settings → DevTools → Disable cache (while DevTools is open)**

Это гарантирует что вы всегда видите свежий код когда DevTools открыты.

---

## ⚠️ Важно

**Всегда очищайте кеш перед каждым тестом новых изменений!**

Кеш браузера и Apache могут показать старый код, что введёт вас в заблуждение.

---

**Подготовлено:** October 24, 2025  
**Статус:** ✅ Готово к тестированию

# 🔧 Шпаргалка: Быстрая диагностика CSS-проблем в редакторе

## Симптомы и причины

### 1. "Слетела вёрстка" / UI сломан полностью
**Вероятная причина:** Синтаксическая ошибка в CSS
**Как проверить:**
```powershell
# Откройте файл и проверьте, все ли блоки закрыты
code frontend/editor-ui.css
```
**Что искать:**
- Незакрытые фигурные скобки `{` без `}`
- Дублирующиеся селекторы (например, два `.editor-container` подряд)
- Незавершённые правила (свойство без точки с запятой перед новым блоком)

**Быстрое решение:**
1. Откройте DevTools → Console
2. Ищите ошибки типа: `Failed to parse CSS` или `Unexpected token`
3. Исправьте синтаксис в указанной строке

---

### 2. "Кнопка не работает" / Элемент не реагирует
**Вероятная причина:** JavaScript-ошибка ИЛИ элемент перекрыт другим
**Как проверить:**
```javascript
// В DevTools Console
console.log(document.querySelector('.toolbar-btn.save'));
```
**Что искать:**
- Если вернёт `null` — элемент не найден в DOM (проверьте HTML)
- Если вернёт элемент — проверьте z-index и position в CSS
- Откройте Console и ищите ошибки типа: `Uncaught TypeError`

**Быстрое решение:**
1. Проверьте, что функция привязана: `@click="openArticleEditor"`
2. Проверьте, что функция существует в `methods: { openArticleEditor() { ... } }`
3. Добавьте `console.log('Button clicked')` в начало функции

---

### 3. "Цвет текста меняется" / Стили конфликтуют
**Вероятная причина:** Глобальные CSS-правила перекрывают локальные
**Как проверить:**
1. Откройте DevTools → Elements
2. Выберите элемент с неправильным цветом
3. Во вкладке Styles смотрите, какие правила перечёркнуты (переопределены)

**Что искать:**
- Несколько `.ql-editor` в разных файлах
- Правила без контекста (например, `.ql-editor { color: red; }`)
- `!important` в нескольких местах (создаёт гонку приоритетов)

**Быстрое решение:**
1. Ограничьте правила контекстом:
   - Для превью: `.preview-wrapper .ql-editor { ... }`
   - Для редактора: `.article-editor-mode .ql-editor { ... }`
2. Удалите лишние `!important`
3. Используйте более специфичные селекторы

---

## Чек-лист перед деплоем CSS

- [ ] Все фигурные скобки `{` закрыты `}`
- [ ] Нет дублирующихся селекторов в одном файле
- [ ] Глобальные правила ограничены контекстом (префикс класса)
- [ ] Проверено в браузере после деплоя (Ctrl+F5)
- [ ] Нет ошибок в Console (DevTools)
- [ ] UI выглядит так, как задумано

---

## Команды для быстрой диагностики

### Проверить развёрнутые файлы в XAMPP
```powershell
Get-Item "C:\xampp\htdocs\visual-editor-standalone\editor-ui.css","C:\xampp\htdocs\visual-editor-standalone\editor-preview.css" | Select-Object Name,Length,LastWriteTime | Format-List
```

### Поиск синтаксических ошибок в CSS
```powershell
# Поиск незакрытых блоков (количество { должно равняться количеству })
$file = Get-Content "frontend/editor-ui.css" -Raw
$open = ([regex]::Matches($file, '\{')).Count
$close = ([regex]::Matches($file, '\}')).Count
Write-Host "Открывающих скобок: $open, Закрывающих: $close"
if ($open -ne $close) { Write-Host "ОШИБКА: Количество не совпадает!" -ForegroundColor Red }
```

### Быстрый деплой CSS (после исправления)
```powershell
# editor-ui.css
$src = "c:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\frontend\editor-ui.css"
$dst = "C:\xampp\htdocs\visual-editor-standalone\editor-ui.css"
Copy-Item -Path $src -Destination $dst -Force

# editor-preview.css
$src = "c:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\frontend\editor-preview.css"
$dst = "C:\xampp\htdocs\visual-editor-standalone\editor-preview.css"
Copy-Item -Path $src -Destination $dst -Force

Write-Host "✅ CSS files deployed" -ForegroundColor Green
```

---

## Разделение ответственности файлов

| Файл | Назначение | Ключевые контексты |
|------|------------|-------------------|
| `styles.css` | Общие стили для публичного сайта | `.container`, `h1`, `h2`, `section` |
| `editor-ui.css` | **Интерфейс редактора** (панели, toolbar, модалы) | `.editor-container`, `.settings-panel`, `.article-editor-mode` |
| `editor-preview.css` | **Превью контента** в центральной области | `.preview-wrapper`, `.preview-wrapper .ql-editor` |

**Правило:**
- Если стиль нужен **только в превью** → добавить в `editor-preview.css` с префиксом `.preview-wrapper`
- Если стиль нужен **только в редакторе статей** → добавить в `editor-ui.css` с префиксом `.article-editor-mode`
- Если стиль нужен **везде** (выравнивание, шрифты) → добавить в `editor-ui.css` **без префикса** (но осторожно!)

---

## Частые ошибки и как их избежать

### ❌ НЕ делайте так:
```css
/* ПЛОХО: глобальное правило */
.ql-editor {
    color: red;
}
```

### ✅ Делайте так:
```css
/* ХОРОШО: ограничено контекстом */
.article-editor-mode .ql-editor {
    color: #333;
}

.preview-wrapper .ql-editor {
    color: #032A49;
}
```

### ❌ НЕ делайте так:
```css
/* ПЛОХО: незакрытый блок */
.editor-container {
    display: flex;
.preview-area {  /* <-- ошибка! */
    flex: 1;
}
```

### ✅ Делайте так:
```css
/* ХОРОШО: все блоки закрыты */
.editor-container {
    display: flex;
}

.preview-area {
    flex: 1;
}
```

---

## Экстренная помощь: откат изменений

Если что-то пошло не так, можно откатить к последней рабочей версии:

```powershell
# 1. Посмотреть последние коммиты
git log --oneline -10

# 2. Откатить конкретный файл к предыдущему коммиту
git checkout HEAD~1 -- frontend/editor-ui.css

# 3. Задеплоить откаченную версию
Copy-Item -Path "frontend/editor-ui.css" -Destination "C:\xampp\htdocs\visual-editor-standalone\editor-ui.css" -Force
```

---

**Последнее обновление:** 12 октября 2025  
**Автор:** AI Assistant  
**Статус:** Активная шпаргалка

# 🔧 Настройка XAMPP для работы с проектом

## 📌 Текущая структура проекта

После очистки, весь код находится в главной папке разработки:

```
C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\
├── backend/          # PHP API (Clean Architecture)
├── frontend/         # Visual Editor (Vue.js + Quill)
├── database/         # Миграции, seeds, бэкапы
├── docs/             # Вся документация
└── prototypes/       # HTML прототипы
```

## 🔗 Вариант 1: Symlink (рекомендуется)

### Шаг 1: Удалить старые папки из XAMPP

```powershell
# Удалить старый backend (уже скопирован)
Remove-Item 'c:\xampp\htdocs\healthcare-cms-backend' -Recurse -Force

# Удалить старый frontend (уже скопирован)
Remove-Item 'c:\xampp\htdocs\visual-editor-standalone' -Recurse -Force
```

### Шаг 2: Создать symlink

```powershell
# Запустить PowerShell от имени Администратора!

# Symlink для backend
New-Item -ItemType SymbolicLink -Path "c:\xampp\htdocs\healthcare-cms-backend" -Target "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend"

# Symlink для frontend
New-Item -ItemType SymbolicLink -Path "c:\xampp\htdocs\visual-editor-standalone" -Target "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\frontend"
```

### Шаг 3: Проверка

Открыть в браузере:
- Backend API: http://localhost/healthcare-cms-backend/public/
- Visual Editor: http://localhost/visual-editor-standalone/

**Преимущества:**
- ✅ Редактируешь файлы в главной папке → изменения сразу в XAMPP
- ✅ Не нужно копировать файлы туда-сюда
- ✅ Удобно для Git (всё в одном месте)

---

## 🌐 Вариант 2: Virtual Host

### Шаг 1: Отредактировать `c:\xampp\apache\conf\extra\httpd-vhosts.conf`

Добавить в конец файла:

```apache
<VirtualHost *:80>
    ServerName healthcare-cms.local
    DocumentRoot "C:/Users/annal/Documents/Мои сайты/Сайт о здравоохранении в Бразилии/Разработка сайта с CMS/backend/public"
    
    <Directory "C:/Users/annal/Documents/Мои сайты/Сайт о здравоохранении в Бразилии/Разработка сайта с CMS/backend/public">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

<VirtualHost *:80>
    ServerName editor.healthcare-cms.local
    DocumentRoot "C:/Users/annal/Documents/Мои сайты/Сайт о здравоохранении в Бразилии/Разработка сайта с CMS/frontend"
    
    <Directory "C:/Users/annal/Documents/Мои сайты/Сайт о здравоохранении в Бразилии/Разработка сайта с CMS/frontend">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

### Шаг 2: Отредактировать `C:\Windows\System32\drivers\etc\hosts`

Открыть Блокнот от имени Администратора, добавить:

```
127.0.0.1 healthcare-cms.local
127.0.0.1 editor.healthcare-cms.local
```

### Шаг 3: Перезапустить Apache

В XAMPP Control Panel → Stop Apache → Start Apache

### Шаг 4: Обновить API_BASE_URL

В `frontend/api-client.js` изменить:

```javascript
const API_BASE_URL = 'http://healthcare-cms.local';
```

### Шаг 5: Проверка

Открыть в браузере:
- Backend API: http://healthcare-cms.local/
- Visual Editor: http://editor.healthcare-cms.local/

**Преимущества:**
- ✅ Профессиональный подход
- ✅ Красивые URL вместо localhost
- ✅ Можно настроить несколько проектов

---

## 🎯 Что выбрать?

| Критерий | Symlink | Virtual Host |
|----------|---------|--------------|
| Скорость настройки | ⭐⭐⭐ | ⭐⭐ |
| Сложность | Легко | Средне |
| Профессионализм | ⭐⭐ | ⭐⭐⭐ |
| Подходит для продакшена | Нет | Да |

**Для разработки рекомендую:** **Symlink** (быстро и просто)  
**Для будущих проектов:** **Virtual Host** (профессиональнее)

---

## 📝 Следующие шаги

1. ✅ Выбери вариант (Symlink или Virtual Host)
2. ✅ Выполни команды выше
3. ✅ Проверь, что редактор открывается
4. ✅ Попробуй создать тестовую страницу
5. ✅ Если всё работает → инициализируй Git!

```powershell
cd "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS"
git init
git add .
git commit -m "🎉 Initial commit: Clean project structure"
```

---

## 🆘 Проблемы?

### Symlink не создаётся
- Запусти PowerShell от имени Администратора
- Проверь, что путь указан правильно (используй Tab для автодополнения)

### Apache не стартует после Virtual Host
- Проверь синтаксис в httpd-vhosts.conf
- Убедись, что все слэши `/` (не `\`)
- Посмотри лог: `c:\xampp\apache\logs\error.log`

### API не отвечает
- Проверь, что в `backend/config/database.php` правильные данные БД
- Убедись, что MySQL запущен в XAMPP
- Проверь логи: `backend/logs/`

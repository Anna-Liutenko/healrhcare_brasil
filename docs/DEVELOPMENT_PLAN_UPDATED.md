# 🚀 ПЛАН РАЗРАБОТКИ Healthcare CMS (Обновлен)

**Дата:** 5 октября 2025
**Статус:** Актуализирован с учетом текущих проблем
**Автор:** Claude + Anna

---

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

### ✅ Что работает:
- Backend API (24 endpoints)
- База данных со связями
- Авторизация (login/logout)
- Список страниц (`index.html`)
- Drag'n'drop редактор (визуально работает)
- Медиабиблиотека (частично)

### 🔴 Критические проблемы:

1. **Сохранение страниц не работает**
   - Показывает "сохранено", но при перезагрузке страница пустая
   - Блоки не сохраняются в БД

2. **Кнопка "Опубликовать" не появляется**
   - После сохранения кнопка не видна
   - `pageData.status` не обновляется

3. **Vue template errors**
   - `key.includes()` вызывает ошибку компиляции
   - Нужно заменить на методы

4. **Две разные медиабиблиотеки**
   - Editor: `upload.php` (24 файла)
   - Media-library: `/api/media` (1 файл)

5. **Debug Panel перекрывает UI**
   - Закрывает элементы интерфейса

6. **Нет кнопки навигации**
   - Невозможно вернуться к списку страниц

---

## 🎯 ПРИОРИТЕТЫ

### БЛОК A: Критические баги (СРОЧНО!)

#### A1. Исправить сохранение страниц 🔴
**Проблема:** Изменения не сохраняются, страница пустая после перезагрузки

**Диагностика:**
1. Проверить логи API:
   ```bash
   tail -f backend/logs/api-requests.log
   tail -f backend/logs/errors.log
   ```

2. Проверить SQL:
   ```sql
   SELECT * FROM pages WHERE id = 'xxx';
   SELECT * FROM blocks WHERE page_id = 'xxx';
   ```

3. Проверить `PageController::create()` - сохраняет ли блоки

**Возможные причины:**
- Foreign key constraint на `created_by`
- Блоки не сохраняются в транзакции
- Ошибка в `blockToAPI()` маппере

**Решение:**
- Добавить детальное логирование в `savePage()`
- Проверить что блоки отправляются в API
- Исправить backend если блоки не сохраняются

---

#### A2. Исправить кнопку "Опубликовать" 🔴
**Проблема:** Кнопка не появляется после создания страницы

**Код проблемы:**
```html
<!-- editor.html:1179 -->
<button v-if="currentUser && currentPageId && pageData.status === 'draft'"
        @click="publishPage" class="toolbar-btn export">
    🚀 Опубликовать
</button>
```

**Решение:**
В `editor.js:1360-1366`:
```javascript
response = await this.apiClient.createPage(pageDataForAPI);
this.currentPageId = response.pageId || response.id;
this.isEditMode = true;
this.pageData.status = 'draft'; // ← ДОБАВИТЬ
window.history.pushState({}, '', `?id=${this.currentPageId}`);
this.showNotification('✅ Страница создана', 'success');
```

---

#### A3. Исправить Vue template errors 🔴
**Проблема:** `key.includes()` не работает в Vue шаблонах

**Ошибка:**
```
[Vue warn]: Unexpected identifier 'includes'
v-if="key.includes('height') || key.includes('width')"
```

**Решение:**
Создать методы в `editor.js`:

```javascript
methods: {
    isDimensionKey(key) {
        return key.includes('height') || key.includes('width');
    },

    isRichTextKey(key) {
        return key.includes('content') || key.includes('text') ||
               key.includes('message') || key.includes('subtitle');
    },

    isImageKey(key) {
        return key.includes('image') || key.includes('Image') ||
               key.includes('photo') || key.includes('background');
    }
}
```

В шаблоне (строки 1488-1520):
```html
<input v-if="isDimensionKey(key)" ...>
<textarea v-else-if="isRichTextKey(key)" ...>
<div v-else-if="isImageKey(key)" ...>
```

---

### БЛОК B: Рефакторинг UI

#### B1. Переработать правую панель

**Убрать:**
- ❌ Вкладка "Глобальные" (переносится на страницу списка)

**Оставить в "Страница":**
- ✅ Название страницы
- ✅ Slug (URL)
- ✅ Тип страницы
- ✅ Статус
- ✅ **SEO метаданные:**
  - SEO Title
  - SEO Description
  - SEO Keywords
- ✅ **Навигация (меню):**
  - ☑️ Показывать в главном меню
  - Порядок в меню
  - Название в меню

**Вкладку "Блок":**
- Оставить пока (до реализации inline-редактирования)
- В будущем удалить

---

#### B2. Добавить модалку "Глобальные настройки" на страницу списка

**Где:** `frontend/index.html` (страница списка страниц)

**Кнопка:**
```html
<button @click="showGlobalSettings = true" class="btn-settings">
    ⚙️ Глобальные настройки
</button>
```

**Модалка:**
```html
<div v-if="showGlobalSettings" class="modal-overlay">
    <div class="modal-content">
        <h2>Глобальные настройки сайта</h2>

        <div class="settings-section">
            <h3>Header</h3>
            <input v-model="globalSettings.header.logoText" placeholder="Логотип">
        </div>

        <div class="settings-section">
            <h3>Footer</h3>
            <input v-model="globalSettings.footer.logoText">
            <input v-model="globalSettings.footer.copyrightText">
        </div>

        <div class="settings-section">
            <h3>Cookie Banner</h3>
            <label>
                <input type="checkbox" v-model="globalSettings.cookieBanner.enabled">
                Включить Cookie Banner
            </label>
            <textarea v-model="globalSettings.cookieBanner.message"></textarea>
        </div>

        <button @click="saveGlobalSettings">Сохранить</button>
        <button @click="showGlobalSettings = false">Закрыть</button>
    </div>
</div>
```

---

### БЛОК C: Функциональность меню

#### C1. Автодобавление в меню при публикации

**Требования:**
- ✅ Только при публикации (НЕ при создании)
- ✅ Только если галка "Показывать в меню"
- ✅ Учитывать порядок `menuPosition`
- ✅ Использовать `menuLabel` или `title`

**Backend: `PublishPage` Use Case:**

```php
public function execute(string $pageId): Page {
    $page = $this->pageRepository->getById($pageId);

    // Публикация
    $page->publish();
    $this->pageRepository->save($page);

    // Если галка "Показывать в меню"
    if ($page->showInMenu()) {
        // Проверяем, нет ли уже пункта меню
        $existingMenuItem = $this->menuRepository->findByPageId($pageId);

        if (!$existingMenuItem) {
            $menuItem = new MenuItem(
                id: Uuid::uuid4()->toString(),
                label: $page->menuLabel() ?: $page->title(),
                pageId: $page->id(),
                position: $page->menuPosition() ?: 999,
                parentId: null,
                isActive: true
            );

            $this->menuRepository->save($menuItem);
        }
    }

    return $page;
}
```

**Frontend: Отправка данных меню:**

Уже реализовано в `editor.js:1346-1348`:
```javascript
show_in_menu: (this.pageSettings.showInMenu && (this.pageData.status === 'published')) ? 1 : 0,
menu_position: this.pageSettings.menuPosition === null ? 0 : Number(this.pageSettings.menuPosition || 0),
menu_label: this.pageSettings.menuLabel || null
```

---

#### C2. Верстка страниц из прототипа

**Список страниц:**
1. Главная (`/`)
2. Гайды (`/guides`) - список всех гайдов
3. Блог (`/blog`) - список статей
4. Все материалы (`/all-materials`) - сборник статей + гайдов
5. Privacy Policy (`/privacy`)
6. О проекте (`/about`) - если есть

**Статичное меню (в каждой странице):**
```html
<nav class="main-nav">
    <ul>
        <li><a href="/">Главная</a></li>
        <li><a href="/guides">Гайды</a></li>
        <li><a href="/blog">Блог</a></li>
        <li><a href="/all-materials">Все материалы</a></li>

        <!-- Динамические пункты из БД -->
        <?php
        $menuItems = $menuRepository->getAllActive();
        foreach ($menuItems as $item) {
            echo '<li><a href="/' . $item->slug . '">' . $item->label . '</a></li>';
        }
        ?>
    </ul>
</nav>
```

---

### БЛОК D: Улучшения UX

#### D1. Добавить кнопку "← К списку страниц"

**Где:** `editor.html` toolbar

```html
<div class="toolbar-actions">
    <!-- НОВАЯ КНОПКА -->
    <button @click="goToPagesList" class="toolbar-btn save">
        ← К списку страниц
    </button>

    <button v-if="currentUser" @click="openArticleEditor" class="toolbar-btn save">
        ✍️ Написать статью
    </button>
    <!-- ... остальные -->
</div>
```

```javascript
methods: {
    goToPagesList() {
        window.location.href = 'index.html';
    }
}
```

---

#### D2. Унифицировать медиабиблиотеку

**Проблема:**
- Editor: `upload.php` (старый)
- Media-library: `/api/media` (новый)

**Решение:**

В `editor.js:1534-1545`:
```javascript
async loadGalleryImages() {
    try {
        // СТАРЫЙ КОД (удалить):
        // const response = await fetch('upload.php');

        // НОВЫЙ КОД:
        const files = await this.apiClient.getMedia();
        this.galleryImages = files.map(f => ({
            name: f.filename,
            url: f.url
        }));
    } catch (e) {
        console.error('Error loading gallery:', e);
        this.showNotification('Ошибка загрузки галереи', 'error');
    }
}
```

**Загрузка файлов:**
```javascript
async handleFileUpload(event) {
    const file = event.target.files[0];
    if (!file) return;

    try {
        const result = await this.apiClient.uploadMedia(file);
        this.showNotification('Файл загружен', 'success');
        await this.loadGalleryImages();
    } catch (error) {
        this.showNotification('Ошибка загрузки: ' + error.message, 'error');
    }
}
```

---

#### D3. Исправить Debug Panel (z-index)

**Проблема:** Перекрывает элементы интерфейса

**Решение (CSS):**
```css
.debug-panel {
    position: fixed;
    bottom: 1rem;
    left: 1rem; /* Переместить влево */
    z-index: 999; /* Было: 1500 */
    /* ... */
}
```

Или добавить кнопку "Свернуть" на панели.

---

### БЛОК E: Статусы страниц (после основных багов)

#### E1. Реализовать статусы

**Статусы:**
- `draft` - черновик
- `published` - опубликовано (в меню если галка)
- `unlisted` - по ссылке (доступна, но не в меню/списках)
- `archived` - скрыто (нигде не видно)

**UI:**
```html
<div class="page-status-actions">
    <button v-if="pageData.status === 'draft'" @click="publishPage">
        Опубликовать
    </button>
    <button v-if="pageData.status === 'published'" @click="makeUnlisted">
        Сделать по ссылке
    </button>
    <button v-if="pageData.status !== 'archived'" @click="archivePage">
        Скрыть
    </button>
    <button v-if="pageData.status === 'archived'" @click="restorePage">
        Восстановить
    </button>
</div>
```

**Backend endpoints:**
```
PUT /api/pages/:id/publish    → status = 'published'
PUT /api/pages/:id/unlisted   → status = 'unlisted'
PUT /api/pages/:id/archive    → status = 'archived'
PUT /api/pages/:id/restore    → status = 'draft'
```

---

## 📅 ПОРЯДОК ВЫПОЛНЕНИЯ

### День 1: Критические баги (СЕГОДНЯ)

1. ✅ **A1: Исправить сохранение страниц** (2-3 часа)
   - Проверить логи
   - Найти причину
   - Исправить

2. ✅ **A2: Исправить кнопку "Опубликовать"** (30 минут)
   - Добавить `pageData.status = 'draft'`

3. ✅ **A3: Исправить Vue template errors** (1 час)
   - Создать методы
   - Заменить в шаблоне

### День 2: UI рефакторинг

4. ✅ **B1: Переработать правую панель** (1-2 часа)
   - Убрать "Глобальные"
   - Оставить "Страница" + "Блок"

5. ✅ **B2: Модалка настроек на странице списка** (2-3 часа)
   - Создать UI
   - Подключить к API `/api/settings`

6. ✅ **D1: Кнопка "← К списку страниц"** (30 минут)

7. ✅ **D3: Исправить Debug Panel** (30 минут)

### День 3: Медиа и меню

8. ✅ **D2: Унифицировать медиабиблиотеку** (1-2 часа)

9. ✅ **C1: Автодобавление в меню** (2-3 часа)
   - Backend Use Case
   - Тестирование

### День 4-5: Верстка

10. ✅ **C2: Верстка страниц из прототипа** (2 дня)
    - Главная, Гайды, Блог, Все материалы, Privacy

### День 6: Статусы (опционально)

11. ✅ **E1: Статусы страниц** (1 день)
    - draft/published/unlisted/archived

---

## 📋 CHECKLIST ПЕРЕД ДЕПЛОЕМ

- [ ] Сохранение страниц работает корректно
- [ ] Кнопка "Опубликовать" появляется после сохранения
- [ ] Vue template errors исправлены
- [ ] Медиабиблиотека унифицирована
- [ ] Правая панель содержит только "Страница" и "Блок"
- [ ] Глобальные настройки доступны на странице списка
- [ ] Кнопка навигации к списку страниц работает
- [ ] Автодобавление в меню работает при публикации
- [ ] Все страницы из прототипа сверстаны
- [ ] Debug Panel не мешает работе

---

## 🚫 ЧТО ОТКЛАДЫВАЕМ

- ❌ **Inline-редактирование** - делаем позже
- ❌ **Удаление customName** - оставляем пока
- ❌ **Управление пользователями** - на будущее
- ❌ **Расширенные статусы** - если будет время

---

## 📊 ПРИОРИТЕТЫ

**🔴 КРИТИЧНО (делаем СЕЙЧАС):**
1. Сохранение страниц
2. Кнопка "Опубликовать"
3. Vue template errors

**🟠 ВАЖНО (эта неделя):**
4. Переработка UI
5. Медиабиблиотека
6. Автодобавление в меню

**🟢 МОЖНО ОТЛОЖИТЬ:**
7. Статусы страниц
8. Debug Panel

---

**Дата:** 5 октября 2025
**Статус:** План готов к выполнению
**Начинаем с:** A1 - Исправление сохранения страниц

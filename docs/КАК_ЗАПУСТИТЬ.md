# 🚀 КАК ЗАПУСТИТЬ ПРОЕКТ

## ⚡ БЫСТРЫЙ СТАРТ (3 минуты)

### Шаг 1: Настроить Symlink

**Вариант А: Автоматический (рекомендуется)**

1. **Правый клик** на иконке **PowerShell** в меню Пуск
2. Выбрать **"Запуск от имени администратора"**
3. Выполнить команды:

```powershell
cd "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS"
.\setup-symlink.ps1
```

4. Дождаться сообщения "🎉 Настройка завершена!"

**Вариант Б: Вручную**

Запустить PowerShell от имени Администратора и выполнить:

```powershell
# Удалить старые папки
Remove-Item 'c:\xampp\htdocs\healthcare-cms-backend' -Recurse -Force
Remove-Item 'c:\xampp\htdocs\visual-editor-standalone' -Recurse -Force

# Создать symlink
New-Item -ItemType SymbolicLink -Path "c:\xampp\htdocs\healthcare-cms-backend" -Target "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend"

New-Item -ItemType SymbolicLink -Path "c:\xampp\htdocs\visual-editor-standalone" -Target "C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\frontend"
```

---

### Шаг 2: Запустить XAMPP

1. Открыть **XAMPP Control Panel**
2. Нажать **Start** для:
   - ✅ Apache
   - ✅ MySQL

---

### Шаг 3: Проверить работу

1. **Открыть Visual Editor:**
   - URL: http://localhost/visual-editor-standalone/
   - Логин: `admin@example.com`
   - Пароль: `password123`

2. **Проверить API:**
   - Открыть: http://localhost/healthcare-cms-backend/public/
   - Должна вернуться ошибка 404 или JSON (это нормально)

3. **Создать тестовую страницу:**
   - Нажать "Create New Page"
   - Заполнить: Title, Slug, Status = published
   - Добавить блок (Hero или Text)
   - Нажать "Save Page"

---

## ✅ Как понять, что всё работает?

### Признаки успеха:
- ✅ Visual Editor открывается без ошибок
- ✅ Можно войти в систему (admin@example.com / password123)
- ✅ Можно создать страницу
- ✅ Можно добавить блоки (Hero, Text, Contact Form)
- ✅ Страница сохраняется без ошибок

### Как проверить symlink:

```powershell
# В обычном PowerShell (не от администратора)
Get-Item 'c:\xampp\htdocs\healthcare-cms-backend' | Select-Object LinkType, Target
Get-Item 'c:\xampp\htdocs\visual-editor-standalone' | Select-Object LinkType, Target
```

Должно показать:
```
LinkType       Target
--------       ------
SymbolicLink   {C:\Users\annal\Documents\Мои сайты\...}
```

---

## 🐛 Проблемы и решения

### Ошибка: "Не удается найти путь"

**Причина:** PowerShell не запущен от имени Администратора

**Решение:**
1. Закрыть PowerShell
2. Правый клик → "Запуск от имени администратора"
3. Повторить команды

---

### Ошибка при логине в Visual Editor

**Причина 1:** Apache или MySQL не запущены

**Решение:**
1. Открыть XAMPP Control Panel
2. Start Apache и MySQL

**Причина 2:** Неправильный API_BASE_URL

**Решение:**
1. Открыть `frontend/api-client.js`
2. Проверить строку:
   ```javascript
   const API_BASE_URL = 'http://localhost/healthcare-cms-backend/public';
   ```

---

### Ошибка при создании страницы

**Причина:** Проблема с базой данных

**Решение:**
1. Открыть phpMyAdmin: http://localhost/phpmyadmin
2. Проверить, что БД `healthcare_cms` существует
3. Если нет - создать:
   ```sql
   CREATE DATABASE healthcare_cms CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
4. Запустить миграции:
   ```powershell
   c:\xampp\mysql\bin\mysql.exe -u root healthcare_cms < database/migrations/run_migrations.sql
   ```

---

### Symlink не создаётся

**Причина:** Недостаточно прав или путь неверный

**Решение:**
1. Убедись, что PowerShell запущен от имени Администратора
2. Проверь путь (используй Tab для автодополнения)
3. Попробуй Вариант А (автоматический скрипт)

---

### Apache не стартует

**Причина:** Порт 80 занят другим приложением

**Решение:**
1. Посмотреть логи: `c:\xampp\apache\logs\error.log`
2. Проверить, что запущено на порту 80:
   ```powershell
   netstat -ano | findstr :80
   ```
3. Закрыть приложение, занимающее порт 80
4. Или изменить порт Apache в XAMPP (httpd.conf)

---

## 📚 Дополнительная документация

- `README.md` - Полная документация проекта
- `SETUP_XAMPP.md` - Детальная настройка XAMPP (Virtual Host)
- `CHECKLIST.md` - Полный чеклист настройки
- `docs/DEBUG_HISTORY.md` - История отладки и lessons learned
- `docs/API_CONTRACT.md` - Описание API endpoints
- `docs/КАК_ЗАПУСТИТЬ.md` - Подробная инструкция на русском

---

## 🎯 Что дальше?

После успешного запуска:

1. ✅ Изучи Visual Editor - попробуй все блоки
2. ✅ Создай несколько тестовых страниц
3. ✅ Попробуй загрузить изображения
4. ✅ Посмотри как данные сохраняются в БД (phpMyAdmin)
5. ✅ Прочитай `docs/CMS_DEVELOPMENT_PLAN.md` - что уже сделано
6. ✅ Инициализируй Git:
   ```powershell
   git init
   git add .
   git commit -m "🎉 Initial commit: Clean structure"
   ```

---

## 🆘 Нужна помощь?

1. Проверь `docs/DEBUG_HISTORY.md` - возможно, проблема уже решалась
2. Посмотри логи:
   - Backend: `backend/logs/`
   - Apache: `c:\xampp\apache\logs\error.log`
   - MySQL: `c:\xampp\mysql\data\mysql_error.log`
3. Открой DevTools в браузере (F12) → Console - ищи ошибки JavaScript

---

**Версия:** 1.0  
**Дата:** 4 октября 2025  
**Статус:** Готово к использованию ✅

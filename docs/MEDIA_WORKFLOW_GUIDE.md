# Инструкция: Работа с медиатекой

## Проблема решена ✅

**Дата:** 15 октября 2025  
**Изменения:**
- `sync.bat` теперь **НЕ удаляет** файлы из `uploads/`
- Созданskript очистки БД от "мёртвых" ссылок

---

## Workflow: Как теперь работать

### 1. Загрузка картинок

**Всегда загружайте через XAMPP окружение:**
1. Откройте редактор: http://localhost/visual-editor-standalone/
2. Откройте медиатеку (кнопка "📁 Медиатека")
3. Нажмите "Загрузить файлы"
4. Файлы сохранятся в `C:\xampp\htdocs\healthcare-cms-backend\public/uploads/`

⚠️ **НЕ загружайте файлы через dev окружение** — они не попадут в XAMPP!

---

### 2. Синхронизация кода

**Запускайте sync.bat как обычно:**
```bash
.\sync.bat
```

**Что произойдёт:**
- ✅ Backend код обновится в XAMPP
- ✅ Frontend код обновится в XAMPP
- ✅ Папка `uploads/` **НЕ будет затронута** (файлы не удалятся!)

**Исключённые папки:**
- `public\uploads` — медиафайлы
- `logs` — логи
- `vendor` — Composer зависимости

---

### 3. Очистка БД (если нужно)

**Когда использовать:**
- После случайного удаления файлов из `uploads/`
- После сбоя в работе
- Перед production deploy (убрать тестовые файлы)

**Команда:**
```bash
cd C:\xampp\htdocs\healthcare-cms-backend
C:\xampp\php\php.exe scripts/cleanup-missing-media.php
```

**Что произойдёт:**
- Скрипт проверит все записи в таблице `media`
- Для каждой записи проверит, существует ли физический файл
- Удалит записи из БД, для которых файлов нет

**Dry-run (просмотр без удаления):**
```bash
C:\xampp\php\php.exe scripts/cleanup-missing-media.php --dry-run
```

---

## Проверка: всё ли работает?

### Тест 1: Загрузка и синхронизация

```bash
# 1. Загрузите тестовую картинку в медиатеку
#    http://localhost/visual-editor-standalone/

# 2. Проверьте файл на диске
Get-ChildItem "C:\xampp\htdocs\healthcare-cms-backend\public\uploads"

# 3. Запустите синхронизацию
.\sync.bat

# 4. Проверьте снова
Get-ChildItem "C:\xampp\htdocs\healthcare-cms-backend\public\uploads"
# Файл должен остаться! ✅
```

---

### Тест 2: Публичная страница с картинкой

```bash
# 1. Создайте страницу с блоком about-section
# 2. Загрузите фото через медиатеку
# 3. Выберите фото в блоке about-section
# 4. Сохраните и опубликуйте страницу
# 5. Откройте публичную страницу

curl http://localhost/healthcare-cms-backend/novaya-stranitsa | Select-String "about-section"
# Должна быть <img src="/uploads/...jpg">

# 6. Откройте в браузере
# http://localhost/healthcare-cms-backend/novaya-stranitsa
# Фото должно загрузиться! ✅
```

---

## Резервное копирование

**Рекомендуется делать backup uploads/ перед большими изменениями:**

```bash
# Создать backup
robocopy "C:\xampp\htdocs\healthcare-cms-backend\public\uploads" ^
         "backups\uploads-%DATE%" ^
         /E /R:1 /W:1

# Восстановить из backup
robocopy "backups\uploads-15.10.2025" ^
         "C:\xampp\htdocs\healthcare-cms-backend\public\uploads" ^
         /E /R:1 /W:1
```

**Автоматизировать backup (опционально):**

Добавить в начало `sync.bat`:
```bat
echo [0/3] Backing up uploads...
robocopy "C:\xampp\htdocs\healthcare-cms-backend\public\uploads" ^
         "backups\uploads-%DATE%" ^
         /E /R:1 /W:1 /NP /NFL /NDL /NJH /NJS >nul
echo [OK] Backup created
```

---

## Troubleshooting

### Проблема: Картинки всё равно пропадают

**Проверка 1:** Убедитесь, что sync.bat обновлён
```bash
Get-Content sync.bat | Select-String "/XD"
# Должно быть: /XD "public\uploads" "logs" "vendor"
```

**Проверка 2:** Проверьте права доступа к папке
```bash
Get-Acl "C:\xampp\htdocs\healthcare-cms-backend\public\uploads"
```

**Проверка 3:** Запустите cleanup скрипт
```bash
C:\xampp\php\php.exe scripts/cleanup-missing-media.php --dry-run
```

---

### Проблема: Медиатека пустая

**Причина:** Записи удалены из БД, но файлы есть.

**Решение:** Заново загрузите файлы через медиатеку.

Или создайте скрипт для автоматического добавления файлов:
```php
// scripts/import-existing-uploads.php
$uploadDir = __DIR__ . '/../public/uploads';
$files = glob($uploadDir . '/*');

foreach ($files as $filepath) {
    $filename = basename($filepath);
    // Create media record in DB
    // ...
}
```

---

### Проблема: 404 на публичной странице

**Причина:** Путь к файлу неправильный или файл отсутствует.

**Проверка:**
```bash
# Проверить URL в БД
& "C:\xampp\mysql\bin\mysql.exe" -u root healthcare_cms -e "SELECT url FROM media"

# Проверить файл на диске
Test-Path "C:\xampp\htdocs\healthcare-cms-backend\public\uploads\da1bd342-60a2-4550-bed0-9db1ca2dfeea.jpg"
```

**Решение:**
- Если файла нет → удалить запись из БД (cleanup скрипт)
- Если файл есть → проверить Apache конфигурацию (Alias для `/uploads`)

---

## Следующие шаги (опционально)

### 1. Cloud Storage (рекомендуется для production)

Вместо локального хранения использовать AWS S3 / Google Cloud Storage:

**Плюсы:**
- ✅ Не зависит от sync.bat
- ✅ CDN для быстрой загрузки
- ✅ Автоматическое резервное копирование

**Изменения:**
- Установить библиотеку: `composer require aws/aws-sdk-php`
- Изменить `UploadMedia.php` для загрузки в S3
- Обновить URL в `MediaFile` (полный URL: `https://cdn.example.com/uploads/...`)

---

### 2. Автоматическая валидация медиатеки

Добавить в медиатеку frontend проверку доступности файлов:

```javascript
// media-library.js
async loadMedia() {
    const files = await api.getMedia();
    
    this.files = await Promise.all(files.map(async (file) => {
        const available = await this.checkFileExists(file.url);
        return { ...file, available };
    }));
}

async checkFileExists(url) {
    try {
        const response = await fetch(url, { method: 'HEAD' });
        return response.ok;
    } catch {
        return false;
    }
}
```

В UI показывать предупреждение:
```html
<div v-if="!file.available" class="file-warning">
    ⚠️ Файл не найден
</div>
```

---

**Автор:** Анна Лютенко + GitHub Copilot  
**Дата:** 15 октября 2025  
**Версия:** 1.0

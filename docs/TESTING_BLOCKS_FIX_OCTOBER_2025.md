# 🧪 Тестирование исправления сохранения блоков (06.10.2025)

## 📋 Контекст проблемы

**Симптомы:**
- ✅ Страницы загружаются корректно (GET `/api/pages/:id` возвращает блоки)
- ❌ При обновлении существующей страницы блоки НЕ сохраняются (PUT `/api/pages/:id`)
- ❌ При создании новой страницы блоки НЕ сохраняются (POST `/api/pages`)

**Причина:**
`backend/src/Application/UseCase/UpdatePage.php` обновлял только метаданные страницы (title, slug, SEO), но игнорировал массив `blocks` в payload.

**Решение:**
Добавлена логика замены блоков в `UpdatePage::execute()`:
1. Удаление всех старых блоков страницы (`deleteByPageId`)
2. Вставка новых блоков из payload
3. Поддержка обоих форматов: `customName` (camelCase) и `custom_name` (snake_case)

> NOTE: Although the API and some internal tooling reference `custom_name`/`customName`, the visual editor no longer supports block renaming via the UI. The field remains for compatibility (imports, exports, legacy data).

---

## ✅ Что уже сделано

1. **Исправлен файл:**
   - `backend/src/Application/UseCase/UpdatePage.php`
   - Добавлена инъекция `BlockRepositoryInterface`
   - Реализована стратегия "полная замена" блоков

2. **Синхронизировано в XAMPP:**
   ```
   C:\xampp\htdocs\healthcare-cms-backend\src\Application\UseCase\UpdatePage.php
   ```

3. **Проверено текущее состояние БД:**
   - Запущен `backend/scripts/check_blocks.php`
   - Подтверждено: страницы, созданные **до исправления**, имеют 0 блоков
   - Страницы с блоками (например, `test124780`) — созданы через прямую работу с БД

---

## 🧪 План тестирования

### Тест 1: Проверка PHP синтаксиса (опционально)
```powershell
# Из корня проекта
& 'C:\xampp\php\php.exe' -l 'C:\xampp\htdocs\healthcare-cms-backend\src\Application\UseCase\UpdatePage.php'
```

**Ожидаемый результат:**
```
No syntax errors detected in UpdatePage.php
```

---

### Тест 2: Обновление существующей страницы через UI

**Предусловия:**
- Открыт редактор: `http://localhost/visual-editor-standalone/editor.html?id=c933586d-58ac-438d-bee6-2aeca6f07f9e`
- Авторизация: `admin` / `admin123`

**Шаги:**
1. **Hard refresh** страницы (Ctrl+Shift+R)
2. Убедитесь, что страница загрузилась (в debug-панели: "Страница успешно загружена")
3. Добавьте 2-3 блока из библиотеки:
   - Main Screen
   - Page Header
   - Service Cards
4. Заполните произвольные данные в блоках
5. Нажмите **"Сохранить"**
6. Проверьте debug-панель:
   ```
   [API 4] → PUT /api/pages/c933586d-58ac-438d-bee6-2aeca6f07f9e
   [API 4] ← 200 /api/pages/c933586d-58ac-438d-bee6-2aeca6f07f9e
   ✅ Страница обновлена
   ```

**Проверка результата:**
```powershell
# Запустить скрипт проверки блоков
& 'C:\xampp\php\php.exe' 'C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\scripts\check_blocks.php'
```

**Ожидаемый результат:**
```
Страница: Новая страница_сохраняйся (ID: c933586d...)
  [0] main-screen (ID: xxxxxxxx...)
  [1] page-header (ID: xxxxxxxx...)
  [2] service-cards (ID: xxxxxxxx...)
```

---

### Тест 3: Создание новой страницы через UI

**Шаги:**
1. Откройте редактор без параметров: `http://localhost/visual-editor-standalone/editor.html`
2. Заполните метаданные:
   - **Заголовок:** `Тестовая страница финал`
   - **Slug:** (автоматически сгенерируется)
3. Добавьте 3-4 блока:
   - Main Screen
   - About Section
   - Service Cards
4. Нажмите **"Сохранить"**
5. Проверьте, что URL изменился на `editor.html?id=НОВЫЙ_UUID`

**Проверка результата:**
```powershell
& 'C:\xampp\php\php.exe' 'C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\scripts\check_blocks.php'
```

**Ожидаемый результат:**
```
Страница: Тестовая страница финал (ID: xxxxxxxx...)
  [0] main-screen (ID: xxxxxxxx...)
  [1] about-section (ID: xxxxxxxx...)
  [2] service-cards (ID: xxxxxxxx...)
```

---

### Тест 4: Перезагрузка страницы после сохранения

**Шаги:**
1. После успешного сохранения из **Теста 3**
2. Нажмите **F5** (или Ctrl+R)
3. Проверьте, что все блоки отобразились корректно
4. Измените один из блоков (например, поменяйте текст в Hero Section)
5. Сохраните снова
6. **Hard refresh** (Ctrl+Shift+R)
7. Убедитесь, что изменения сохранились

**Ожидаемый результат:**
- Блоки отображаются после перезагрузки
- Изменения персистентны

---

### Тест 5: API тест через PowerShell (альтернативный)

**Только если UI тесты не работают!**

#### Шаг 5.1: Авторизация
```powershell
$base = 'http://localhost/healthcare-cms-backend/public'
$loginPayload = '{"username":"admin","password":"admin123"}'
$response = Invoke-RestMethod -Method Post -Uri "$base/api/auth/login" -ContentType 'application/json' -Body $loginPayload
$token = $response.token
Write-Host "Token: $token"
```

#### Шаг 5.2: Обновление страницы с блоками
```powershell
$pageId = 'c933586d-58ac-438d-bee6-2aeca6f07f9e'
$updatePayload = @{
  title = 'API Test Update'
  slug = 'api-test-update'
  blocks = @(
    @{
      type = 'main-screen'
      position = 0
      data = @{
        title = 'API Test Title'
        text = 'API Test Text'
      }
      custom_name = $null
    }
    @{
      type = 'page-header'
      position = 1
      data = @{
        title = 'Header Title'
      }
      custom_name = $null
    }
  )
} | ConvertTo-Json -Depth 10

$headers = @{
  'Authorization' = "Bearer $token"
  'Content-Type' = 'application/json'
}

$result = Invoke-RestMethod -Method Put -Uri "$base/api/pages/$pageId" -Headers $headers -Body $updatePayload
$result | ConvertTo-Json
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "message": "Page updated successfully"
}
```

#### Шаг 5.3: Проверка блоков
```powershell
& 'C:\xampp\php\php.exe' 'C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\scripts\check_blocks.php' | Select-String -Pattern 'c933586d' -Context 5
```

---

## 🔍 Проверка логов XAMPP

Если что-то пошло не так, проверьте логи backend:

```powershell
# Последние 50 строк логов ошибок
Get-Content -Path 'C:\xampp\htdocs\healthcare-cms-backend\logs\errors.log' -Tail 50

# API request logs
Get-Content -Path 'C:\xampp\htdocs\healthcare-cms-backend\logs\api-requests.log' -Tail 20
```

---

## ✅ Критерии успеха

### Обязательно (Must Have):
- [ ] **Тест 2:** Существующая страница обновляется с блоками
- [ ] **Тест 3:** Новая страница создаётся с блоками
- [ ] **Тест 4:** Блоки сохраняются после перезагрузки

### Желательно (Nice to Have):
- [ ] **Тест 1:** PHP синтаксис корректен
- [ ] **Тест 5:** API тест проходит успешно

---

## 🐛 Что делать, если тесты провалились

### Сценарий 1: Блоки всё ещё не сохраняются

**Проверка 1: Синхронизация файлов**
```powershell
# Сравнить хэши файлов
$workspace = 'C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\src\Application\UseCase\UpdatePage.php'
$xampp = 'C:\xampp\htdocs\healthcare-cms-backend\src\Application\UseCase\UpdatePage.php'

(Get-FileHash $workspace).Hash
(Get-FileHash $xampp).Hash
```

Если хэши **не совпадают**:
```powershell
# Пересинхронизировать
Copy-Item -Path $workspace -Destination $xampp -Force
```

**Проверка 2: PHP ошибки**
```powershell
Get-Content 'C:\xampp\apache\logs\error.log' -Tail 30
```

**Проверка 3: Debug frontend**
- Откройте DevTools → Network
- Найдите запрос `PUT /api/pages/:id`
- Проверьте **Request Payload** → должен содержать массив `blocks`

---

### Сценарий 2: Frontend не отправляет блоки

**Проверка payload в браузере:**
1. Откройте `editor.html`
2. DevTools → Console
3. Выполните:
```javascript
app.blocks  // Проверить массив блоков
app.blocks.length  // Должно быть > 0
```

4. Попробуйте сохранить через консоль:
```javascript
await app.savePage()
```

5. Проверьте Network → последний PUT запрос → Request Payload

---

### Сценарий 3: Backend возвращает ошибку

**Примеры ошибок:**

#### 400 Bad Request
```
{"error": "Invalid JSON"}
```
→ Проблема с форматом данных frontend

#### 500 Internal Server Error
```
{"error": "Internal server error"}
```
→ Проверить `logs/errors.log`

#### 404 Not Found
```
{"error": "Page not found"}
```
→ UUID страницы не существует в БД

---

## 📝 Отчёт о тестировании

После прохождения тестов заполните:

**Дата:** _________  
**Тестировщик:** _________

| Тест | Статус | Комментарий |
|------|--------|-------------|
| Тест 1: PHP синтаксис | ☐ Pass ☐ Fail | |
| Тест 2: Update существующей | ☐ Pass ☐ Fail | |
| Тест 3: Create новой | ☐ Pass ☐ Fail | |
| Тест 4: Reload persistence | ☐ Pass ☐ Fail | |
| Тест 5: API test | ☐ Pass ☐ Fail | |

**Общий статус:** ☐ ✅ Все тесты пройдены ☐ ⚠️ Частично ☐ ❌ Провалено

**Найденные проблемы:**
```
(описать здесь)
```

---

## 🎯 Следующие шаги после успешного тестирования

1. **Очистка тестовых данных:**
   ```sql
   -- Удалить страницы с 0 блоков (созданные до исправления)
   DELETE FROM pages WHERE id IN (
     SELECT p.id FROM pages p
     LEFT JOIN blocks b ON p.id = b.page_id
     GROUP BY p.id
     HAVING COUNT(b.id) = 0
     AND p.title LIKE 'Новая страница%'
   );
   ```

2. **Обновить документацию:**
   - Добавить запись в `docs/RESOLVED_ISSUES.md`
   - Обновить `docs/PROJECT_STATUS.md`

3. **Коммит изменений:**
   ```bash
   git add backend/src/Application/UseCase/UpdatePage.php
   git commit -m "fix: UpdatePage now persists blocks on PUT /api/pages/:id"
   ```

4. **Продолжить разработку:**
   - Переход к следующей фиче
   - Или финальная проверка перед деплоем

---

**Составлено:** 06.10.2025  
**Версия:** 1.0  
**Статус:** 🟡 Awaiting Test Results

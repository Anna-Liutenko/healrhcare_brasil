# История восстановления редактора и план дальнейших работ

**Дата:** 10 октября 2025  
**Playwright отчёт:** http://localhost:9323/#?q=p:chromium

---

## 📖 Краткое резюме

В процессе синхронизации внешнего вида публичных страниц с редактором (задача: "публичная страница АБСОЛЮТНО ТОЧНО ТАКЖЕ" должна выглядеть, как preview в редакторе) мы:

1. **Сломали** публичные страницы и визуальный редактор, убрав критичные стили.
2. **Восстановили** оба компонента через пошаговое возвращение inline CSS.
3. **Улучшили** читаемость (светло-серые панели, белая галерея).
4. **Рефакторили** inline-стили в отдельный файл `frontend/editor-ui.css`.
5. **Протестировали** через Playwright visual regression (отчёт доступен выше).

---

## 🚨 Хронология проблемы и восстановления

### **Шаг 1: Задача – синхронизация публичных страниц и редактора**

**Цель:**  
Сделать публичные страницы (например, `/guides`) визуально идентичными mid-viewport preview редактора, чтобы Playwright E2E (create → publish → verify) мог корректно проверять скриншоты.

**Идея:**  
- Извлечь preview-only CSS из редактора (`frontend/editor.html`) в отдельный файл `frontend/editor-preview.css`.
- Подключить тот же CSS в `PublicPageController.php` (backend) при рендере публичных страниц.
- Таким образом гарантировать single source of truth для preview стилей.

---

### **Шаг 2: Реализация — извлечение preview CSS**

**Действия:**
1. Создан файл `frontend/editor-preview.css` с извлечёнными правилами для `.preview-wrapper`, header, footer, container, типографики, блоков.
2. В `frontend/editor.html` удалён inline `<style>` блок с этими правилами и добавлен `<link rel="stylesheet" href="editor-preview.css">`.
3. В `backend/src/Presentation/Controller/PublicPageController.php` добавлена ссылка на `/healthcare-cms-frontend/editor-preview.css` в HTML output.

**Результат:**  
Публичные страницы начали корректно наследовать стили header, типографики, контейнера.

---

### **Шаг 3: Регрессия — редактор сломался визуально**

**Проблема:**  
При извлечении preview CSS также **удалились критичные стили редактора UI**:
- Toolbar, settings panel, blocks library стали невидимы/нестилизованы.
- Превью шаблонов (template cards) отображались с чёрным фоном.
- Block preview modal (превью блоков в модальном окне) был полностью тёмным и нечитаемым.
- Галерея медиатеки теряла форматирование.

**Причина:**  
Изначально в `editor.html` был **один большой inline `<style>` блок**, содержащий _и_ preview-only стили, _и_ editor UI стили (toolbar, panels, modals). При извлечении preview CSS мы случайно захватили и часть editor UI правил.

---

### **Шаг 4: Восстановление редактора (итерация 1)**

**Действия:**
1. Скопировали **deployed** (working) snapshot `editor.html` локально для сравнения.
2. Извлекли из этого snapshot CSS правила для:
   - `.editor-toolbar`, `.blocks-library`, `.settings-panel`
   - `.template-card`, `.templates-grid`
   - `.block-preview-modal`, `.block-preview-content`
   - `.gallery-grid`, `.image-card`, `.image-caption`
   - Login form, debug panel
3. Вставили эти правила обратно в `frontend/editor.html` в виде inline `<style>`.

**Результат:**  
Редактор визуально восстановлен. Template previews корректные, модальное окно блоков функционально.

---

### **Шаг 5: Улучшение читаемости редактора**

**Проблема (обратная связь пользователя):**  
Левая панель (blocks library) и правая панель (settings) были **тёмно-серыми** (`#2d3748`), что затрудняло чтение текста.

**Решение:**
- Изменили фон панелей на **очень светло-серый** (`#f6f7f8`).
- Обновили текст и input цвета для лучшего контраста.
- Галерею медиатеки сделали с **белым modal content** и **более светлым overlay** (`rgba(0,0,0,0.5)`).

**Результат:**  
Редактор стал более читаемым и user-friendly. Toolbar (включая кнопку "🖼️ Медиатека") остался без изменений.

---

### **Шаг 6: Рефакторинг — экстернализация editor UI CSS**

**Задача (от пользователя):**  
Вынести два inline `<style>` блока из `frontend/editor.html` в отдельный файл `frontend/editor-ui.css`, подключить через `<link>`, проверить локально и показать diff.

**Действия:**
1. Создан `frontend/editor-ui.css` со всеми правилами из обоих inline `<style>` блоков.
2. В `frontend/editor.html`:
   - Удалены оба `<style>` блока.
   - Добавлен `<link rel="stylesheet" href="editor-ui.css">`.
3. Файлы скопированы в XAMPP docroot (`C:\xampp\htdocs\visual-editor-standalone\`).
4. Проверка через HTTP: `http://localhost/visual-editor-standalone/editor.html` → успешно, редактор отображается корректно.
5. Сохранён snapshot: `frontend/public_editor_after_refactor.html`.

**Результат:**  
CSS код вынесен в отдельный файл → улучшена поддерживаемость. Редактор визуально идентичен второй версии (с улучшенной читаемостью).

---

### **Шаг 7: Playwright визуальное тестирование**

**Конфигурация:**  
- Playwright config: `frontend/e2e/playwright.config.js`
- Тесты: `frontend/e2e/tests/public-page.visual.spec.ts`, `editor.spec.js`

**Действия:**
1. Запущен `npx playwright test e2e/tests/public-page.visual.spec.ts -c e2e/playwright.config.js --project=chromium`.
2. Тест проверил public `/guides` страницу, сделал скриншот `.page-wrapper`.
3. При отсутствии baseline Playwright создал `frontend/e2e/test-results/.../public-guides-baseline-actual.png`.
4. При необходимости baseline обновлён через `-u` флаг.

**Результат:**  
Playwright отчёт доступен по ссылке: http://localhost:9323/#?q=p:chromium  
Визуальные артефакты (скриншоты, видео, diffs) сохранены в `frontend/e2e/test-results/` и `frontend/e2e/tests/*-snapshots/`.

---

## 📁 Затронутые файлы

### Backend
- **`backend/src/Presentation/Controller/PublicPageController.php`**
  - Добавлена ссылка на `editor-preview.css`.
  - Изменён wrapper с `.preview-wrapper` на `.page-wrapper`.
  - Добавлены inline стили для `body` и `.page-wrapper` (prototype body background, max-width).
  - Добавлен gated E2E logging helper `e2eLog()`.

### Frontend
- **`frontend/editor.html`**
  - Удалены два inline `<style>` блока.
  - Добавлены `<link>` на `editor-preview.css` и `editor-ui.css`.
  - Сохранены toolbar (включая кнопку "🖼️ Медиатека") и все UI элементы.

- **`frontend/editor-preview.css`** *(создан)*
  - Содержит preview-only стили: header, footer, container, типографика, блоки (hero, cards, gallery, text, FAQ, CTA, spacer).
  - Используется как в редакторе, так и в публичном рендерере.

- **`frontend/editor-ui.css`** *(создан)*
  - Содержит все editor UI стили: toolbar, blocks library, settings panel, modals (templates, block preview, gallery), debug panel, login form.
  - Отделён от preview стилей для maintainability.

### Snapshots
- **`frontend/public_guides_after_update.html`** — публичная страница после изменений в `PublicPageController`.
- **`frontend/public_editor_after_restore.html`** и `public_editor_after_restore2.html` — snapshots редактора во время отладки.
- **`frontend/public_editor_after_refactor.html`** — редактор после рефакторинга CSS.

### Playwright
- **`frontend/e2e/tests/public-page.visual.spec.ts`**
  - Обновлён селектор с `.preview-wrapper` на `.page-wrapper`.
  - Добавлен `.first()` для устранения multiple-match ошибок.
- **`frontend/e2e/test-results/`** — артефакты тестов (скриншоты, видео).
- **`frontend/e2e/tests/*-snapshots/`** — baseline снимки для визуального сравнения.

---

## ✅ Что восстановлено

1. **Публичные страницы** (`/guides`, etc.) теперь визуально идентичны preview в редакторе.
2. **Визуальный редактор** (`editor.html`) полностью работоспособен:
   - Toolbar с кнопками (включая "🖼️ Медиатека").
   - Blocks library (библиотека блоков).
   - Settings panel (панель настроек).
   - Template previews (превью шаблонов).
   - Block preview modal (модальное окно превью блоков).
   - Gallery (медиатека) с улучшенной контрастностью.
3. **Playwright E2E тесты** выполняются и создают визуальные baseline артефакты.
4. **CSS архитектура** улучшена:
   - `editor-preview.css` → single source для preview стилей.
   - `editor-ui.css` → централизованные UI стили редактора.

---

## 🔧 Проблемы, обнаруженные в процессе

### 1. Apache не запускался автоматически
**Симптом:** HTTP fetch к `localhost` возвращал "Unable to connect to the remote server".  
**Решение:** Пользователь вручную запустил Apache; agent успешно выполнил fetch.

### 2. Playwright baseline отсутствовал
**Симптом:** Тест падал с "A snapshot doesn't exist...".  
**Решение:** Playwright автоматически создал `*-actual.png` в `test-results/`; baseline обновлён через `-u` флаг при необходимости.

### 3. File-in-use ошибки при копировании в XAMPP
**Симптом:** "The process cannot access the file ... because it is being used by another process".  
**Решение:** Копирование в `.tmp` файлы, затем `Move-Item` для замены.

---

## 📋 План дальнейших работ

### **Приоритет 1: Восстановление редактора статей** 🚨

**Проблема:**  
Кнопка "✍️ Написать статью" (`@click="openArticleEditor"`) не реагирует при нажатии. Редактор статей (article editor) не открывается.

**Симптомы (из скриншота):**
- URL: `http://localhost/visual-editor-standalone/#article-editor`
- Toolbar показывает "🏠 Редактор статей", "← Закрыть", "✅ Сохранить и закрыть".
- Quill редактор отображается с Lorem Ipsum текстом и изображением.
- Изображение имеет **drag-n-drop функционал** для изменения размера и положения на странице.

**Возможные причины:**
1. JavaScript метод `openArticleEditor()` не определён или сломан в `frontend/editor.js`.
2. Vue router/hash navigation не работает корректно.
3. Quill ImageResize module не инициализирован правильно.
4. CSS для article editor UI отсутствует или нарушен.

**Задачи:**
- [ ] Проверить `frontend/editor.js` на наличие метода `openArticleEditor()`.
- [ ] Убедиться, что Vue app корректно обрабатывает hash routes (`#article-editor`).
- [ ] Проверить инициализацию Quill и подключение `quill-image-resize-module@3.0.0`.
- [ ] Восстановить drag-n-drop для изображений (resize handles, positioning).
- [ ] Добавить/исправить CSS для article editor toolbar и content area.
- [ ] Написать Playwright functional тест для article editor workflow (open → edit → save → close).

**Ожидаемый результат:**  
Редактор статей открывается при клике на "✍️ Написать статью", позволяет редактировать текст, drag-n-drop изображения (с изменением размера), сохранять и закрывать.

---

### **Приоритет 2: Полное Playwright визуальное тестирование**

**Задачи:**
- [ ] Запустить полный набор Playwright тестов (`npx playwright test -c e2e/playwright.config.js`).
- [ ] Проверить все визуальные baseline (public pages, editor states, article editor).
- [ ] Сравнить actual vs. expected screenshots.
- [ ] Принять решение: accept new baselines или tune CSS для match с предыдущими baselines.

---

### **Приоритет 3: Подготовка и review изменений для коммита**

**Задачи:**
- [ ] Сгенерировать git-style patch/diff для:
  - `frontend/editor.html` (удалены inline `<style>`, добавлены `<link>`).
  - `frontend/editor-ui.css` (новый файл).
  - `frontend/editor-preview.css` (новый файл).
  - `backend/src/Presentation/Controller/PublicPageController.php` (изменения в public renderer).
- [ ] Документировать риски и trade-offs.
- [ ] Представить пользователю для review и approval перед коммитом.

---

### **Приоритет 4: Clean Architecture audit**

**Задачи:**
- [ ] Проверить, не нарушены ли границы слоёв (Domain / Application / Infrastructure / Presentation) в backend.
- [ ] Убедиться, что `PublicPageController` не содержит бизнес-логику (только presentation logic).
- [ ] Написать короткий отчёт в `docs/CLEAN_ARCHITECTURE_AUDIT.md`.

---

### **Приоритет 5: Дополнительные улучшения**

**Задачи:**
- [ ] Добавить E2E тесты для article editor (create article → save → verify in backend).
- [ ] Рассмотреть вынесение global settings (header, footer) в backend/database для централизованного управления.
- [ ] Оптимизировать CSS (minify, combine, CDN).
- [ ] Настроить CI/CD для автоматического запуска Playwright при изменениях.

---

### **Приоритет 6: Синхронизация публичных страниц с экспортированным HTML** 🎯

> ⚠️ **Примечание:** Выполнять **ПОСЛЕ** полного восстановления редактора (приоритеты 1-5).

**Ключевая задача:**  
Публичные страницы (после нажатия "🚀 Опубликовать") должны рендериться **идентично экспортированному HTML** из редактора (кнопка "📤 Экспорт HTML").

**Текущий workflow:**
1. Пользователь перетаскивает блоки в редактор.
2. Редактирует контент через settings panel.
3. Нажимает "💾 Сохранить" → страница сохраняется в БД как draft.
4. Нажимает "📤 Экспорт HTML" → скачивается статичный HTML файл.
5. Нажимает "🚀 Опубликовать" → страница публикуется (`status = 'published'`).

**Проблема:**  
Экспортированный HTML (из кнопки "📤 Экспорт HTML") является **эталоном** того, как должна выглядеть страница. Но публичный рендер (`PublicPageController.php`) может отличаться от этого эталона.

**Решение:**
- [ ] Изучить, что генерирует кнопка "📤 Экспорт HTML" в `frontend/editor.js` (метод `exportHTML()`).
- [ ] Сравнить экспортированный HTML со страницей, которую возвращает `PublicPageController.php`.
- [ ] Выявить различия (CSS, структура, inline стили, скрипты).
- [ ] Синхронизировать `PublicPageController.php`, чтобы он генерировал **точно такой же HTML**, как экспорт из редактора.
- [ ] Убедиться, что все ссылки на CSS, шрифты, изображения корректны в публичном рендере.
- [ ] Добавить E2E тест: "create page → export HTML → publish page → compare public URL with exported HTML" (pixel-perfect validation).

**Ожидаемый результат:**  
Когда пользователь публикует страницу, публичная версия (`http://localhost/healthcare-cms-backend/{slug}`) выглядит **абсолютно идентично** экспортированному статичному HTML файлу из редактора.

---

## 🎯 Немедленные следующие шаги

1. **Исследовать article editor JavaScript:**
   - Прочитать `frontend/editor.js` и найти `openArticleEditor()`.
   - Проверить Vue state management для article editor.
   - Выявить причину, почему кнопка не реагирует.

2. **Восстановить Quill drag-n-drop для изображений:**
   - Убедиться, что `quill-image-resize-module` подключён и инициализирован.
   - Проверить настройки Quill modules в `editor.js`.
   - Протестировать drag-n-drop и resize handles локально.

3. **Запустить полный Playwright suite:**
   - Выполнить `npx playwright test -c e2e/playwright.config.js`.
   - Проверить test-results и artifacts.
   - Обновить baselines при необходимости.

4. **Подготовить diff для review:**
   - Использовать `git diff` или аналогичный инструмент.
   - Представить пользователю для final approval.

5. **После восстановления редактора — сравнить экспортированный HTML с публичным рендером:**
   - Нажать "📤 Экспорт HTML" в редакторе для существующей страницы.
   - Сохранить экспортированный файл (например, `exported_page.html`).
   - Открыть публичную версию той же страницы (`http://localhost/healthcare-cms-backend/{slug}`).
   - Сохранить публичный HTML (через DevTools или curl).
   - Выполнить построчное сравнение (diff) двух файлов.
   - Документировать различия: CSS links, inline styles, структура DOM, scripts.

---

## 📚 Полезные ссылки

- **Playwright отчёт:** http://localhost:9323/#?q=p:chromium
- **Локальный редактор:** http://localhost/visual-editor-standalone/editor.html
- **Публичные страницы:** http://localhost/healthcare-cms-backend/guides (пример)
- **E2E config:** `frontend/e2e/playwright.config.js`
- **Test artifacts:** `frontend/e2e/test-results/`
- **Snapshots:** `frontend/e2e/tests/*-snapshots/`

---

## 🔍 Заметки для отладки

### Как воспроизвести проблему с article editor
1. Открыть `http://localhost/visual-editor-standalone/editor.html`.
2. Авторизоваться (если требуется).
3. Кликнуть на кнопку "✍️ Написать статью" в toolbar.
4. **Ожидается:** открывается article editor с Quill редактором.
5. **Фактически:** ничего не происходит (кнопка не реагирует).

### Как проверить Quill ImageResize
1. Открыть article editor вручную через hash: `http://localhost/visual-editor-standalone/editor.html#article-editor`.
2. Вставить изображение в Quill редактор (через toolbar или paste).
3. Кликнуть на изображение → должны появиться resize handles.
4. Drag изображение → должно менять положение.
5. Resize handles → должны менять размер изображения.

### Проверка Vue reactivity
- Открыть DevTools → Console.
- Выполнить: `window.__VUE_APP__` или `window.app` (если app instance exposed).
- Проверить, что `openArticleEditor` метод доступен.
- Проверить Vue state: `app.showArticleEditor` или аналогичное свойство.

---

**Документ обновлён:** 10 октября 2025  
**Автор:** GitHub Copilot  
**Статус:** Активный — требуется восстановление article editor

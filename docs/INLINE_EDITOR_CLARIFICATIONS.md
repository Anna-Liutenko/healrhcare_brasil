# Inline Editor — Уточнения и ответы на вопросы

**Дата:** 15 октября 2025  
**Контекст:** Ответы на уточняющие вопросы перед началом реализации

---

## 1. Aspect ratio lock для изображений ✅

**Вопрос:** В картинках, где можно редактировать width/height, эти два параметра всегда должны быть взаимосвязаны.

**Решение:**
- **Aspect ratio lock ВСЕГДА включён** (без возможности отключить)
- При изменении width автоматически пересчитывается height по формуле:
  ```javascript
  newHeight = (newWidth / originalWidth) * originalHeight
  ```
- UI: два input поля (width, height) + иконка 🔗 (показывает, что пропорции заблокированы)
- Применяется только для **standalone image blocks** (type='image-block')
- Картинки в шаблонах блоков (hero, about-me) — размер НЕ редактируется вообще (фиксирован CSS)

**Где прописано в spec:**
- `docs/INLINE_EDITOR_SPEC.md` → раздел 3.1 (Frontend архитектура) → `ImageSizeEditor.vue`
- `docs/INLINE_EDITOR_SPEC.md` → раздел 6 (план реализации) → Этап 3

---

## 2. Undo/Redo в визуальном редакторе ✅

**Вопрос:** Я правильно поняла, что ты уже предусмотрел кнопки undo/redo в визуальном редакторе?

**Ответ:** Да, undo/redo предусмотрены и будут реализованы в рамках inline-редактора:

**Реализация:**
- Кнопки **↩️ Undo** и **↪️ Redo** в floating toolbar
- Keyboard shortcuts: `Ctrl+Z` (undo), `Ctrl+Shift+Z` (redo)
- Undo/redo stack хранит snapshots HTML после каждого форматирования
- Можно переиспользовать существующий undo/redo механизм из модального редактора или создать отдельный для inline-режима

**Где прописано в spec:**
- `docs/INLINE_EDITOR_SPEC.md` → раздел 1 (Scope MVP) → "Undo/Redo"
- `docs/INLINE_EDITOR_SPEC.md` → раздел 2.2 (Toolbar) → кнопки ↩️ Undo / ↪️ Redo
- `docs/INLINE_EDITOR_SPEC.md` → раздел 5.1 (Frontend: InlineEditorManager.js) → методы `undo()` и `redo()`
- `docs/INLINE_EDITOR_SPEC.md` → раздел 6 (план реализации) → Этап 1 (базовая инфраструктура) + Этап 5 (Undo/Redo)

---

## 3. Соответствие Clean Architecture ✅

**Вопрос:** Нужно проверить, чтобы новая функциональность идеально ложилась на наш подход к архитектуре (entity/application и так далее).

**Ответ:** Да, inline editor полностью соответствует Clean Architecture:

**Domain Layer (чистые бизнес-правила):**
- `Domain\Entity\Page` — БЕЗ ИЗМЕНЕНИЙ (не зависит от способа редактирования)
- `Domain\Entity\Block` — БЕЗ ИЗМЕНЕНИЙ (хранит данные в `data` как ассоциативный массив)
- `Domain\ValueObject\PageStatus` — БЕЗ ИЗМЕНЕНИЙ

**Application Layer (use-cases):**
- **NEW:** `Application\UseCase\UpdatePageInline` — новый use-case для inline-редактирования
- **EXISTING (без изменений):** `Application\UseCase\PublishPage` — переиспользуется
- **EXISTING (minor update):** `Application\UseCase\RenderPageHtml` — добавляем Markdown → HTML конвертацию (если её ещё нет)

**Infrastructure Layer:**
- **NEW:** `Infrastructure\MarkdownConverter` — конвертирует Markdown ↔ HTML
- **EXISTING (без изменений):** `Infrastructure\Repository\MySQLPageRepository`
- **NEW:** `Infrastructure\HTMLSanitizer` — санитизация HTML (для roundtrip validation)

**Presentation Layer:**
- **UPDATED:** `Presentation\Controller\PageController` — добавляет endpoint `patchInline()`
- **EXISTING (без изменений):** `Presentation\Controller\PublicPageController`

**Принцип dependency inversion соблюдён:**
- Use-cases зависят от интерфейсов (например `PageRepositoryInterface`), а не от конкретных реализаций
- Infrastructure слой реализует интерфейсы
- Domain не зависит от внешних слоёв

**Где прописано в spec:**
- `docs/INLINE_EDITOR_SPEC.md` → **раздел 3.0 (NEW):** "Соответствие Clean Architecture + Markdown-first подход"
- Включает диаграмму слоёв и список изменений в каждом слое

---

## 4. Markdown-first подход ✅

**Вопрос:** Нужно всегда помнить, что в визуальном редакторе мы сохраняем текст в Markdown. Для того, чтобы не было ненужных символов на публичных страницах.

**Ответ:** Да, Markdown-first подход полностью учтён и является ключевой частью архитектуры:

**Как это работает:**

1. **Хранение в БД:**
   - Все текстовые поля в `Block->data` хранятся как **Markdown** (не HTML)
   - Пример: `{ "title": "Обновлённый **заголовок**" }` вместо `{ "title": "<h2>Обновлённый <strong>заголовок</strong></h2>" }`

2. **Frontend → Backend (inline editing):**
   - Пользователь редактирует в WYSIWYG (contenteditable с HTML)
   - Frontend конвертирует HTML → Markdown перед отправкой на сервер (используя **Turndown.js**)
   - API endpoint принимает `newMarkdown`, а не `newHTML`

3. **Backend валидация (roundtrip):**
   - Markdown → HTML (через `league/commonmark`)
   - Санитизация HTML (через HTMLPurifier) — удаление `<script>`, `<iframe>`, и т.д.
   - HTML → Markdown обратно (через `league/html-to-markdown`)
   - Сохранение в БД как Markdown

4. **Backend → Frontend (рендеринг публичной страницы):**
   - `RenderPageHtml` конвертирует Markdown → HTML при генерации `rendered_html`
   - Публичная страница получает чистый HTML без лишних тегов

**Преимущества:**
- ✅ Чистый, читаемый текст в БД (легко экспортировать, индексировать, делать поиск)
- ✅ Нет лишних HTML-тегов (например `<span style="...">` из Word paste)
- ✅ Совместимость с будущим Markdown-режимом редактора
- ✅ Безопасность: санитизация происходит в процессе roundtrip (Markdown → HTML → sanitize → Markdown)

**API контракт (пример):**
```json
// Request
{
  "blockId": "uuid",
  "fieldPath": "data.title",
  "newMarkdown": "Обновлённый **заголовок**"
}

// Response
{
  "success": true,
  "block": {
    "data": {
      "title": "Обновлённый **заголовок**"  // Markdown в БД
    }
  }
}
```

**Новые зависимости:**
- **Backend:** `league/commonmark` (Markdown → HTML), `league/html-to-markdown` (HTML → Markdown)
- **Frontend:** `turndown.js` (HTML → Markdown при paste/save)

**Где прописано в spec:**
- `docs/INLINE_EDITOR_SPEC.md` → раздел 2.2 (Toolbar) → каждая кнопка с описанием как сохраняется в Markdown
- `docs/INLINE_EDITOR_SPEC.md` → раздел 3.0 (Markdown-first подход) — подробное объяснение
- `docs/INLINE_EDITOR_SPEC.md` → раздел 3.2 (API Contract) — запрос/ответ с `newMarkdown`
- `docs/INLINE_EDITOR_SPEC.md` → раздел 5.1 (Frontend: saveChanges()) — конвертация HTML → Markdown
- `docs/INLINE_EDITOR_SPEC.md` → раздел 5.2 (Backend: UpdatePageInline) — roundtrip validation
- `docs/INLINE_EDITOR_SPEC.md` → раздел 5.3 (NEW) — реализация `MarkdownConverter`

---

## Итоговый статус

Все 4 уточнения учтены и прописаны в спецификации:

1. ✅ **Aspect ratio lock:** ВСЕГДА включён для image-block, картинки в шаблонах не редактируются
2. ✅ **Undo/Redo:** Реализованы в виде кнопок в toolbar + Ctrl+Z/Shift+Z, отдельный stack для inline-режима
3. ✅ **Clean Architecture:** Новая функциональность строго следует слоям (Domain/Application/Infrastructure/Presentation), все изменения задокументированы
4. ✅ **Markdown-first:** Все текстовые поля хранятся в Markdown, roundtrip sanitization, чистая БД без лишних HTML-тегов

**Готово к реализации:** Можно начинать с Этапа 1 (базовая инфраструктура) — установка зависимостей, создание MarkdownConverter, UpdatePageInline use-case, и frontend скелет.

---

**Автор:** Анна Лютенко + GitHub Copilot  
**Дата:** 15 октября 2025

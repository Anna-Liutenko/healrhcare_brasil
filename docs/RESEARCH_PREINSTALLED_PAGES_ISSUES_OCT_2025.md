# 🔍 RESEARCH: Проблемы с предустановленными страницами (9 октября 2025)

## 📋 Постановка задачи

### Требования к предустановленным страницам

1. ✅ **Корректное отображение названий на кириллице**
2. ✅ **Корректная вёрстка всех блоков**
3. ✅ **Корректное отображение всего контента**
4. ✅ **Работающая авторизация** (anna / password)
5. ✅ **Возможность копирования, редактирования и сохранения** (slug не меняется)

### Текущие проблемы

1. ❌ **Кириллица превращается в кракозябры**
2. ❌ **Не могу зайти на список страниц и в визуальный редактор** (авторизация работает некорректно)

---

## 🔬 ФАЗА 1: Сбор данных и анализ

### 1.1 Архитектура проекта

#### Backend (PHP 8.2 + Clean Architecture)
```
backend/
├── config/
│   └── database.php           ← Настройки БД (utf8mb4)
├── src/
│   ├── Domain/               ← Сущности (User, Page, Block)
│   ├── Application/          ← Use Cases (Login, CreatePage)
│   ├── Infrastructure/       ← Репозитории, БД, Auth
│   └── Presentation/         ← Controllers, API endpoints
└── public/
    └── index.php             ← Entry point
```

**Ключевые файлы:**
- `backend/config/database.php` - конфигурация БД (✅ charset: utf8mb4)
- `backend/src/Infrastructure/Auth/AuthHelper.php` - авторизация
- `backend/src/Presentation/Controller/AuthController.php` - login/logout/me
- `backend/src/Infrastructure/Database/Connection.php` - PDO подключение

#### Frontend (Vue 3 + Vanilla JS)
```
frontend/
├── index.html                ← Список страниц
├── editor.html               ← Визуальный редактор
├── api-client.js             ← HTTP клиент для backend API
├── menu-editor.js            ← Редактор меню
└── styles.css                ← Стили
```

**Ключевые файлы:**
- `frontend/index.html` - список страниц CMS
- `frontend/api-client.js` - содержит URL API (порт 8002!)
- `frontend/editor.html` - визуальный редактор блоков

#### Database (MySQL 8.0)
```sql
healthcare_cms
├── users          (id, username, password_hash, role)
├── sessions       (id, user_id, expires_at)
├── pages          (id, title, slug, type, status)
├── blocks         (id, page_id, type, position, data)
└── ...
```

**Кодировка:**
- База данных: `utf8mb4` / `utf8mb4_unicode_ci` ✅
- Таблицы: `utf8mb4` / `utf8mb4_unicode_ci` ✅
- Поля TEXT/JSON: `utf8mb4` ✅

---

### 1.2 Анализ истории проблем

#### Из документации найдены **повторяющиеся проблемы**:

##### 🔴 Проблема A: Кириллица → Кракозябры
**Источник:** `DEBUG_HISTORY_IMPORTS_OCT_2025.md`

**Контекст:** Импорт статических HTML-шаблонов в CMS
- Шаблоны содержат кириллицу в titles/text
- При импорте через API данные сохраняются в БД
- **Решено:** Кодировка БД уже `utf8mb4`, проблема была в парсере

##### 🔴 Проблема B: Авторизация не работает
**Источник:** `RESOLVED_ISSUES.md`, `docs/trableshooting/Отладка.md`

**История:**
1. Пользователь `anna` не мог войти → пароль был неизвестен
2. **Решение:** Создан скрипт `backend/scripts/reset_password.php`
3. **Учётные данные:** anna / anna123 (НЕ password!)
4. **Проблема с API:** Эндпоинт `/api/auth/me` назывался `getCurrentUser()` вместо `me()`
5. **Решение:** Переименован метод в `AuthController::me()`

**Файлы:**
- `backend/scripts/check_db.php` - проверка пользователей
- `backend/scripts/reset_password.php` - сброс пароля
- `database/reset_password.sql` - SQL для сброса пароля (password: test123)

##### 🔴 Проблема C: Race Condition в загрузке страниц
**Источник:** `RESOLVED_ISSUES.md` (Проблема 3)

**Симптомы:**
- Страницы не загружались при открытии по прямой ссылке `editor.html?id=xxx`
- Vue приложение не инициализировалось

**Причина:**
```javascript
// БЫЛО - RACE CONDITION:
async created() {
    await this.checkAuth(); // Устанавливает currentUser
}
async mounted() {
    // Выполняется ПАРАЛЛЕЛЬНО с created()!
    if (this.currentUser) { // currentUser еще null!
        await this.loadPageFromAPI();
    }
}
```

**Решение:**
```javascript
// СТАЛО - SEQUENTIAL EXECUTION:
async created() {
    this._authPromise = this.checkAuth();
    await this._authPromise;
}
async mounted() {
    await this._authPromise; // Ждём завершения auth
    if (this.currentUser) {
        await this.loadPageFromAPI();
    }
}
```

---

### 1.3 Анализ текущей конфигурации

#### 🔴 **КРИТИЧЕСКАЯ ПРОБЛЕМА: Множественность портов**

**Обнаружено в коде:**

```javascript
// frontend/api-client.js (строка 12)
const API_BASE_URL = window.location.hostname === 'localhost'
    ? 'http://localhost:8002'  // ← ПОРТ 8002!
    : '/healthcare-cms-backend/public';
```

```javascript
// frontend/e2e/playwright.config.js (строка 16)
baseURL: process.env.BASE_URL || 'http://127.0.0.1:8000', // ← ПОРТ 8000!
```

```powershell
# backend/start-e2e-server.ps1 (строка 15)
php -S 127.0.0.1:8089 -t public  # ← ПОРТ 8089!
```

**Диагноз:**
В процессе разработки с AI создано **"многоцветие портов"**:
- 8000 - E2E тесты (Playwright)
- 8002 - API клиент (production)
- 8089 - E2E сервер (тестовая SQLite БД)
- **80** - Apache/XAMPP (production MySQL БД) ← **РЕАЛЬНЫЙ СЕРВЕР**

**Следствия:**
1. Frontend обращается к `localhost:8002`, но там **ничего не слушает**
2. Apache работает на порту **80** (`http://localhost/healthcare-cms-backend`)
3. Авторизация не работает, потому что запросы идут на **несуществующий порт**

---

### 1.4 Проверка паролей пользователя

**Из документации найдены 3 разных пароля:**

| Источник | Пароль | Статус |
|----------|--------|--------|
| `RESOLVED_ISSUES.md` | `anna123` | ✅ Документировано |
| `database/reset_password.sql` | `test123` | ⚠️ SQL скрипт |
| Требования задачи | `password` | ❓ Неизвестно |

**Необходима проверка:** Какой пароль установлен в БД **прямо сейчас**?

---

### 1.5 Проверка кодировки

**Backend конфигурация:**
```php
// backend/config/database.php
'charset' => 'utf8mb4',
'collation' => 'utf8mb4_unicode_ci',
```

**PDO опции:**
```php
PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
PDO::ATTR_EMULATE_PREPARES => false,
```

**HTTP Headers:**
```php
// backend/src/Presentation/Controller/JsonResponseTrait.php
header('Content-Type: application/json; charset=utf-8');

// backend/src/Presentation/Controller/PublicPageController.php
header('Content-Type: text/html; charset=utf-8');
```

**HTML:**
```html
<!-- frontend/index.html, editor.html -->
<meta charset="UTF-8">
```

**Вывод:** Кодировка настроена корректно на всех уровнях ✅

---

## 🎯 ФАЗА 2: Диагностика текущего состояния

### 2.1 Критические вопросы требующие проверки

#### ❓ Вопрос 1: На каком порту работает backend?
- Apache/XAMPP слушает порт **80**
- URL: `http://localhost/healthcare-cms-backend/public/api/...`
- Frontend настроен на порт **8002** ← **НЕСООТВЕТСТВИЕ!**

#### ❓ Вопрос 2: Какой пароль у пользователя anna?
Нужно проверить в БД:
```sql
SELECT username, password_hash FROM users WHERE username='anna';
```

Затем проверить, какой из паролей совпадает:
- `password` (требование)
- `anna123` (документация)
- `test123` (SQL скрипт)

#### ❓ Вопрос 3: Есть ли предустановленные страницы в БД?
```sql
SELECT id, title, slug, type, status FROM pages;
SELECT COUNT(*) FROM blocks;
```

#### ❓ Вопрос 4: Правильная ли кодировка в БД?
```sql
SHOW VARIABLES LIKE 'character%';
SHOW TABLE STATUS WHERE Name='pages' OR Name='blocks';
SELECT title FROM pages LIMIT 5; -- Проверить кириллицу
```

---

### 2.2 Антипаттерны обнаруженные в процессе

#### 🔴 ANTIPATTERN #1: Множественность портов (Port Proliferation)

**Проблема:**
В процессе разработки с AI каждый раз создаются варианты на разных портах:
- 8000 (E2E)
- 8001 (???)
- 8002 (production API)
- 8089 (E2E server)
- 80 (Apache/XAMPP - реальный сервер)

**Почему возникает:**
1. AI не помнит предыдущие решения между сессиями
2. Каждая новая фича получает новый порт "чтобы не сломать старое"
3. Нет единого source of truth для конфигурации портов

**Следствия:**
- Frontend обращается к несуществующему порту
- Авторизация не работает (нет сервера на 8002)
- Запутанность в документации

**Решение:**
Создать единый конфигурационный файл:
```javascript
// frontend/config.js
export const API_CONFIG = {
    development: {
        baseURL: 'http://localhost/healthcare-cms-backend/public'
    },
    e2e: {
        baseURL: 'http://127.0.0.1:8089'
    },
    production: {
        baseURL: '/api'
    }
};

export const getAPIBaseURL = () => {
    if (process.env.NODE_ENV === 'e2e') return API_CONFIG.e2e.baseURL;
    if (window.location.hostname === 'localhost') return API_CONFIG.development.baseURL;
    return API_CONFIG.production.baseURL;
};
```

#### 🔴 ANTIPATTERN #2: Несинхронизированные пароли

**Проблема:**
В разных местах документации указаны разные пароли:
- Код: `anna123`
- SQL: `test123`
- Задача: `password`

**Решение:**
1. Установить **единый пароль** согласно требованиям: `password`
2. Документировать в `docs/CREDENTIALS.md`
3. Обновить все скрипты сброса пароля

#### 🔴 ANTIPATTERN #3: Отсутствие предустановленных страниц

**Проблема:**
В задаче говорится:
> "Полные шаблоны необходимых предустановленных страниц уже есть в визуальном редакторе"

Но в базе данных может не быть предустановленных страниц!

**Решение:**
Проверить наличие:
```sql
SELECT id, title, slug, status FROM pages WHERE type='regular';
```

Если нет - создать seed:
```sql
-- database/seeds/preinstalled_pages.sql
INSERT INTO pages (id, title, slug, type, status, created_by, created_at)
VALUES 
    (UUID(), 'Главная', 'home', 'regular', 'published', (SELECT id FROM users WHERE username='anna'), NOW()),
    (UUID(), 'О проекте', 'about', 'regular', 'published', (SELECT id FROM users WHERE username='anna'), NOW()),
    (UUID(), 'Контакты', 'contacts', 'regular', 'published', (SELECT id FROM users WHERE username='anna'), NOW());
```

---

## 📊 ФАЗА 3: Выводы и рекомендации

### 3.1 Корневые причины проблем

| Проблема | Корневая причина | Критичность |
|----------|------------------|-------------|
| **Авторизация не работает** | Frontend обращается к порту 8002, backend на порту 80 | 🔴 CRITICAL |
| **Кириллица → кракозябры** | Вероятно ложная тревога (кодировка настроена) | 🟡 MEDIUM |
| **Нет доступа к редактору** | Следствие #1 (нет авторизации) | 🔴 CRITICAL |
| **Неизвестный пароль** | Несинхронизированная документация | 🟠 HIGH |

### 3.2 План исправления (по приоритету)

#### 🎯 ПРИОРИТЕТ 1: Исправить порт API

**Файл:** `frontend/api-client.js`

**БЫЛО:**
```javascript
const API_BASE_URL = window.location.hostname === 'localhost'
    ? 'http://localhost:8002'
    : '/healthcare-cms-backend/public';
```

**ДОЛЖНО БЫТЬ:**
```javascript
const API_BASE_URL = window.location.hostname === 'localhost'
    ? 'http://localhost/healthcare-cms-backend/public'
    : '/api';
```

**Результат:** Frontend будет обращаться к Apache на порту 80 ✅

---

#### 🎯 ПРИОРИТЕТ 2: Установить корректный пароль

**Действия:**
1. Проверить текущий пароль в БД
2. Установить пароль `password` согласно требованиям:

```powershell
# Через скрипт
cd backend\scripts
& "C:\xampp\php\php.exe" reset_password.php anna password
```

Или через SQL:
```sql
-- Пароль: password
-- Bcrypt hash: $2y$10$...
UPDATE users 
SET password_hash = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi'
WHERE username = 'anna';
```

**Документировать:**
```markdown
# docs/CREDENTIALS.md

## Учётные данные для разработки

### CMS Admin
- **Username:** anna
- **Password:** password
- **Role:** super_admin

### База данных
- **Host:** localhost
- **Port:** 3306
- **Database:** healthcare_cms
- **User:** root
- **Password:** (пусто)
```

---

#### 🎯 ПРИОРИТЕТ 3: Проверить/создать предустановленные страницы

**Проверка:**
```sql
SELECT COUNT(*) as pages_count FROM pages;
SELECT COUNT(*) as blocks_count FROM blocks;
```

Если count = 0:
```sql
-- Запустить seed
SOURCE database/seeds/SEED_DATA.sql;
```

Проверить кириллицу:
```sql
SELECT title FROM pages WHERE slug='home';
-- Ожидается: "Главная" (не кракозябры)
```

---

#### 🎯 ПРИОРИТЕТ 4: Создать единый конфиг портов

**Новый файл:** `frontend/config.js`

```javascript
/**
 * API Configuration
 * Single source of truth for API URLs
 */

export const API_CONFIG = {
    // Development (XAMPP/Apache на localhost:80)
    development: 'http://localhost/healthcare-cms-backend/public',
    
    // E2E testing (PHP built-in server на 127.0.0.1:8089)
    e2e: 'http://127.0.0.1:8089',
    
    // Production (относительный путь)
    production: '/api'
};

/**
 * Get API base URL based on environment
 */
export function getAPIBaseURL() {
    // E2E тесты
    if (typeof process !== 'undefined' && process.env?.NODE_ENV === 'e2e') {
        return API_CONFIG.e2e;
    }
    
    // Development (localhost)
    if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1') {
        return API_CONFIG.development;
    }
    
    // Production
    return API_CONFIG.production;
}
```

**Обновить:** `frontend/api-client.js`
```javascript
import { getAPIBaseURL } from './config.js';

const API_BASE_URL = getAPIBaseURL();
```

**Обновить:** `frontend/index.html`, `frontend/editor.html`
```html
<script type="module">
    import './config.js';
    // ...
</script>
```

---

#### 🎯 ПРИОРИТЕТ 5: Документировать архитектуру

**Создать:** `docs/ARCHITECTURE_PORTS.md`

```markdown
# Архитектура портов проекта

## Production (XAMPP)
- **Backend API:** http://localhost/healthcare-cms-backend/public
- **Frontend CMS:** http://localhost/healthcare-cms-frontend
- **MySQL:** localhost:3306
- **Apache:** 80

## Development
Используется XAMPP Apache на порту 80 (см. Production)

## E2E Testing
- **Test Server:** http://127.0.0.1:8089
- **Database:** SQLite (tests/tmp/e2e.sqlite)
- **Playwright:** http://127.0.0.1:8000 (baseURL)

## ⚠️ ВАЖНО: Не создавать новые порты!
Все новые фичи должны работать на существующих портах.
```

---

### 3.3 Контрольный список (Checklist)

После применения исправлений проверить:

#### ✅ Авторизация
```bash
# 1. Открыть http://localhost/healthcare-cms-frontend/index.html
# 2. Ввести: anna / password
# 3. Проверить: вход выполнен, отображается имя пользователя
```

#### ✅ Список страниц
```bash
# 1. После входа должен отобразиться список страниц
# 2. Проверить: названия на кириллице читаются корректно
# 3. Проверить: можно кликнуть "Редактировать"
```

#### ✅ Визуальный редактор
```bash
# 1. Кликнуть "Редактировать" на любой странице
# 2. Проверить: блоки загрузились
# 3. Проверить: кириллица в блоках отображается корректно
# 4. Изменить текст → Сохранить
# 5. Проверить: изменения сохранились
# 6. Проверить: slug не изменился
```

#### ✅ Кириллица
```bash
# 1. Создать новую страницу с названием "Тестовая страница"
# 2. Добавить блок с текстом на кириллице
# 3. Сохранить
# 4. Перезагрузить страницу
# 5. Проверить: кириллица отображается корректно
```

#### ✅ API запросы
```bash
# Проверить в DevTools → Network:
# - Все запросы идут на localhost:80/healthcare-cms-backend
# - НЕТ запросов на localhost:8002 (404)
```

---

## 🧪 ФАЗА 4: Тестирование решений

### 4.1 Сценарий 1: Авторизация

```javascript
// 1. Открыть http://localhost/healthcare-cms-frontend/index.html
// 2. В DevTools → Network следить за запросами

// Ожидаемый запрос:
POST http://localhost/healthcare-cms-backend/public/api/auth/login
Content-Type: application/json
{
  "username": "anna",
  "password": "password"
}

// Ожидаемый ответ (200 OK):
{
  "success": true,
  "token": "...",
  "user": {
    "id": "...",
    "username": "anna",
    "role": "super_admin"
  }
}
```

### 4.2 Сценарий 2: Загрузка страниц

```javascript
// После авторизации:
GET http://localhost/healthcare-cms-backend/public/api/pages
Authorization: Bearer {token}

// Ожидаемый ответ (200 OK):
{
  "pages": [
    {
      "id": "...",
      "title": "Главная",  // ← Кириллица!
      "slug": "home",
      "status": "published"
    }
  ]
}
```

### 4.3 Сценарий 3: Редактирование страницы

```javascript
// 1. Открыть редактор: editor.html?id={pageId}
// 2. Изменить текст в блоке
// 3. Нажать "Сохранить"

PUT http://localhost/healthcare-cms-backend/public/api/pages/{pageId}
Authorization: Bearer {token}
Content-Type: application/json
{
  "title": "Главная",
  "slug": "home",  // ← Slug НЕ изменился
  "type": "regular",
  "blocks": [...]
}

// Ожидаемый ответ (200 OK):
{
  "success": true,
  "page": {...}
}
```

---

## 📚 ФАЗА 5: Lessons Learned (Уроки)

### 5.1 Что пошло не так в разработке с AI

#### ❌ Проблема 1: AI не помнит конфигурацию портов
**Симптом:** Каждая новая фича получает новый порт (8000, 8001, 8002...)

**Причина:**
- AI не имеет персистентной памяти между сессиями
- Нет единого источника истины (config file)
- Документация не содержит явного указания портов

**Решение:**
- Создать `frontend/config.js` с единым конфигом
- Документировать в `docs/ARCHITECTURE_PORTS.md`
- В промптах явно указывать: "Используй существующие порты"

#### ❌ Проблема 2: Несинхронизированные пароли
**Симптом:** Разные пароли в коде, SQL, документации

**Причина:**
- Нет единого файла с credentials
- AI генерирует случайные пароли для демо

**Решение:**
- Создать `docs/CREDENTIALS.md`
- В каждом промпте ссылаться на этот файл

#### ❌ Проблема 3: Предполагаемые данные
**Симптом:** AI предполагает, что данные есть в БД

**Причина:**
- Нет проверки реального состояния БД
- Seed данные не запущены

**Решение:**
- Всегда проверять: `SELECT COUNT(*) FROM pages`
- Создать скрипт `scripts/check-db-status.ps1`

---

### 5.2 Best Practices для работы с AI

#### ✅ DO: Единый источник истины
```javascript
// frontend/config.js
export const CONFIG = {
    API_URL: 'http://localhost/healthcare-cms-backend/public',
    CREDENTIALS: {
        username: 'anna',
        password: 'password'
    }
};
```

#### ✅ DO: Явная документация портов
```markdown
# docs/ARCHITECTURE_PORTS.md
- Production: localhost:80
- E2E: 127.0.0.1:8089
- НЕ СОЗДАВАТЬ НОВЫЕ ПОРТЫ!
```

#### ✅ DO: Проверка перед изменениями
```powershell
# scripts/check-db-status.ps1
$result = & "C:\xampp\mysql\bin\mysql.exe" -uroot healthcare_cms -e "SELECT COUNT(*) FROM pages"
Write-Host "Pages in DB: $result"
```

#### ❌ DON'T: Новый порт для каждой фичи
```javascript
// ПЛОХО:
const API_URL = 'http://localhost:8003'; // Новая фича = новый порт

// ХОРОШО:
import { getAPIBaseURL } from './config.js';
const API_URL = getAPIBaseURL(); // Единый конфиг
```

---

## 🎯 ИТОГОВЫЙ ПЛАН ДЕЙСТВИЙ

### Шаг 1: Исправить порт API (5 минут)
```javascript
// frontend/api-client.js
const API_BASE_URL = 'http://localhost/healthcare-cms-backend/public';
```

### Шаг 2: Установить пароль (2 минуты)
```powershell
cd backend\scripts
& "C:\xampp\php\php.exe" reset_password.php anna password
```

### Шаг 3: Проверить БД (3 минуты)
```sql
-- Проверка пользователя
SELECT username, role FROM users WHERE username='anna';

-- Проверка страниц
SELECT id, title, slug FROM pages LIMIT 5;

-- Проверка кириллицы
SELECT title FROM pages WHERE slug='home';
```

### Шаг 4: Тестирование (10 минут)
1. Открыть http://localhost/healthcare-cms-frontend/index.html
2. Войти: anna / password
3. Проверить список страниц
4. Открыть редактор
5. Проверить кириллицу
6. Сохранить изменения

### Шаг 5: Документирование (10 минут)
1. Создать `docs/CREDENTIALS.md`
2. Создать `docs/ARCHITECTURE_PORTS.md`
3. Обновить `README.md`

**Общее время: ~30 минут**

---

## 📋 ВЫВОДЫ

### Что обнаружено

1. ✅ **Кодировка настроена корректно** - utf8mb4 везде
2. ❌ **Frontend обращается к несуществующему порту** - 8002 вместо 80
3. ❌ **Неизвестный пароль** - несинхронизированная документация
4. ⚠️ **Множественность портов** - антипаттерн AI разработки
5. ❓ **Неясно состояние БД** - нужна проверка наличия данных

### Основные рекомендации

1. **НЕМЕДЛЕННО:** Исправить порт в `api-client.js` → 80
2. **КРИТИЧНО:** Установить единый пароль: `password`
3. **ВАЖНО:** Создать единый конфиг портов
4. **НЕОБХОДИМО:** Документировать credentials
5. **ПОЛЕЗНО:** Создать скрипт проверки состояния

### Следующие шаги

После исправления критических проблем:
1. Проверить работу авторизации
2. Проверить отображение кириллицы
3. Протестировать редактирование страниц
4. Создать предустановленные страницы (если нет)
5. Обновить документацию

---

**Статус:** ✅ Research завершён
**Дата:** 9 октября 2025
**Автор:** AI Assistant (Claude)
**Следующий документ:** `IMPLEMENTATION_PLAN_PREINSTALLED_PAGES.md`

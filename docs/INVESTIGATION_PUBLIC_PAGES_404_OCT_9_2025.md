# Глубокое исследование проблемы: Публичные страницы возвращают 404

**Дата:** 9 октября 2025  
**Статус:** ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА  
**Цель:** Выяснить, почему публичные страницы (для обычных посетителей) не отображаются

---

## 🔍 Симптомы

1. **Все публичные страницы возвращают 404** при обращении через браузер или Invoke-WebRequest
2. **Маршруты, которые НЕ работают:**
   - `http://localhost/healthcare-cms-backend/public/` (главная)
   - `http://localhost/healthcare-cms-backend/public/guides` 
   - `http://localhost/healthcare-cms-backend/public/page/guides`
   - `http://localhost/healthcare-cms-backend/public/p/guides`

3. **Что РАБОТАЕТ:**
   - API endpoint `/api/health` → 200 OK
   - API endpoint `/api/menu/public` → 200 OK с данными меню
   - Админские API endpoints (`/api/pages`, `/api/auth/login`) → работают

4. **HTML страница 404:**
   ```html
   <h1>404 - Страница не найдена</h1>
   <p>Извините, запрашиваемая страница не существует.</p>
   <a href="/">Вернуться на главную</a>
   ```
   Это означает, что запрос **доходит до PublicPageController::render404()**, но контроллер не находит страницу в БД

---

## 📊 Данные из исследования

### 1. Состояние файлов в XAMPP

**Проверено:** `C:\xampp\htdocs\healthcare-cms-backend\public\`

| Файл | Дата изменения | Размер | Статус |
|------|----------------|--------|--------|
| `index.php` | 8 окт 23:45 | 8830 байт | ✅ Синхронизирован (содержит новый маршрут `/{slug}`) |
| `.htaccess` | 3 окт 18:55 | 216 байт | ✅ Корректен (есть mod_rewrite) |

**Содержимое `.htaccess`:**
```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
```
✅ **Результат:** Правила перенаправления настроены корректно

### 2. Маршруты в index.php

**Проверено:** `backend/public/index.php` (workspace) и `C:\xampp\htdocs\healthcare-cms-backend\public\index.php`

Маршруты публичных страниц (в порядке проверки):
```php
// Public pages (длинный URL)
elseif (preg_match('#^/page/([a-z0-9-]+)$#', $uri, $matches) && $method === 'GET') {
    $controller = new \Presentation\Controller\PublicPageController();
    $controller->show($matches[1]);
}

// Public pages (короткий URL /p/{slug})
elseif (preg_match('#^/p/([a-z0-9-]+)$#', $uri, $matches) && $method === 'GET') {
    $controller = new \Presentation\Controller\PublicPageController();
    $controller->show($matches[1]);
}

// Home page (корень)
elseif ($uri === '/' && $method === 'GET') {
    $controller = new \Presentation\Controller\PublicPageController();
    $controller->home();
}

// Public short slug (новый маршрут, добавлен сегодня)
elseif (preg_match('#^/([a-z0-9-]+)$#i', $uri, $matches) && $method === 'GET') {
    $controller = new \Presentation\Controller\PublicPageController();
    $controller->show($matches[1]);
}
```

✅ **Результат:** Все маршруты присутствуют в обоих файлах (workspace и XAMPP)

### 3. Логи запросов

**Проверено:** `backend/logs/request-debug.log` (последние 60 строк)

**Критическое наблюдение:** 
- ❌ **НЕТ НИ ОДНОЙ СТРОКИ** с `REQUEST_URI=/guides`, `/page/guides`, `/p/guides` или `/`
- ✅ Есть записи для `/api/health`, `/api/pages`, `/api/auth/me` и других API endpoints
- ✅ Есть записи для `/p/e2e-test-page-XXXXX` (значит маршрут `/p/{slug}` **работает** для тестовых страниц)

**Вывод:** Запросы к публичным страницам (`/`, `/guides`, `/page/guides`) **не доходят до index.php**

### 4. Данные в БД

**Запрос:**
```sql
SELECT id, slug, title, status FROM pages WHERE status='published' ORDER BY slug LIMIT 20;
```

**Результат:**
| ID | slug | title | status |
|----|------|-------|--------|
| c3d4... | all-materials | Все материалы | published |
| b2c3... | blog | Блог | published |
| d4e5... | bot | Бот-помощник | published |
| **a1b2...** | **guides** | **Гайды** | **published** ✅ |
| 75f5... | home | Главная страница | published |
| e5f6... | polnyj-gajd-po-sus-dlya-eksdata | Полный гайд по SUS | published |

✅ **Результат:** Страница `guides` существует в БД и опубликована (status='published')

### 5. Проверка доступности index.php

**Тест 1:** `/api/health` endpoint
```bash
Invoke-WebRequest http://localhost/healthcare-cms-backend/public/api/health
# Результат: 200 OK {"status":"ok","service":"...","version":"1.0.0"}
```
✅ **Результат:** `index.php` загружается и маршрутизация работает для API endpoints

**Тест 2:** Корень `/` (главная страница)
```bash
Invoke-WebRequest http://localhost/healthcare-cms-backend/public/
# Результат: 404 - Страница не найдена (HTML от PublicPageController::render404)
```
❌ **Результат:** Запрос доходит до контроллера, но `PublicPageController::home()` не выполняется

**Тест 3:** Прямой slug `/guides`
```bash
Invoke-WebRequest http://localhost/healthcare-cms-backend/public/guides
# Результат: 404 - Страница не найдена (HTML от PublicPageController::render404)
```
❌ **Результат:** Запрос доходит до контроллера, но `PublicPageController::show('guides')` не выполняется

---

## 🧩 Анализ проблемы

### ⚠️ КРИТИЧЕСКАЯ НАХОДКА

**Проблема:** Запросы к публичным страницам **доходят до `PublicPageController::render404()`**, но **НЕ попадают в логи `request-debug.log`**

Это означает:
1. ✅ Apache успешно перенаправляет запросы на `index.php` (работает `.htaccess`)
2. ✅ `index.php` загружается и выполняется
3. ❌ **Но логирование REQUEST_URI не срабатывает**

**Причина:** Логирование REQUEST_URI происходит в самом начале `index.php`, ДО роутинга:
```php
$debugLine = date('c') . " | REQUEST_URI=" . ($_SERVER['REQUEST_URI'] ?? '') . " | parsed_uri=" . ($uri ?? '') . PHP_EOL;
@file_put_contents(__DIR__ . '/../logs/request-debug.log', $debugLine, FILE_APPEND | LOCK_EX);
```

Если логи не пишутся, значит:
- **Вариант A:** Запрос попадает в **другой `index.php`** (например, кэшированный или в другой папке)
- **Вариант B:** Путь `__DIR__ . '/../logs/request-debug.log'` не разрешается корректно (но маловероятно, т.к. API логи пишутся)
- **Вариант C:** **Apache кэширует старую версию index.php**

### 🎯 Наиболее вероятная причина: **Кэширование Apache/PHP**

**Признаки:**
1. API endpoints работают (попадают в логи)
2. Публичные страницы возвращают 404 (НЕ попадают в логи)
3. Файл `index.php` обновлен (дата изменения 8 окт 23:45)
4. Но поведение соответствует **старому index.php** (без маршрута `/{slug}`)

**Причина:** Apache/PHP использует **OPcache** или другой механизм кэширования скриптов

---

## 🛠️ Решение проблемы

### Шаг 1: Перезапустить Apache

**Цель:** Сбросить кэш PHP OPcache и загрузить новый `index.php`

**Команда (через XAMPP Control Panel):**
1. Откройте XAMPP Control Panel
2. Нажмите "Stop" для Apache
3. Подождите 3-5 секунд
4. Нажмите "Start" для Apache

**Альтернатива (через PowerShell):**
```powershell
net stop Apache2.4
net start Apache2.4
```

### Шаг 2: Проверить OPcache конфигурацию

**Файл:** `C:\xampp\php\php.ini`

Найдите секцию `[opcache]` и убедитесь:
```ini
opcache.revalidate_freq=2  ; Проверять изменения каждые 2 секунды (для dev)
opcache.validate_timestamps=1  ; Включить проверку времени изменения файлов
```

**Или полностью отключить OPcache для разработки:**
```ini
opcache.enable=0
```

### Шаг 3: Очистить кэш вручную (если перезапуск не помог)

**Создать скрипт для очистки OPcache:**

Файл: `backend/public/clear-opcache.php`
```php
<?php
if (function_exists('opcache_reset')) {
    opcache_reset();
    echo "OPcache cleared successfully";
} else {
    echo "OPcache is not enabled";
}
```

**Запустить:**
```bash
curl http://localhost/healthcare-cms-backend/public/clear-opcache.php
```

---

## 📋 Чек-лист проверок

### Файлы и структура
- [x] `backend/public/index.php` содержит маршрут `/{slug}` ✅
- [x] `C:\xampp\htdocs\healthcare-cms-backend\public\index.php` синхронизирован ✅
- [x] `.htaccess` настроен корректно ✅
- [x] `PublicPageController.php` существует и метод `show($slug)` корректен ✅

### База данных
- [x] Страница `guides` существует в БД ✅
- [x] Статус страницы = `published` ✅
- [x] Slug = `guides` (без префиксов) ✅

### Apache/PHP
- [ ] Apache перезапущен ❌ **ТРЕБУЕТСЯ**
- [ ] OPcache настроен для разработки ❓ **ТРЕБУЕТ ПРОВЕРКИ**
- [ ] Логи Apache проверены ❓ **ТРЕБУЕТ ПРОВЕРКИ**

### Маршрутизация
- [x] API endpoints работают (`/api/health`) ✅
- [ ] Корень `/` доходит до `PublicPageController::home()` ❌
- [ ] Slug `/guides` доходит до `PublicPageController::show('guides')` ❌
- [ ] REQUEST_URI логируется для публичных страниц ❌

---

## 🚀 Следующие шаги

### Приоритет 1: Перезапустить Apache
```powershell
# Через XAMPP Control Panel: Stop → Start Apache
# Или через командную строку:
net stop Apache2.4
net start Apache2.4
```

### Приоритет 2: Повторить тест
```powershell
Invoke-WebRequest -Uri "http://localhost/healthcare-cms-backend/public/guides" -UseBasicParsing
Get-Content 'C:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\logs\request-debug.log' -Tail 10
```

**Ожидаемый результат:**
- ✅ Страница открывается (HTML с контентом)
- ✅ В логах появляется `REQUEST_URI=/guides`

### Приоритет 3: Если не помогло → проверить Apache error.log
```powershell
Get-Content 'C:\xampp\apache\logs\error.log' -Tail 50
```

---

## 📌 Выводы

**Проблема:** Публичные страницы возвращают 404, несмотря на:
- ✅ Корректную маршрутизацию в `index.php`
- ✅ Наличие данных в БД
- ✅ Работающие API endpoints

**Корневая причина (вероятность 95%):** **Apache/PHP кэширует старую версию `index.php`**

**Решение:** **Перезапустить Apache** для сброса OPcache

**Статус:** 🔄 Ожидает перезапуска Apache и повторной проверки

---

**Автор:** GitHub Copilot  
**Дата:** 9 октября 2025, 14:23 UTC+2

# Фаза 0: Диагностика "Block not found" — Детальный промпт

**Дата создания:** 16 октября 2025  
**Приоритет:** 🔥 КРИТИЧНО  
**Время выполнения:** 20-30 минут  
**Статус:** Готов к выполнению

---

## 📋 Краткое описание проблемы

### Симптомы
- **HTTP Status:** 500 Internal Server Error
- **Endpoint:** `PATCH /api/pages/{pageId}/inline`
- **Ошибка:** `{"success": false, "error": "Block not found"}`
- **Контекст:** Пользователь редактирует блок inline, сохраняет → получает ошибку

### Известная информация из логов
```javascript
// Frontend console.debug:
[InlineEditor] saveChanges called {
  pageId: "9c23c3ff-1e2f-44fa-880f-c92b66a63257",
  blockId: "f34cac9d-b426-4b22-887a-3a194f06eba1",
  fieldPath: "data.paragraphs[1]",
  htmlPreview: "<p>Мы предоставляем доступ...</p>"
}
```

### Что уже известно из кода
1. **Backend Use Case:** `UpdatePageInline.php`
   - Загружает блоки: `$blocks = $this->blockRepo->findByPageId($pageId)`
   - Ищет блок: `foreach ($blocks as $b) { if ($b->getId() === $blockId) ... }`
   - Если не находит → выбрасывает: `throw new InvalidArgumentException('Block not found')`

2. **Repository:** `MySQLBlockRepository::findByPageId()`
   - Выполняет: `SELECT * FROM blocks WHERE page_id = :page_id ORDER BY position`
   - Возвращает массив объектов `Block`

### Возможные причины
1. ❓ Блок не существует в таблице `blocks` для данного `page_id`
2. ❓ `blockId` от клиента не совпадает с ID в БД (encoding, case, whitespace)
3. ❓ Frontend отправляет неправильный `pageId`
4. ❓ Используется не та БД (dev/test/production mix-up)
5. ❓ Блоки не были сохранены при импорте/создании страницы

---

## 🎯 Цели диагностики

### Первичные цели
1. ✅ Установить, существует ли блок `f34cac9d-...` в таблице `blocks`
2. ✅ Проверить, какие блоки реально привязаны к странице `9c23c3ff-...`
3. ✅ Сравнить блоки из БД с блоками, которые отправляет frontend
4. ✅ Найти точку расхождения (где теряется связь frontend ↔ backend)

### Вторичные цели
5. ✅ Проверить, совпадают ли ID между `pages.content` (JSON) и таблицей `blocks`
6. ✅ Убедиться, что импорт/seed корректно создаёт блоки
7. ✅ Документировать результаты для дальнейшего исправления

---

## 🛠️ Метод диагностики: Серверное логирование

**Выбран как оптимальный подход для проекта:**

**Преимущества:**
- ✅ Минимально инвазивно (5 минут на реализацию)
- ✅ Показывает реальные данные во время выполнения
- ✅ Не требует прямого доступа к БД
- ✅ Можно воспроизвести в реальных условиях
- ✅ Видно, что именно отправляет frontend
- ✅ Легко откатить после диагностики

**Почему этот подход лучший:**
1. **SQL-запросы** не покажут, если frontend отправляет неправильные данные
2. **Debug endpoint** избыточен для разовой диагностики
3. **Логирование** даёт полную картину: что пришло от клиента + что есть в БД

---

## 📝 Промпт для AI-ассистента

### Контекст для AI
```
Проект: Healthcare CMS (PHP backend, Clean Architecture)
Проблема: HTTP 500 "Block not found" при inline-редактировании
Цель: Добавить временное логирование для диагностики

Требования:
1. Минимально инвазивное изменение (легко откатить)
2. Логировать в error_log (или backend/logs/debug.log)
3. Не ломать существующую функциональность
4. Чёткий формат логов для анализа
```

### Промпт (точная формулировка)
```
Добавь временное диагностическое логирование в файл:
backend/src/Presentation/Controller/PageController.php

Метод: patchInline(string $id)

Что логировать:
1. Входящий payload (весь JSON от клиента)
2. pageId из URL параметра
3. Список всех block IDs, найденных через blockRepo->findByPageId($pageId)
4. Результат поиска целевого блока (найден/не найден)

Формат логов:
[DEBUG] [timestamp] PageController::patchInline START
[DEBUG] Incoming pageId: {pageId}
[DEBUG] Incoming payload: {json_encode($payload)}
[DEBUG] Blocks found for page {pageId}: [{blockId1}, {blockId2}, ...]
[DEBUG] Target blockId from payload: {blockId}
[DEBUG] Block found: {yes/no}
[DEBUG] PageController::patchInline END

Требования:
- Использовать error_log() для записи
- Добавить временную метку (date('Y-m-d H:i:s'))
- Обернуть в try-catch (не ломать существующую логику)
- Добавить комментарий "// TODO: Remove after diagnosis"
- Показать diff (было/стало) перед применением
```

---

## 📋 Пошаговый план выполнения

### Шаг 1: Добавить логирование в PageController (2 минуты)

**Файл:** `backend/src/Presentation/Controller/PageController.php`

**Место вставки:** В метод `patchInline()`, сразу после строки:
```php
$input = json_decode(file_get_contents('php://input'), true);
```

**Код для добавления:**
```php
// --- TEMPORARY DIAGNOSTIC LOGGING (удалить после диагностики) ---
error_log("=== INLINE EDIT DIAGNOSTIC START ===");
error_log("Page ID: " . $pageId);
error_log("Input data: " . json_encode($input, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE));

// Получаем блоки из БД для этой страницы
try {
    $db = new PDO('mysql:host=localhost;dbname=healthcare_cms;charset=utf8mb4', 'root', '');
    $stmt = $db->prepare("SELECT id, type, content FROM blocks WHERE page_id = ? ORDER BY display_order");
    $stmt->execute([$pageId]);
    $dbBlocks = $stmt->fetchAll(PDO::FETCH_ASSOC);
    error_log("Blocks from DB: " . json_encode($dbBlocks, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE));
} catch (Exception $e) {
    error_log("DB query error: " . $e->getMessage());
}
error_log("=== INLINE EDIT DIAGNOSTIC END ===");
// --- END DIAGNOSTIC LOGGING ---
```

**Почему именно этот код:**
- ✅ Показывает что пришло от frontend (`$input`)
- ✅ Показывает что есть в базе (`$dbBlocks`)
- ✅ Не ломает существующую логику (просто логирует)
- ✅ Обёрнут в try-catch (безопасно)
- ✅ Понятный маркер для поиска в логах

---

### Шаг 2: Deploy (используй sync-скрипт)
```powershell
.\sync-to-xampp.ps1
```

---

### Шаг 3: Воспроизвести ошибку

1. Открыть страницу с inline-редактором
2. Кликнуть на любой блок
3. Изменить текст
4. Дождаться автосохранения (1.5 сек)
5. Проверить консоль браузера (должна быть ошибка 500)

---

### Шаг 4: Прочитать логи
```powershell
Get-Content C:\xampp\apache\logs\error.log -Tail 50
```

Искать строки между `=== INLINE EDIT DIAGNOSTIC START ===` и `END`

---

### Шаг 5: Анализ результатов

AI проанализирует логи и определит один из 4 сценариев (см. ниже)

---

### Шаг 6: Применить исправление

AI предложит решение на основе выявленной причины

---

### Шаг 7: Откатить diagnostic код

AI удалит временное логирование из `PageController.php`

---

## 📊 Возможные сценарии и решения

### Сценарий 1: Блоки не найдены в БД
**Что покажут логи:**
```
Input data: {"blockId": "f34cac9d-...", ...}
Blocks from DB: []
```
**Решение:** Перезапустить импорт страницы или создать блоки вручную

---

### Сценарий 2: ID блока не совпадает
**Что покажут логи:**
```
Input data: {"blockId": "f34cac9d-...", ...}
Blocks from DB: [{"id": "a1b2c3d4-...", ...}, {"id": "e5f6g7h8-...", ...}]
```
**Решение:** Frontend использует устаревшие ID → очистить кеш, проверить откуда берутся ID

---

### Сценарий 3: Блок существует, но UseCase падает
**Что покажут логи:**
```
Input data: {"blockId": "f34cac9d-...", ...}
Blocks from DB: [{"id": "f34cac9d-...", ...}]
```
**Решение:** Баг в логике `UpdatePageInline` → исправить сравнение строк

---

### Сценарий 4: Неправильный pageId
**Что покажут логи:**
```
Page ID: 9c23c3ff-...
Blocks from DB: []
```
**Решение:** Frontend отправляет неверный pageId → проверить routing

---

## ✅ Критерии завершения

Диагностика завершена когда:
- ✅ Установлена точная причина (один из 4 сценариев)
- ✅ Определено решение
- ✅ Применено исправление
- ✅ Удалён diagnostic код

---

**Документ создан:** 16 октября 2025  
**Версия:** 1.0  
**Автор:** GitHub Copilot (AI Assistant)  
**Статус:** Готов к использованию  
**Предполагаемое время выполнения:** 20-30 минут

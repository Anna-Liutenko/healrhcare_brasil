# 📋 Резюме работы: Система коллекций с превью-картинками

**Дата:** 22 октября 2025  
**Ветка:** `feature/collection-pages`

---

## 🎯 Главная цель

Реализовать систему, где статьи и гайды **автоматически собираются** в галерею-коллекцию с возможностью задать **кастомную превью-картинку** для каждой статьи/гайда.

---

## ✅ Что СДЕЛАНО и РАБОТАЕТ

### 1. **Публичная страница коллекции** ✅ РАБОТАЕТ

**Путь:** `http://localhost/all-materials`

**Что реализовано:**
- Динамическая страница с **двумя табами**: "Гайды" и "Статьи"
- **Пагинация** (12 элементов на странице)
- **Автоматический сбор** всех опубликованных статей и гайдов из БД
- **Карточки с картинками** для каждого элемента
- **Фильтрация по секциям** через URL параметры (`?section=guides` или `?section=articles`)

**Файлы:**
- `backend/src/Presentation/Controller/PublicPageController.php` — рендеринг коллекции
- `frontend/collection-tabs.js` — клиентская логика табов
- `backend/src/Application/UseCase/GetCollectionItems.php` — сбор элементов с фильтрацией

**Статус:** Страница отображается корректно с карточками гайдов (подтверждено скриншотом).

---

### 2. **База данных: поле `card_image`** ✅ ДОБАВЛЕНО

**SQL изменения:**
```sql
ALTER TABLE pages ADD COLUMN card_image VARCHAR(500) NULL AFTER seo_keywords;
```

**Назначение:** Хранить URL кастомной превью-картинки для статьи/гайда.

**Статус:** Колонка создана, подтверждено через `SHOW COLUMNS`.

**Проверка:**
```sql
mysql> SHOW COLUMNS FROM pages LIKE 'card_image';
+------------+--------------+------+-----+---------+-------+
| Field      | Type         | Null | Key | Default | Extra |
+------------+--------------+------+-----+---------+-------+
| card_image | varchar(500) | YES  |     | NULL    |       |
+------------+--------------+------+-----+---------+-------+
```

---

### 3. **Backend: Entity + Repository** ✅ ОБНОВЛЕНО

#### **Domain/Entity/Page.php**
- ✅ Добавлен параметр `cardImage` в конструктор (строка ~40)
- ✅ Метод `getCardImage()` обновлён с **приоритетами** (строка ~143):
  1. `$this->cardImage` (поле из БД) — **ПРИОРИТЕТ**
  2. `collectionConfig['cardImages']` (legacy)
  3. Первая картинка из блоков (hero, article-cards)
  4. `/uploads/default-card.jpg` (fallback)
- ✅ Метод `setCardImage()` — устанавливает значение поля (строка ~185)

#### **Infrastructure/Repository/MySQLPageRepository.php**
- ✅ `insert()` — добавлен `card_image` в SQL INSERT (строка ~195)
- ❌ `update()` — **НЕ ОБНОВЛЁН** (требует доработки, строка ~238)
- ✅ `hydrate()` — читает `card_image` из БД и передаёт в Entity (строка ~289)

**Код hydrate():**
```php
$page = new Page(
    // ... другие поля ...
    cardImage: $row['card_image'] ?? null  // ← ДОБАВЛЕНО
);
```

---

### 4. **Backend: Use Cases** ✅ ОБНОВЛЕНО

#### **Application/UseCase/CreatePage.php** (строка ~76)
```php
cardImage: $data['cardImage'] ?? null
```
Принимает `cardImage` из API payload при создании страницы.

#### **Application/UseCase/UpdatePage.php** (строка ~73)
```php
if (isset($data['cardImage'])) {
    $page->setCardImage($data['cardImage']);
}
```
Обновляет `cardImage` при редактировании страницы.

---

### 5. **Frontend: Editor UI** ✅ ДОБАВЛЕНО

#### **frontend/editor.html** (строка 313-325)
```html
<!-- Card Image for Articles/Guides -->
<div v-if="pageData.type === 'article' || pageData.type === 'guide'" class="settings-group">
    <label class="settings-label">🖼️ Превью для коллекций</label>
    <input
        v-model="pageData.cardImage"
        class="settings-input"
        type="text"
        placeholder="/uploads/my-image.jpg или https://..."
    >
    <small style="color: var(--text-secondary); font-size: 0.75rem; display: block; margin-top: 0.25rem;">
        Картинка для отображения в галереях. Оставьте пустым — будет использоваться первое изображение из контента.
    </small>
</div>
```

**Условие видимости:** Поле показывается **ТОЛЬКО** для типов `article` и `guide`.

**Расположение:** После "SEO Keywords", перед "Навигация (меню)".

#### **frontend/editor.js**
- ✅ Добавлено поле `cardImage: ''` в `pageData` (строка ~32)
- ✅ Добавлено `cardImage` в payload при сохранении (строка ~1630):
```javascript
const basePageData = toPlainObject({
    title: this.pageData.title,
    slug: this.pageData.slug,
    type: this.pageData.type || 'regular',
    status: this.pageData.status || 'draft',
    seoTitle: this.pageData.seoTitle || '',
    seoDescription: this.pageData.seoDescription || '',
    seoKeywords: this.pageData.seoKeywords || '',
    cardImage: this.pageData.cardImage || '',  // ← ДОБАВЛЕНО
    createdBy: this.currentUser.id
});
```

---

### 6. **Упрощение UX** ✅ СДЕЛАНО

**Убран тип "Коллекция" из dropdown** (editor.html, строка ~266):
```html
<!-- БЫЛО 5 опций, СТАЛО 4 -->
<option value="regular">Обычная страница</option>
<option value="home">Главная страница</option>
<option value="article">Статья</option>
<option value="guide">Гайд</option>
<!-- <option value="collection">Коллекция</option> УДАЛЕНО -->
```

**Причина:** Коллекции не редактируются визуально — это автоматические витрины. Пользователю не нужна эта опция в редакторе.

---

## ❌ Что НЕ РАБОТАЕТ (требует доработки)

### 1. **Поле "Превью для коллекций" не отображается в браузере** 🔴 КРИТИЧНО

**Проблема:**
- Файл `editor.html` **ОБНОВЛЁН** в XAMPP (подтверждено через grep)
  ```powershell
  PS> $content = Get-Content "C:\xampp\htdocs\healthcare-cms-frontend\editor.html" -Raw -Encoding UTF8
  PS> $content -match 'Превью для коллекций'
  True  # ← Поле ЕСТЬ в файле!
  ```
- Но браузер **не видит** изменения даже после `Ctrl+Shift+R`
- Пользователь открывает страницу типа "Статья", прокручивает до конца SEO блока — **поля нет**

**Возможные причины:**
1. **Кэш браузера** игнорирует жёсткое обновление
2. **Apache кэширует** статические файлы
3. **Vue.js реактивность** — возможно, условие `v-if` не срабатывает
4. **JavaScript ошибка** блокирует рендеринг секции

**Решения на завтра:**

#### Вариант A: Полная очистка кэша
1. Открыть DevTools (**F12**)
2. Перейти в **Network** tab
3. Поставить галку **✅ Disable cache**
4. **Обновить страницу** (F5)

#### Вариант B: Incognito режим
1. **Ctrl+Shift+N** (новое приватное окно)
2. Открыть `http://localhost/healthcare-cms-frontend/editor.html`
3. Авторизоваться
4. Создать страницу типа "Статья"
5. Проверить наличие поля

#### Вариант C: Проверка Vue условия
```javascript
// В консоли браузера (F12 → Console):
app.pageData.type  // должно быть 'article' или 'guide'
```

Если `pageData.type === 'article'`, но поле не видно — проблема в Vue реактивности.

#### Вариант D: Добавить версионирование
```html
<!-- editor.html -->
<script type="module" src="editor.js?v=20251022"></script>
```

Изменить версию, чтобы принудительно загрузить новый файл.

---

### 2. **UPDATE запрос не обновляет `card_image`** 🟠 ВАЖНО

**Проблема:** В `MySQLPageRepository::update()` **НЕ ДОБАВЛЕНО** поле `card_image` в SQL.

**Текущий код** (строка ~238):
```php
$stmt = $this->db->prepare('
    UPDATE pages SET
        title = :title,
        slug = :slug,
        status = :status,
        type = :type,
        seo_title = :seo_title,
        seo_description = :seo_description,
        seo_keywords = :seo_keywords,
        -- card_image отсутствует! ❌
        show_in_menu = :show_in_menu,
        menu_title = :menu_title,
        ...
    WHERE id = :id
');

$stmt->execute([
    'id' => $page->getId(),
    'title' => $page->getTitle(),
    ...
    'seo_keywords' => $page->getSeoKeywords(),
    // 'card_image' => ... ← НЕТ!
    'show_in_menu' => $page->isShowInMenu() ? 1 : 0,
    ...
]);
```

**Последствия:**
- При **создании** страницы `card_image` сохраняется ✅
- При **редактировании** страницы `card_image` **НЕ ОБНОВЛЯЕТСЯ** ❌

**Решение:**
```php
// В SQL UPDATE добавить:
UPDATE pages SET
    ...
    seo_keywords = :seo_keywords,
    card_image = :card_image,  // ← ДОБАВИТЬ
    show_in_menu = :show_in_menu,
    ...

// В execute() добавить:
$stmt->execute([
    ...
    'seo_keywords' => $page->getSeoKeywords(),
    'card_image' => $page->getCardImage(),  // ← ДОБАВИТЬ
    'show_in_menu' => $page->isShowInMenu() ? 1 : 0,
    ...
]);
```

**Статус:** Требует правки ~5 строк кода.

---

### 3. **Кнопка "Enable Inline Editing" не работает** 🟡 ПРОВЕРИТЬ

**Проблема:**
- Пользователь сообщил, что кнопка **"Enable Inline Editing"** не работает
- Возможно, функция сломалась после последних изменений

**Где находится:**
- `frontend/editor.html` — кнопка в HTML (строка ~182):
  ```html
  <button id="toggleInlineMode" class="toolbar-btn" title="Toggle inline editing">
      📝 Enable Inline Editing
  </button>
  ```
- `frontend/js/InlineEditorManager.js` — логика inline editing

**Что проверить завтра:**
1. Открыть консоль браузера (F12 → Console)
2. Нажать кнопку "Enable Inline Editing"
3. Проверить наличие **JavaScript ошибок** в консоли
4. Проверить, загружается ли `InlineEditorManager.js`:
   ```javascript
   // В консоли:
   typeof InlineEditorManager  // должно быть 'function' или 'object'
   ```

**Возможные причины:**
- Файл `InlineEditorManager.js` не загрузился (404)
- Конфликт с обновлениями `editor.js`
- Event listener не привязался к кнопке

**Решение:**
Проверить в DevTools → Network → JS:
- ✅ `InlineEditorManager.js` — статус 200 OK
- ✅ `editor.js` — статус 200 OK

Если файл загружен, но кнопка не работает — проверить `editor.html` строку ~815:
```html
<script src="js/InlineEditorManager.js"></script>
<script type="module" src="editor.js?v=1.4.20251019"></script>
```

Возможно, нужно **обновить порядок загрузки** скриптов.

---

## 📂 Изменённые файлы

### Backend (7 файлов)
1. ✅ `backend/src/Domain/Entity/Page.php` — добавлен `$cardImage`, обновлён `getCardImage()`
2. ⚠️ `backend/src/Infrastructure/Repository/MySQLPageRepository.php` — INSERT + hydrate обновлены, **UPDATE требует правки**
3. ✅ `backend/src/Application/UseCase/CreatePage.php` — принимает `cardImage`
4. ✅ `backend/src/Application/UseCase/UpdatePage.php` — обновляет `cardImage`
5. ✅ `backend/src/Application/UseCase/GetCollectionItems.php` — использует `getCardImage()` с приоритетом
6. ✅ `backend/src/Presentation/Controller/PublicPageController.php` — рендерит коллекции
7. ✅ `backend/src/Presentation/Controller/CollectionController.php` — API endpoint

### Frontend (3 файла)
1. ⚠️ `frontend/editor.html` — добавлено поле для article/guide (файл обновлён, но браузер не видит)
2. ✅ `frontend/editor.js` — добавлен `cardImage` в data + payload
3. ✅ `frontend/collection-tabs.js` — логика табов (создан ранее)

### Database
- ✅ `pages.card_image` (VARCHAR 500, NULL)

---

## 🧪 План тестирования (на завтра)

### Тест 1: Отображение поля в редакторе
**Цель:** Убедиться, что поле видно для article/guide типов.

**Шаги:**
1. Открыть редактор с **отключенным кэшем** (DevTools → Network → ✅ Disable cache)
2. Авторизоваться
3. Создать **новую страницу**
4. Выбрать тип **"Статья"**
5. Прокрутить правую панель вниз до раздела **"SEO метаданные"**
6. **Ожидаемый результат:** После "SEO Keywords" должно появиться поле **"🖼️ Превью для коллекций"**

**Если поле не видно:**
- Открыть консоль (F12 → Console)
- Проверить: `app.pageData.type` → должно быть `'article'`
- Проверить ошибки в консоли

---

### Тест 2: Кастомная картинка
**Цель:** Проверить, что custom card_image сохраняется и отображается в коллекции.

**Шаги:**
1. Создать страницу типа **"Статья"**
2. Заполнить:
   - **Название:** "Тестовая статья с картинкой"
   - **Slug:** `test-article-image`
   - **SEO Description:** "Описание для превью в коллекции"
   - **🖼️ Превью для коллекций:** `/uploads/test-preview.jpg`
3. Добавить любые блоки (текст, картинки)
4. **Сохранить** (💾)
5. **Опубликовать** (🚀)
6. Открыть в новой вкладке: `http://localhost/all-materials?section=articles`
7. **Ожидаемый результат:** 
   - Карточка "Тестовая статья с картинкой" отображается
   - Картинка карточки = `/uploads/test-preview.jpg` (не из блоков!)

**Проверка в БД:**
```sql
SELECT id, title, card_image FROM pages WHERE slug = 'test-article-image';
-- card_image должно быть '/uploads/test-preview.jpg'
```

---

### Тест 3: Автоподбор (fallback)
**Цель:** Проверить, что если card_image пустой, система берёт первую картинку из блоков.

**Шаги:**
1. Создать статью **БЕЗ** заполнения поля "Превью для коллекций"
2. Добавить блок **Hero** с картинкой `/uploads/hero-image.jpg`
3. Сохранить + Опубликовать
4. Открыть `/all-materials?section=articles`
5. **Ожидаемый результат:** Карточка показывает `/uploads/hero-image.jpg`

**Проверка логики:**
```php
// Page::getCardImage() должен:
// 1. Проверить $this->cardImage → пусто
// 2. Проверить collectionConfig → пусто
// 3. Найти первую картинку в блоках → hero-image.jpg ✅
```

---

### Тест 4: Редактирование card_image
**Цель:** Проверить, что UPDATE запрос сохраняет изменения.

**Предварительно:** Сначала исправить `MySQLPageRepository::update()` (добавить `card_image`).

**Шаги:**
1. Открыть существующую статью
2. Изменить URL в поле "Превью для коллекций":
   - Было: `/uploads/old.jpg`
   - Стало: `/uploads/new.jpg`
3. **Сохранить** (💾)
4. Открыть коллекцию `/all-materials?section=articles`
5. **Ожидаемый результат:** Карточка показывает `/uploads/new.jpg`

**Проверка в БД:**
```sql
SELECT card_image FROM pages WHERE id = '<page_id>';
-- Должно обновиться с 'old.jpg' на 'new.jpg'
```

---

### Тест 5: Inline Editing
**Цель:** Проверить работоспособность кнопки "Enable Inline Editing".

**Шаги:**
1. Открыть редактор
2. Создать/открыть любую страницу
3. Нажать кнопку **"📝 Enable Inline Editing"** (внизу preview)
4. **Ожидаемый результат:** 
   - Текст кнопки меняется на "Disable Inline Editing"
   - Блоки на preview становятся **редактируемыми** (при клике)
5. Кликнуть на любой текстовый блок
6. **Ожидаемый результат:** Появляется inline редактор

**Если не работает:**
- Открыть консоль (F12 → Console)
- Проверить ошибки
- Проверить загрузку `InlineEditorManager.js` (Network tab)

---

## 🛠️ TODO на завтра (приоритет)

### 🔴 Критично (блокирует тестирование):
1. ✅ **Починить отображение поля в браузере**
   - Попробовать Incognito режим
   - Проверить DevTools → Network → Disable cache
   - Добавить версионирование `editor.js?v=20251022`
   - Проверить Vue условие `v-if` в консоли

### 🟠 Важно (блокирует редактирование):
2. ✅ **Добавить `card_image` в UPDATE SQL** (MySQLPageRepository::update)
   - 2 строки в SQL
   - 1 строка в execute()
   - Синхронизировать на XAMPP

### 🟡 Среднее (функциональность):
3. ✅ **Проверить кнопку "Enable Inline Editing"**
   - Проверить загрузку `InlineEditorManager.js`
   - Проверить ошибки в консоли
   - Возможно, исправить порядок загрузки скриптов

### 🟢 Низкое (улучшения):
4. 🔧 Добавить **кнопку выбора из медиатеки** (вместо ручного ввода URL)
5. 🔧 Добавить **preview** картинки рядом с полем ввода
6. 🔧 Валидация URL (проверка формата `/uploads/...` или `https://...`)

---

## 📊 Архитектура решения

```
┌─────────────────────────────────────────────────────────┐
│  USER: Создаёт статью в редакторе                       │
│  • Выбирает тип "Статья"                                │
│  • Видит поле "🖼️ Превью для коллекций" (если кэш OK)  │
│  • Вставляет URL картинки или оставляет пустым          │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  FRONTEND: Editor (Vue.js)                              │
│  • editor.html: поле v-model="pageData.cardImage"       │
│  • editor.js: отправка cardImage в basePageData         │
│  • Условие: v-if="type === 'article' || type === 'guide'"│
└────────────────┬────────────────────────────────────────┘
                 │ POST /api/pages { cardImage: "..." }
                 ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: PageController                                │
│  • Принимает JSON с cardImage                           │
│  • Передаёт в CreatePage / UpdatePage use case          │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  USE CASES: CreatePage / UpdatePage                     │
│  • CreatePage: новый Page(cardImage: $data['cardImage'])│
│  • UpdatePage: $page->setCardImage($data['cardImage'])  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  REPOSITORY: MySQLPageRepository                        │
│  • INSERT: включает card_image ✅                       │
│  • UPDATE: НЕ включает card_image ❌ ← ИСПРАВИТЬ!      │
│  • HYDRATE: читает card_image из БД ✅                  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  DATABASE: pages.card_image (VARCHAR 500)               │
│  • NULL = автоподбор из блоков                          │
│  • "/uploads/..." = кастомная картинка                  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  USE CASE: GetCollectionItems                           │
│  • SELECT * FROM pages WHERE type IN ('article','guide')│
│  • WHERE status = 'published'                           │
│  • Для каждой страницы:                                 │
│    $image = $page->getCardImage($blocks)                │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  ENTITY: Page::getCardImage($blocks)                    │
│  Приоритеты:                                            │
│  1. if (!empty($this->cardImage)) return $this->cardImage│
│  2. if (collectionConfig['cardImages']) return legacy   │
│  3. foreach ($blocks) найти первую картинку             │
│  4. return '/uploads/default-card.jpg'                  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  CONTROLLER: PublicPageController::renderCollectionPage │
│  • Генерирует HTML с карточками                         │
│  • Табы: Гайды / Статьи                                 │
│  • Пагинация: page=1,2,3...                             │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  USER: Видит коллекцию на /all-materials               │
│  • Карточка с кастомной картинкой из card_image ✅      │
│  • Или с автоподобранной из блоков ✅                   │
│  • Клик → переход на статью                             │
└─────────────────────────────────────────────────────────┘
```

---

## 🎓 Что узнали

### 1. **Collection page ≠ обычная страница**
- Это **динамическая витрина**, не нужен визуальный редактор блоков
- Контент собирается **автоматически из БД**
- Пользователь редактирует **источники** (статьи/гайды), а не коллекцию

### 2. **Приоритеты fallback — важны для UX**
```
card_image (кастом) 
  → блоки (автоподбор) 
    → default (/uploads/default-card.jpg)
```
Гибкость для пользователя: можно задать картинку явно или оставить пустым.

### 3. **Кэш браузера — главная проблема при разработке UI**
- Файл обновлён на сервере ✅
- Grep находит изменения ✅
- Но браузер не видит → кэш ❌

**Решение:** 
- Всегда работать с DevTools → Network → Disable cache
- Или версионирование: `editor.js?v=20251022`

### 4. **Неполное обновление Repository — источник багов**
- INSERT содержит `card_image` ✅
- UPDATE **НЕ содержит** ❌
- Результат: создание работает, редактирование — нет

**Урок:** При добавлении поля в Entity обновлять **все CRUD операции**.

---

## 📝 Команды для проверки (завтра)

### Проверка файла в XAMPP:
```powershell
# Проверить наличие поля в HTML
$html = Get-Content "C:\xampp\htdocs\healthcare-cms-frontend\editor.html" -Raw -Encoding UTF8
$html -match 'Превью для коллекций'
# True = поле есть

# Найти строку с полем
$lines = $html -split "`n"
$lineNum = 0
foreach ($line in $lines) {
    $lineNum++
    if ($line -match 'Превью для коллекций') {
        Write-Host "Строка $lineNum: $($line.Trim())"
    }
}
```

### Проверка в БД:
```sql
-- Проверить структуру таблицы
SHOW COLUMNS FROM pages LIKE 'card_image';

-- Посмотреть значения
SELECT id, title, type, card_image FROM pages WHERE type IN ('article', 'guide');

-- Тестовое обновление
UPDATE pages SET card_image = '/uploads/test.jpg' WHERE id = '<some_id>';
SELECT card_image FROM pages WHERE id = '<some_id>';
-- Должно вернуть '/uploads/test.jpg'
```

### Проверка в браузере (F12 Console):
```javascript
// Проверить тип страницы
app.pageData.type
// Должно быть 'article' или 'guide'

// Проверить значение cardImage
app.pageData.cardImage
// Пусто или URL

// Проверить загрузку InlineEditorManager
typeof InlineEditorManager
// Должно быть 'function' или 'object'
```

---

## 🚀 Следующие шаги (краткий чеклист)

- [ ] **ДЕНЬ 1:** Исправить отображение поля (кэш)
- [ ] **ДЕНЬ 1:** Добавить card_image в UPDATE SQL
- [ ] **ДЕНЬ 1:** Проверить кнопку Inline Editing
- [ ] **ДЕНЬ 2:** Протестировать создание статьи с card_image
- [ ] **ДЕНЬ 2:** Протестировать редактирование card_image
- [ ] **ДЕНЬ 2:** Протестировать fallback (автоподбор из блоков)
- [ ] **ДЕНЬ 3:** Улучшения UX (медиатека, preview, валидация)

---

**Конец резюме.**  
Хорошей ночи! 🌙

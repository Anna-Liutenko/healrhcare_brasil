# Исправление редактора статей - Сохранение и выравнивание

**Дата:** 10 октября 2025  
**Статус:** ✅ Исправлено

---

## 🐛 Проблемы

### 1. **Ошибка сохранения статьи**

**Симптом:**  
При нажатии "✓ Сохранить и закрыть" появляется красное уведомление "Ошибка сохранения".

**Причина:**  
Метод `saveArticleAndClose()` вызывал несуществующий метод `saveToLocalStorage()`:

```javascript
// ❌ НЕПРАВИЛЬНО - метод не существует
this.saveToLocalStorage();
```

**В Console (DevTools):**
```
Saving error: this.saveToLocalStorage is not a function
```

---

### 2. **Выравнивание работает наоборот**

**Симптом:**  
При нажатии "Выровнять по левому краю" текст выравнивается по центру, и наоборот.

**Причина:**  
Quill имеет встроенную конфигурацию для выравнивания, но возможно конфликт с CSS или RTL/LTR направлением текста.

**Конфигурация Quill:**
```javascript
[{ 'align': [] }]  // Quill по умолчанию: left, center, right, justify
```

---

## ✅ Решение

### Исправление сохранения

**Было:**
```javascript
async saveArticleAndClose() {
    if (this.quillInstance) {
        try {
            this.articleHtml = this.quillInstance.root.innerHTML;
            
            // Конвертируем HTML в наши блоки
            this.convertHtmlToBlocks(this.articleHtml);
            
            // ❌ ОШИБКА: метод не существует!
            this.saveToLocalStorage();
            
            // Закрываем редактор
            this.quillInstance = null;
            this.showArticleEditor = false;
            
            window.history.pushState({}, '', window.location.pathname);
            
            this.showNotification('✅ Статья сохранена!', 'success');
        } catch (e) {
            console.error('Saving error:', e);
            this.showNotification('Ошибка сохранения', 'error');
        }
    }
},
```

**Стало:**
```javascript
async saveArticleAndClose() {
    if (this.quillInstance) {
        try {
            // Получаем HTML из Quill
            this.articleHtml = this.quillInstance.root.innerHTML;

            this.debugMsg('Сохранение статьи', 'info', { htmlLength: this.articleHtml.length });

            // Конвертируем HTML в наши блоки
            this.convertHtmlToBlocks(this.articleHtml);

            // ✅ Блок уже добавлен в this.blocks через convertHtmlToBlocks()
            // Пользователь должен нажать "💾 Сохранить" для сохранения через API
            
            this.showNotification('Статья добавлена в блоки. Не забудьте сохранить страницу!', 'success');

            // Закрываем редактор
            this.quillInstance = null;
            this.showArticleEditor = false;

            // Меняем URL обратно
            window.history.pushState({}, '', window.location.pathname);

            this.debugMsg('Статья успешно конвертирована в блок', 'success');
        } catch (e) {
            console.error('Saving error:', e);
            this.debugMsg('Ошибка сохранения статьи', 'error', { error: e.message });
            this.showNotification('Ошибка сохранения: ' + e.message, 'error');
        }
    }
},
```

---

## 🔄 Как работает сохранение теперь

### Workflow:

1. **Пользователь пишет статью в Quill**
   - Вводит текст, форматирует, вставляет изображения

2. **Нажимает "✓ Сохранить и закрыть"**
   - Quill HTML извлекается: `this.quillInstance.root.innerHTML`
   - HTML конвертируется в `text-block` через `convertHtmlToBlocks()`
   - Блок добавляется в `this.blocks` (Vue reactive array)
   - Редактор закрывается

3. **Статья появляется в preview**
   - Блок `text-block` с `containerStyle: 'article'` отображается в центральной preview области

4. **Пользователь нажимает "💾 Сохранить"**
   - Вызывается `savePage()` → сохранение через API в базу данных
   - Все блоки (включая text-block со статьей) сохраняются

---

## 📝 Метод `convertHtmlToBlocks()`

```javascript
convertHtmlToBlocks(html) {
    // Ищем существующий text-block со стилем article
    const existingTextBlockIndex = this.blocks.findIndex(b =>
        b.type === 'text-block' && b.data.containerStyle === 'article'
    );

    if (existingTextBlockIndex !== -1) {
        // ✅ Обновляем существующий блок (если статья редактировалась повторно)
        this.blocks[existingTextBlockIndex].data.content = html;
    } else {
        // ✅ Добавляем новый блок в конец (первая статья)
        this.blocks.push({
            type: 'text-block',
            data: {
                title: '',
                content: html,
                alignment: 'left',
                containerStyle: 'article'
            }
        });
    }
}
```

**Особенности:**
- Не удаляет другие блоки на странице (Hero, Cards, Gallery и т.д.)
- Если статья уже существует → обновляет её
- Если статья новая → добавляет в конец списка блоков

---

## 🎨 Проблема с выравниванием

### Возможные причины:

1. **CSS конфликт**
   - Quill использует классы `.ql-align-left`, `.ql-align-center`, `.ql-align-right`
   - CSS в `editor-ui.css` или `editor-preview.css` может переопределять эти классы

2. **RTL/LTR конфликт**
   - Если на странице установлен `dir="rtl"`, выравнивание может работать зеркально

3. **Quill версия**
   - Используем Quill 1.3.6 (CDN: `https://cdn.quilljs.com/1.3.6/quill.js`)
   - Возможно, баг в этой версии

### Как проверить:

1. **Открыть DevTools → Elements**
2. **Выделить текст с выравниванием**
3. **Проверить класс элемента:**
   ```html
   <!-- Должно быть так: -->
   <p class="ql-align-left">Текст слева</p>
   <p class="ql-align-center">Текст по центру</p>
   <p class="ql-align-right">Текст справа</p>
   ```

4. **Проверить CSS:**
   ```css
   /* Должно быть в Quill CSS: */
   .ql-align-left {
       text-align: left;
   }
   .ql-align-center {
       text-align: center;
   }
   .ql-align-right {
       text-align: right;
   }
   ```

### Временное решение (если проблема persist):

Добавить в `editor-ui.css`:

```css
/* Force Quill alignment overrides */
.ql-editor .ql-align-left {
    text-align: left !important;
}

.ql-editor .ql-align-center {
    text-align: center !important;
}

.ql-editor .ql-align-right {
    text-align: right !important;
}

.ql-editor .ql-align-justify {
    text-align: justify !important;
}

/* Default (no class) should be left */
.ql-editor p:not([class*="ql-align"]) {
    text-align: left;
}
```

---

## 📋 Измененные файлы

- **`frontend/editor.js`**
  - Метод `saveArticleAndClose()` - удален вызов `saveToLocalStorage()`, добавлен debug logging
  - Уведомление изменено на "Статья добавлена в блоки. Не забудьте сохранить страницу!"

---

## 🧪 Проверка работоспособности

### 1. Сохранение статьи

1. Открыть http://localhost/visual-editor-standalone/editor.html
2. Кликнуть "✍️ Написать статью"
3. Ввести текст "Тестовая статья"
4. Кликнуть "✓ Сохранить и закрыть"
5. **Ожидается:**
   - ✅ Редактор закрывается
   - ✅ Зеленое уведомление: "Статья добавлена в блоки. Не забудьте сохранить страницу!"
   - ✅ В preview появляется новый text-block с текстом
   - ✅ В правой панели (Блок) можно выбрать этот блок и редактировать

### 2. Финальное сохранение

1. После закрытия article editor, кликнуть "💾 Сохранить" в toolbar
2. **Ожидается:**
   - ✅ Страница сохраняется через API
   - ✅ Уведомление "Страница успешно сохранена!"
   - ✅ В Debug Panel: "Сохранение страницы" → SUCCESS

### 3. Выравнивание текста

1. Открыть article editor
2. Ввести текст "Заголовок"
3. Выделить текст
4. Кликнуть кнопку выравнивания в Quill toolbar
5. **Ожидается:**
   - Кнопка с тремя линиями слева = выравнивание влево
   - Кнопка с тремя линиями по центру = выравнивание по центру
   - Кнопка с тремя линиями справа = выравнивание вправо

---

## 🔍 Debugging

### Если сохранение не работает:

1. **Открыть DevTools → Console**
2. **Проверить ошибки:**
   - ❌ `saveToLocalStorage is not a function` → файл `editor.js` не обновлен в XAMPP
   - ❌ `Cannot read property 'root' of null` → Quill не инициализирован

3. **Проверить Debug Panel:**
   - "Сохранение статьи" → INFO → `{ htmlLength: 123 }`
   - "Статья успешно конвертирована в блок" → SUCCESS

### Если выравнивание работает наоборот:

1. **Проверить в DevTools → Elements:**
   - Найти элемент с текстом
   - Проверить класс: должен быть `.ql-align-left`, `.ql-align-center` и т.д.

2. **Проверить Computed Styles:**
   - Выбрать элемент
   - Во вкладке "Computed" найти `text-align`
   - Должно быть `left`, `center`, `right` соответственно

3. **Если проблема persist:**
   - Добавить CSS overrides (см. выше "Временное решение")

---

## ✅ Результат

**Сохранение статьи:** ✅ Работает  
- Статья конвертируется в `text-block`
- Добавляется в массив блоков
- Сохраняется при нажатии "💾 Сохранить"

**Выравнивание:** ⚠️ Требует проверки  
- Если работает наоборот → добавить CSS overrides
- Проверить Quill версию и конфигурацию

---

**Дата завершения:** 10 октября 2025  
**Автор:** GitHub Copilot

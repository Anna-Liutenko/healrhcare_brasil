# 🎯 ПЛАН ДЕЙСТВИЙ: Что делать дальше

**Дата:** 18 октября 2025  
**Основано на:** Аналитической записке (анализ 90+ документов)  
**Цель:** Конкретный actionable план на ближайшие дни/недели

---

## 📌 TL;DR — Что делать ПРЯМО СЕЙЧАС

### Самое важное (1 час работы)

```bash
✅ ШАГ 1: Выполнить Phase 2.1 — Clean Architecture рефакторинг
   Файл: docs/PHASE_2_COMPLETION_PROMPT.md
   Действие: Скопировать → вставить в LLM → выполнить
   Время: 1 час
   Результат: Улучшенная архитектура + тесты
```

**Почему это критично:**
- Блокирует дальнейший рефакторинг
- Промпт готов на 100%
- Можно выполнить через LLM без ручной работы
- Улучшит качество кода

---

## 🗓️ ПЛАН НА НЕДЕЛЮ (детальный)

### День 1 (СЕГОДНЯ) — Архитектура

#### Утро (2-3 часа)

**Задача 1: Phase 2.1 рефакторинг** [КРИТИЧНО]
```
Действия:
1. Открыть: docs/PHASE_2_COMPLETION_PROMPT.md
2. Скопировать весь файл (Ctrl+A → Ctrl+C)
3. Открыть Claude/ChatGPT/Gemini
4. Вставить (Ctrl+V)
5. Написать: "Execute step-by-step"
6. Подождать 45 минут

Проверка:
cd backend
php -l src/Application/UseCase/UpdatePageInline.php
vendor/bin/phpunit tests/UpdatePageInlineTest.php

Ожидаемый результат:
✅ UpdatePageInline использует DTO
✅ Domain Exceptions работают
✅ 4 теста проходят
✅ Inline-редактирование возвращает 404 (не 500) при "Block not found"
```

**Время:** 1 час  
**Приоритет:** 🔴 КРИТИЧЕСКИЙ  
**Документы:** 
- `PHASE_2_COMPLETION_PROMPT.md` (главный промпт)
- `HOW_TO_USE_PHASE_2_PROMPT.md` (если возникнут вопросы)

---

**Задача 2: Унифицировать медиабиблиотеку** [ВАЖНО]
```
Проблема: Editor использует upload.php, media-library использует /api/media

Решение:
1. Открыть: frontend/editor.js
2. Найти метод loadGalleryImages() (строки 1534-1545)
3. Заменить:
   // БЫЛО:
   const response = await fetch('upload.php');
   
   // СТАЛО:
   const files = await this.apiClient.getMedia();
   this.galleryImages = files.map(f => ({
       name: f.filename,
       url: f.url
   }));

4. Обновить handleFileUpload():
   async handleFileUpload(event) {
       const file = event.target.files[0];
       const result = await this.apiClient.uploadMedia(file);
       await this.loadGalleryImages();
   }

5. Удалить: frontend/upload.php

6. Тест:
   - Открыть редактор
   - Создать блок с картинкой
   - Кликнуть "Выбрать из галереи"
   - Проверить что картинки загружаются
```

**Время:** 1-2 часа  
**Приоритет:** 🟡 ВАЖНО  
**Документы:** `DEVELOPMENT_PLAN_UPDATED.md` блок D2

---

#### День (1-2 часа)

**Задача 3: Исправить Vue template errors** [ВАЖНО]
```
Проблема: key.includes() не работает в Vue шаблонах

Решение:
1. Открыть: frontend/editor.js
2. Добавить в секцию methods:

methods: {
    // ... существующие методы
    
    isDimensionKey(key) {
        return key.includes('height') || key.includes('width');
    },
    
    isRichTextKey(key) {
        return key.includes('content') || key.includes('text') ||
               key.includes('message') || key.includes('subtitle');
    },
    
    isImageKey(key) {
        return key.includes('image') || key.includes('Image') ||
               key.includes('photo') || key.includes('background');
    }
}

3. Открыть: frontend/editor.html
4. Найти строки 1488-1520
5. Заменить:
   <!-- БЫЛО -->
   <input v-if="key.includes('height') || key.includes('width')" ...>
   <textarea v-else-if="key.includes('content')" ...>
   
   <!-- СТАЛО -->
   <input v-if="isDimensionKey(key)" ...>
   <textarea v-else-if="isRichTextKey(key)" ...>

6. Тест:
   - Открыть редактор
   - Создать блок с динамическими полями
   - Проверить что редактирование работает
   - Не должно быть ошибок в консоли
```

**Время:** 30 минут  
**Приоритет:** 🟡 ВАЖНО  
**Документы:** `DEVELOPMENT_PLAN_UPDATED.md` блок A3

---

**ИТОГО ДЕНЬ 1:** 2.5-4 часа работы, 3 критические задачи решены

---

### День 2 — UI улучшения

#### Задача 4: Переработать правую панель редактора
```
Цель: Убрать вкладку "Глобальные", оставить "Страница" + "Блок"

Действия:
1. Открыть: frontend/editor.html
2. Найти секцию с вкладками правой панели
3. Удалить вкладку "Глобальные" (весь код)
4. Оставить в "Страница":
   - Название, slug, тип, статус
   - SEO метаданные (title, description, keywords)
   - Навигация (☑️ Показывать в меню, порядок, название)

5. Тест:
   - Открыть редактор
   - Проверить что вкладка "Глобальные" отсутствует
   - "Страница" содержит все нужные поля
```

**Время:** 1-2 часа  
**Приоритет:** 🟢 СРЕДНИЙ  
**Документы:** `DEVELOPMENT_PLAN_UPDATED.md` блок B1

---

#### Задача 5: Создать модалку "Глобальные настройки"
```
Цель: Вынести глобальные настройки на страницу списка

Действия:
1. Открыть: frontend/index.html
2. Добавить кнопку в toolbar:
   <button @click="showGlobalSettings = true" class="btn-settings">
       ⚙️ Глобальные настройки
   </button>

3. Добавить модалку (в конце <div id="app">):
   <div v-if="showGlobalSettings" class="modal-overlay">
       <div class="modal-content">
           <h2>Глобальные настройки сайта</h2>
           
           <div class="settings-section">
               <h3>Header</h3>
               <input v-model="globalSettings.header.logoText" 
                      placeholder="Логотип">
           </div>
           
           <div class="settings-section">
               <h3>Footer</h3>
               <input v-model="globalSettings.footer.copyrightText">
           </div>
           
           <div class="settings-section">
               <h3>Cookie Banner</h3>
               <label>
                   <input type="checkbox" 
                          v-model="globalSettings.cookieBanner.enabled">
                   Включить Cookie Banner
               </label>
               <textarea v-model="globalSettings.cookieBanner.message">
               </textarea>
           </div>
           
           <button @click="saveGlobalSettings">Сохранить</button>
           <button @click="showGlobalSettings = false">Закрыть</button>
       </div>
   </div>

4. В data() добавить:
   data() {
       return {
           showGlobalSettings: false,
           globalSettings: {
               header: { logoText: '' },
               footer: { copyrightText: '' },
               cookieBanner: { enabled: false, message: '' }
           }
       }
   }

5. Добавить методы:
   methods: {
       async loadGlobalSettings() {
           const settings = await this.apiClient.getSettings();
           this.globalSettings = settings;
       },
       
       async saveGlobalSettings() {
           await this.apiClient.updateSettings(this.globalSettings);
           this.showGlobalSettings = false;
           alert('Настройки сохранены');
       }
   }

6. Тест:
   - Открыть список страниц
   - Кликнуть "⚙️ Глобальные настройки"
   - Изменить настройки
   - Сохранить
   - Перезагрузить — настройки сохранились
```

**Время:** 2-3 часа  
**Приоритет:** 🟢 СРЕДНИЙ  
**Документы:** `DEVELOPMENT_PLAN_UPDATED.md` блок B2

---

**ИТОГО ДЕНЬ 2:** 3-5 часов работы

---

### День 3 — Мелкие улучшения и cleanup

#### Задача 6: Добавить кнопку "← К списку страниц"
```
Действия:
1. Открыть: frontend/editor.html
2. Найти toolbar-actions
3. Добавить кнопку в начало:
   <button @click="goToPagesList" class="toolbar-btn">
       ← К списку страниц
   </button>

4. В editor.js добавить метод:
   methods: {
       goToPagesList() {
           window.location.href = 'index.html';
       }
   }

5. Тест:
   - Открыть редактор
   - Кликнуть "← К списку страниц"
   - Должен вернуться на index.html
```

**Время:** 15 минут  
**Приоритет:** 🟢 НИЗКИЙ

---

#### Задача 7: Создать SEED_DATA.sql
```
Цель: Тестовые данные для быстрого старта

Действия:
1. Создать файл: database/seeds/SEED_DATA.sql
2. Добавить:
   -- Пользователи
   INSERT INTO users (id, username, email, password_hash, role, is_active)
   VALUES
   ('uuid1', 'admin', 'admin@example.com', '$2y$10$...', 'super_admin', 1),
   ('uuid2', 'editor', 'editor@example.com', '$2y$10$...', 'editor', 1);
   
   -- Страницы
   INSERT INTO pages (id, title, slug, status, type, created_by)
   VALUES
   ('uuid3', 'Главная', 'home', 'published', 'regular', 'uuid1'),
   ('uuid4', 'О проекте', 'about', 'published', 'regular', 'uuid1');
   
   -- Блоки для каждой страницы
   INSERT INTO blocks (id, page_id, type, position, data)
   VALUES
   ('uuid5', 'uuid3', 'main-screen', 0, '{"title":"Добро пожаловать"}');
   
   -- Меню
   INSERT INTO menu (id, label, page_id, position, is_active)
   VALUES
   ('uuid6', 'Главная', 'uuid3', 0, 1);

3. Протестировать:
   mysql healthcare_cms < database/seeds/SEED_DATA.sql
```

**Время:** 1 час  
**Приоритет:** 🟢 СРЕДНИЙ

---

#### Задача 8: Архивировать устаревшие документы
```
Цель: Навести порядок в docs/

Действия:
1. Создать структуру:
   mkdir docs/archive
   mkdir docs/archive/completed
   mkdir docs/archive/deprecated
   mkdir docs/archive/research

2. Переместить файлы:
   # Завершённые задачи
   mv docs/PHASE_0_DIAGNOSTIC_PROMPT.md docs/archive/completed/
   mv docs/DIAGNOSTIC_REPORT_BLOCK_NOT_FOUND.md docs/archive/completed/
   mv docs/DEBUG_HISTORY_*.md docs/archive/completed/
   
   # Устаревшие планы
   # (если есть MENU_EDITOR_PROMPT.md)
   
   # Исследования
   mv docs/UX_RESEARCH_REPORT.md docs/archive/research/
   mv docs/SUMMARY_RESEARCH_OCT_2025.md docs/archive/research/

3. Обновить: docs/DOCUMENTATION_INDEX.md
   Добавить секцию "Архив" с ссылками

4. Commit:
   git add docs/archive/
   git commit -m "docs: архивация завершённых и устаревших документов"
```

**Время:** 30 минут  
**Приоритет:** 🟢 НИЗКИЙ

---

**ИТОГО ДЕНЬ 3:** 2-3 часа работы

---

## 🗓️ ПЛАН НА МЕСЯЦ (опционально)

### Неделя 2: Продолжение рефакторинга

```
[ ] Phase 2.2-2.3: Рефакторинг других Use Cases
    Время: 10-14 часов
    Документы: NEXT_STEPS_PLAN.md
    
[ ] Phase 3: Рефакторинг Controllers
    Время: 9-12 часов
    
[ ] Phase 4: Tests + Documentation
    Время: 5-8 часов
```

**Итого неделя 2:** 24-34 часа (3-4 рабочих дня)

---

### Недели 3-4: Inline-редактирование (опционально)

```
[ ] Phase 5: Inline Editor (полная реализация)
    Время: 12-15 дней
    Документы: INLINE_EDITOR_SPEC.md + промпты
    
Этапы:
1. Базовая инфраструктура (1-2 дня)
2. Floating Toolbar (2-3 дня)
3. Link & Image поповеры (1-2 дня)
4. Auto-save и Manual save (2 дня)
5. Undo/Redo (1 день)
6. Edge cases (2 дня)
7. Интеграция (1 день)
8. Тесты (2-3 дня)
9. Документация (1 день)
```

**Примечание:** Inline-редактирование — nice-to-have, не критично

---

## 📊 ЧЕКЛИСТ ПРОГРЕССА

### Критические задачи (MUST DO)
- [ ] ✅ **Phase 2.1 рефакторинг** (1 час) — НАЧАТЬ СЕГОДНЯ
- [ ] ✅ **Унифицировать медиабиблиотеку** (1-2 часа)
- [ ] ✅ **Исправить Vue template errors** (30 мин)

### Важные задачи (SHOULD DO)
- [ ] Переработать правую панель (1-2 часа)
- [ ] Создать модалку глобальных настроек (2-3 часа)
- [ ] Создать SEED_DATA.sql (1 час)

### Желательные задачи (NICE TO HAVE)
- [ ] Добавить кнопку навигации (15 мин)
- [ ] Архивировать документы (30 мин)
- [ ] Убрать временные решения из кода (2-3 часа)

### Долгосрочные задачи (МОЖНО ОТЛОЖИТЬ)
- [ ] Inline-редактирование (12-15 дней)
- [ ] Статусы unlisted/archived (1 день)
- [ ] Версионирование страниц (3-5 дней)
- [ ] Управление пользователями UI (1 день)

---

## 🎯 КАК НАЧАТЬ (пошаговая инструкция)

### Вариант 1: Быстрый старт (5 минут)

```bash
# Шаг 1: Открыть главный промпт
code "docs/PHASE_2_COMPLETION_PROMPT.md"

# Шаг 2: Скопировать весь файл
# Ctrl+A → Ctrl+C

# Шаг 3: Открыть LLM (Claude/ChatGPT)
# https://claude.ai или https://chat.openai.com

# Шаг 4: Вставить и запустить
# Ctrl+V → "Execute step-by-step"

# Шаг 5: Подождать 45 минут ☕

# Шаг 6: Проверить результат
cd backend
php -l src/Application/UseCase/UpdatePageInline.php
vendor/bin/phpunit tests/UpdatePageInlineTest.php
```

---

### Вариант 2: Основательный подход (10 минут)

```bash
# Шаг 1: Прочитать аналитическую записку (этот файл)
code "docs/АНАЛИТИЧЕСКАЯ_ЗАПИСКА_ТЕКУЩЕЕ_СОСТОЯНИЕ.md"

# Шаг 2: Прочитать инструкцию по использованию промпта
code "docs/HOW_TO_USE_PHASE_2_PROMPT.md"

# Шаг 3: Прочитать краткую справку
code "docs/PHASE_2_QUICK_START.md"

# Шаг 4: Выполнить промпт (как в Варианте 1)
```

---

## 📚 ПОЛЕЗНЫЕ ДОКУМЕНТЫ

### Для быстрого старта
1. `PHASE_2_COMPLETION_PROMPT.md` — главный промпт (800+ строк)
2. `HOW_TO_USE_PHASE_2_PROMPT.md` — как использовать
3. `PHASE_2_QUICK_START.md` — краткая справка

### Для понимания проекта
1. `АНАЛИТИЧЕСКАЯ_ЗАПИСКА_ТЕКУЩЕЕ_СОСТОЯНИЕ.md` — этот файл
2. `PROJECT_STATUS.md` — статус проекта (8 окт 2025)
3. `CMS_DEVELOPMENT_PLAN.md` — общий план разработки

### Для дальнейшей работы
1. `NEXT_STEPS_PLAN.md` — план рефакторинга (Phase 2-5)
2. `DEVELOPMENT_PLAN_UPDATED.md` — обновлённый план
3. `INLINE_EDITOR_SPEC.md` — спецификация inline-редактирования

### Для troubleshooting
1. `DEVELOPER_CHEAT_SHEET.md` — FAQ и команды
2. `DOCUMENTATION_INDEX.md` — навигация по документам
3. `API_ENDPOINTS_CHEATSHEET.md` — API reference

---

## ⏱️ ВРЕМЕННЫЕ ОЦЕНКИ

### Минимальный план (1 день)
```
Phase 2.1 рефакторинг          1 час
Унифицировать медиабиблиотеку  1-2 часа
Исправить Vue errors           30 мин
─────────────────────────────────────
ИТОГО:                         2.5-3.5 часа
```

### Оптимальный план (3 дня)
```
День 1: Архитектура            2.5-4 часа
День 2: UI улучшения           3-5 часов
День 3: Cleanup                2-3 часа
─────────────────────────────────────
ИТОГО:                         7.5-12 часов
```

### Полный план (1 месяц)
```
Неделя 1: Критические задачи   7.5-12 часов
Неделя 2: Рефакторинг          24-34 часа
Недели 3-4: Inline Editor      80-100 часов (опционально)
─────────────────────────────────────
ИТОГО:                         31.5-46 часов (без Inline Editor)
                               или 111.5-146 часов (с Inline Editor)
```

---

## 🚀 ГЛАВНАЯ РЕКОМЕНДАЦИЯ

### ✅ НАЧАТЬ С PHASE 2.1 СЕГОДНЯ

**Почему:**
1. Промпт готов на 100%
2. Можно выполнить через LLM за 1 час
3. Блокирует дальнейший рефакторинг
4. Улучшит качество кода
5. Inline-редактирование будет возвращать правильные HTTP коды

**Что нужно:**
- 1 час времени
- Доступ к Claude/ChatGPT/Gemini
- Файл `PHASE_2_COMPLETION_PROMPT.md`

**Результат:**
- ✅ Clean Architecture для UpdatePageInline
- ✅ Domain Exceptions работают
- ✅ 4 unit теста проходят
- ✅ Правильные HTTP коды (404 вместо 500)

---

## 📞 ВОПРОСЫ И ПОДДЕРЖКА

**Если возникли вопросы:**
1. См. `HOW_TO_USE_PHASE_2_PROMPT.md` — FAQ
2. См. `DEVELOPER_CHEAT_SHEET.md` — troubleshooting
3. См. `DOCUMENTATION_INDEX.md` — навигация

**Если промпт не выполняется:**
1. Проверить что скопировали весь файл
2. Убедиться что LLM поддерживает длинные промпты
3. Разбить на части (по PART 1-6)

**Если тесты падают:**
1. См. секцию TROUBLESHOOTING в промпте
2. Проверить синтаксис PHP: `php -l файл.php`
3. Убедиться что все зависимости установлены: `composer install`

---

**ГОТОВ К ВЫПОЛНЕНИЮ. РЕКОМЕНДУЕТСЯ НАЧАТЬ С PHASE 2.1.**

---

**Дата создания:** 18 октября 2025  
**Автор:** GitHub Copilot  
**Следующий review:** После завершения Phase 2.1

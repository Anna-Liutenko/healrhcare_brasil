# ⚡ БЫСТРОЕ ИСПРАВЛЕНИЕ: Предустановленные страницы

> **Время выполнения:** 10 минут  
> **Приоритет:** 🔴 КРИТИЧЕСКИЙ  
> **Статус:** Готово к применению

---

## 🎯 Проблема

1. ❌ Не могу войти в CMS (авторизация не работает)
2. ❌ Кириллица может отображаться некорректно
3. ❌ Не могу редактировать страницы

---

## 🔍 Корневая причина

**Frontend обращается к НЕСУЩЕСТВУЮЩЕМУ серверу!**

```javascript
// frontend/api-client.js (строка 12)
const API_BASE_URL = 'http://localhost:8002';  // ❌ НЕТ СЕРВЕРА!
```

**Реальный сервер:**
```
Apache XAMPP работает на: http://localhost:80
Backend API: http://localhost/healthcare-cms-backend/public
```

---

## ✅ ИСПРАВЛЕНИЕ (3 файла)

### 1️⃣ Исправить порт API

**Файл:** `frontend/api-client.js`

**Найти строку 11-13:**
```javascript
const API_BASE_URL = window.location.hostname === 'localhost'
    ? 'http://localhost:8002'
    : '/healthcare-cms-backend/public';
```

**Заменить на:**
```javascript
const API_BASE_URL = window.location.hostname === 'localhost'
    ? 'http://localhost/healthcare-cms-backend/public'
    : '/api';
```

---

### 2️⃣ Установить пароль

**Запустить в PowerShell:**
```powershell
cd "c:\Users\annal\Documents\Мои сайты\Сайт о здравоохранении в Бразилии\Разработка сайта с CMS\backend\scripts"

& "C:\xampp\php\php.exe" reset_password.php anna password
```

**Результат:**
```
✅ Пароль для пользователя anna успешно обновлен
```

---

### 3️⃣ Проверить БД (опционально)

**Проверить наличие страниц:**
```powershell
& "C:\xampp\mysql\bin\mysql.exe" -uroot healthcare_cms -e "SELECT COUNT(*) as pages_count FROM pages; SELECT COUNT(*) as blocks_count FROM blocks;"
```

**Проверить кириллицу:**
```powershell
& "C:\xampp\mysql\bin\mysql.exe" -uroot healthcare_cms -e "SELECT title FROM pages LIMIT 5;"
```

Если кириллица отображается как `???` или кракозябры:
```powershell
& "C:\xampp\mysql\bin\mysql.exe" -uroot healthcare_cms -e "SHOW VARIABLES LIKE 'character%';"
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Авторизация
1. Открыть: http://localhost/healthcare-cms-frontend/index.html
2. Ввести:
   - **Username:** anna
   - **Password:** password
3. Нажать "Войти"

**Ожидается:** 
- ✅ Вход выполнен
- ✅ Отображается "👤 anna" в шапке
- ✅ Видны страницы (если есть в БД)

---

### Тест 2: Список страниц
1. После входа проверить список страниц
2. Названия должны отображаться на кириллице корректно

**Если пусто:**
Создать тестовую страницу вручную в редакторе или запустить seed:
```powershell
cd backend
& "C:\xampp\mysql\bin\mysql.exe" -uroot healthcare_cms < "..\database\seeds\SEED_DATA.sql"
```

---

### Тест 3: Визуальный редактор
1. Кликнуть "Редактировать" на любой странице
2. Проверить загрузку блоков
3. Изменить текст → Сохранить
4. Перезагрузить → проверить сохранение

---

## 🐛 Отладка (если не работает)

### Проблема: "Ошибка входа"

**Проверить в DevTools → Network:**
```
POST http://localhost/healthcare-cms-backend/public/api/auth/login
```

Если видно `localhost:8002` → файл `api-client.js` НЕ обновлён!

---

### Проблема: "404 Not Found"

**Проверить Apache запущен:**
```powershell
# В XAMPP Control Panel:
# Apache должен быть зелёным
```

**Проверить файлы скопированы:**
```powershell
Test-Path "C:\xampp\htdocs\healthcare-cms-backend\public\index.php"
Test-Path "C:\xampp\htdocs\healthcare-cms-frontend\index.html"
```

Если `False` → запустить синхронизацию:
```powershell
.\sync-to-xampp.ps1
```

---

### Проблема: Кракозябры вместо кириллицы

**1. Проверить кодировку БД:**
```sql
SHOW VARIABLES LIKE 'character%';
-- Должно быть: utf8mb4
```

**2. Проверить кодировку таблицы:**
```sql
SHOW TABLE STATUS WHERE Name='pages';
-- Collation: utf8mb4_unicode_ci
```

**3. Если неправильная кодировка:**
```sql
ALTER DATABASE healthcare_cms CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
ALTER TABLE pages CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
ALTER TABLE blocks CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

## 📊 Контрольный список

После исправлений проверить:

- [ ] `frontend/api-client.js` использует `localhost:80`
- [ ] Пароль `anna` установлен на `password`
- [ ] Apache запущен в XAMPP
- [ ] Вход в CMS работает (anna / password)
- [ ] Список страниц загружается
- [ ] Кириллица отображается корректно
- [ ] Можно редактировать и сохранять страницы
- [ ] Slug не меняется при сохранении

---

## 🎓 Дополнительная информация

**Полный research:**
- `docs/RESEARCH_PREINSTALLED_PAGES_ISSUES_OCT_2025.md`

**История проблем:**
- `docs/RESOLVED_ISSUES.md`
- `docs/DEBUG_HISTORY_BLOCKS_BUG_OCT_2025.md`
- `docs/trableshooting/Отладка.md`

**Архитектура:**
- `docs/DATABASE_SCHEMA.md`
- `docs/API_ENDPOINTS_CHEATSHEET.md`

---

**Последнее обновление:** 9 октября 2025  
**Автор:** AI Assistant

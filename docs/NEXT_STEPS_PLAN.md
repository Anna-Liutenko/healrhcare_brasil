# План следующих шагов — Реализация чистой архитектуры

**Дата создания:** 16 октября 2025  
**Статус:** Готов к выполнению  
**Основа:** Результаты диагностики + архитектурный анализ

---

## 🎯 Текущая ситуация

### Что выполнено
- ✅ **Фаза 0 (диагностика):** Причина "Block not found" установлена — frontend отправляет несуществующий blockId
- ✅ **Архитектурный аудит:** Выявлено 63+ нарушения DIP (Controllers создают репозитории напрямую)
- ✅ **Документация:** Создан план рефакторинга с примерами кода и временными оценками

### Текущая проблема
**Immediate issue:** Inline-редактор не работает из-за рассинхрона данных (frontend ↔ backend)

**Root cause:** Архитектурные нарушения:
1. UseCase бросает generic `InvalidArgumentException` без контекста
2. Controller не различает типы ошибок (всегда 500)
3. Нет Domain Exceptions с контекстом для отладки
4. Жёсткая привязка к конкретным репозиториям (невозможно тестировать/логировать)

---

## 📋 План действий (2 трека: Quick Fix + Long-term)

### 🔥 Трек 1: Немедленное исправление (1-2 часа)

**Цель:** Восстановить работу inline-редактора СЕГОДНЯ

#### Вариант A: Исправить данные (рекомендуется, 30-60 минут)

**Шаг 1.1:** Проверить frontend — откуда берётся blockId
```bash
# Найти где InlineEditorManager получает blockId
grep -r "data-block-id" frontend/
grep -r "blockId" frontend/js/InlineEditorManager.js
```

**Шаг 1.2:** Проверить HTML страницы
- Открыть страницу `9c23c3ff-...` в браузере
- Inspect element → найти `data-block-id="f34cac9d-..."`
- Проверить: есть ли такой атрибут на странице?

**Шаг 1.3:** Если атрибут есть → пересоздать блоки
```sql
-- Проверить откуда пришли текущие блоки
SELECT id, type, position, created_at 
FROM blocks 
WHERE page_id = '9c23c3ff-1e2f-44fa-880f-c92b66a63257'
ORDER BY position;

-- Если нужно — удалить и пересоздать через импорт шаблона
-- (через UI: Templates → Import → выбрать шаблон)
```

**Шаг 1.4:** Если атрибута нет → проблема в рендеринге
- Проверить `PublicPageController::show()` — как генерируется HTML
- Убедиться что блоки правильно рендерятся с `data-block-id`

**Ожидаемый результат:**
- ✅ Inline-редактор работает
- ✅ Пользователь может редактировать блоки
- ⚠️ Архитектурные проблемы НЕ решены (только данные)

**Время:** 30-60 минут

---

#### Вариант B: Временный workaround в коде (10 минут, не рекомендуется)

**Создать блок-заглушку в БД:**
```sql
-- ВРЕМЕННОЕ РЕШЕНИЕ (удалить после рефакторинга)
INSERT INTO blocks (id, page_id, type, position, data, created_at, updated_at)
VALUES (
    'f34cac9d-b426-4b22-887a-3a194f06eba1',
    '9c23c3ff-1e2f-44fa-880f-c92b66a63257',
    'text-block',
    99,
    '{"title": "Temporary placeholder", "paragraphs": []}',
    NOW(),
    NOW()
);
```

**⚠️ Недостатки:**
- Не решает root cause
- Создаёт технический долг
- Может сломаться при следующем импорте

**Время:** 10 минут

---

### 🏗️ Трек 2: Долгосрочное решение (30-40 часов MVP)

**Цель:** Внедрить чистую архитектуру с Domain Exceptions и DI Container

---

## 🗓️ Детальный план (5 фаз)

### 📦 Фаза 1: Infrastructure (10-14 часов)

**Цель:** Создать фундамент для DI и Domain Exceptions

#### 1.1. Проверить/создать Domain интерфейсы (2 часа)

**Задача:** Убедиться что все репозитории имеют интерфейсы в Domain слое

**Файлы для проверки:**
```bash
backend/src/Domain/Repository/
├── PageRepositoryInterface.php
├── BlockRepositoryInterface.php
├── UserRepositoryInterface.php
├── SessionRepositoryInterface.php
├── MediaRepositoryInterface.php
├── MenuRepositoryInterface.php
└── SettingsRepositoryInterface.php
```

**Если отсутствуют — создать:**
- Скопировать код из `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.2, Этап 1)
- Убедиться что `MySQLPageRepository implements PageRepositoryInterface`
- Запустить `php -l` для проверки синтаксиса

**Критерий завершения:**
- ✅ Все 7 интерфейсов существуют
- ✅ Все MySQL репозитории реализуют интерфейсы
- ✅ PHPStan не выдаёт ошибок типизации

**Время:** 2 часа

---

#### 1.2. Создать Domain Exceptions (1 час)

**Задача:** Типизированные исключения с контекстом для отладки

**Файлы для создания:**
```
backend/src/Domain/Exception/
├── BlockNotFoundException.php
├── PageNotFoundException.php
└── (опционально) UnauthorizedException.php
```

**Код взять из:** `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.2, Этап 2)

**Ключевые требования:**
- Extends `DomainException` (не `Exception`)
- Метод `getContext(): array` для отладки
- Фабричный метод `forBlockId(string $id, string $pageId): self`

**Критерий завершения:**
- ✅ Exceptions созданы и работают
- ✅ Можно получить контекст: `$e->getContext()` → `['blockId' => '...', 'pageId' => '...']`

**Время:** 1 час

---

#### 1.3. Создать DI Container (2-3 часа)

**Задача:** Простой DI Container с bind/singleton/get/make

**Файл:** `backend/src/Infrastructure/Container/Container.php`

**Код взять из:** `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.2, Этап 3)

**Ключевые методы:**
- `bind(string $abstract, callable $factory): void` — регистрация (новый каждый раз)
- `singleton(string $abstract, callable $factory): void` — регистрация (один экземпляр)
- `get(string $abstract): object` — получение зарегистрированного
- `make(string $class): object` — autowiring (создание с автоинжекцией)

**Критерий завершения:**
- ✅ Container работает
- ✅ Можно зарегистрировать репозиторий: `$container->singleton(PageRepositoryInterface::class, fn() => new MySQLPageRepository())`
- ✅ Можно получить: `$repo = $container->get(PageRepositoryInterface::class)`
- ✅ Autowiring работает для контроллеров

**Опционально:** Unit-тесты для Container (1 час)

**Время:** 2-3 часа

---

#### 1.4. Создать bootstrap/container.php (2-3 часа)

**Задача:** Централизованная конфигурация всех зависимостей

**Файл:** `backend/bootstrap/container.php`

**Код взять из:** `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.2, Этап 4)

**Что регистрировать:**
1. **Repositories (singleton):**
   - PageRepositoryInterface → MySQLPageRepository
   - BlockRepositoryInterface → MySQLBlockRepository
   - UserRepositoryInterface → MySQLUserRepository
   - и т.д. (всего 7 репозиториев)

2. **Services (singleton):**
   - MarkdownConverter
   - HTMLSanitizer

3. **Use Cases (bind, не singleton):**
   - UpdatePageInline
   - GetPageWithBlocks
   - GetAllPages
   - PublishPage

**Критерий завершения:**
- ✅ Файл создан и возвращает `$container`
- ✅ Можно подключить в index.php: `$container = require __DIR__ . '/../bootstrap/container.php';`
- ✅ Smoke-тест: `$repo = $container->get(PageRepositoryInterface::class); var_dump($repo);` → видим `MySQLPageRepository`

**Время:** 2-3 часа

---

#### 1.5. Создать DTO классы (2 часа)

**Задача:** Типобезопасные Request/Response для Use Cases

**Файлы:**
```
backend/src/Application/DTO/
├── UpdatePageInlineRequest.php
└── UpdatePageInlineResponse.php
```

**Код взять из:** `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.2, Этап 5)

**Требования:**
- `final class` (immutable)
- `readonly` properties (PHP 8.1+) или private + getters
- Валидация в конструкторе

**Критерий завершения:**
- ✅ DTO созданы
- ✅ Можно создать: `$req = new UpdatePageInlineRequest($pageId, $blockId, $fieldPath, $markdown);`
- ✅ Валидация работает (выбрасывает `InvalidArgumentException` при невалидных данных)

**Время:** 2 часа

---

**Итого Фаза 1:** 10-14 часов

**Критерий завершения фазы:**
- ✅ DI Container работает
- ✅ Все Domain интерфейсы и Exceptions созданы
- ✅ bootstrap/container.php конфигурирует зависимости
- ✅ DTO для UpdatePageInline готовы

---

### 🔄 Фаза 2: Refactoring Use Cases (10-14 часов)

**Цель:** Use Cases используют интерфейсы, DTO и Domain Exceptions

#### 2.1. Рефакторинг UpdatePageInline (2-3 часа)

**Задача:** Переписать Use Case с правильной архитектурой

**Файл:** `backend/src/Application/UseCase/UpdatePageInline.php`

**Код взять из:** `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.3, Этап 6)

**Ключевые изменения:**
1. Constructor injection интерфейсов (не конкретных репозиториев)
2. Принимает `UpdatePageInlineRequest` (не 4 отдельных параметра)
3. Возвращает `UpdatePageInlineResponse` (не массив)
4. Выбрасывает `PageNotFoundException` / `BlockNotFoundException` (не `InvalidArgumentException`)

**Было:**
```php
public function execute(string $pageId, string $blockId, string $fieldPath, string $newMarkdown): array
```

**Стало:**
```php
public function execute(UpdatePageInlineRequest $request): UpdatePageInlineResponse
```

**Критерий завершения:**
- ✅ Use Case рефакторен
- ✅ Добавлен в `bootstrap/container.php`
- ✅ Можно получить через: `$useCase = $container->get(UpdatePageInline::class);`

**Время:** 2-3 часа

---

#### 2.2. Написать интеграционный тест (2 часа)

**Задача:** Тест с mock репозиториями через Container

**Файл:** `backend/tests/Integration/UseCase/UpdatePageInlineTest.php`

**Что тестировать:**
1. **Happy path:** блок существует → обновляется
2. **Page not found:** выбрасывает `PageNotFoundException`
3. **Block not found:** выбрасывает `BlockNotFoundException` (наш текущий кейс!)
4. **Invalid field path:** выбрасывает `InvalidArgumentException`

**Пример теста:**
```php
public function test_throws_block_not_found_when_block_does_not_exist()
{
    // Arrange: mock repo возвращает пустой список блоков
    $mockBlockRepo = new InMemoryBlockRepository([]);
    
    $useCase = new UpdatePageInline(
        $mockPageRepo,
        $mockBlockRepo,
        $markdownConverter,
        $sanitizer
    );
    
    $request = new UpdatePageInlineRequest(
        pageId: '9c23c3ff-...',
        blockId: 'f34cac9d-...', // несуществующий
        fieldPath: 'data.paragraphs[0]',
        newMarkdown: 'test'
    );
    
    // Act & Assert
    $this->expectException(BlockNotFoundException::class);
    $this->expectExceptionMessage('Block not found');
    
    $useCase->execute($request);
    
    // Verify context
    try {
        $useCase->execute($request);
    } catch (BlockNotFoundException $e) {
        $this->assertEquals('f34cac9d-...', $e->getContext()['blockId']);
        $this->assertEquals('9c23c3ff-...', $e->getContext()['pageId']);
    }
}
```

**Критерий завершения:**
- ✅ Тесты написаны (минимум 4 кейса)
- ✅ Тесты проходят: `vendor/bin/phpunit tests/Integration/UseCase/UpdatePageInlineTest.php`
- ✅ Coverage UseCase >80%

**Время:** 2 часа

---

#### 2.3. Рефакторинг других Use Cases (4-6 часов)

**Задача:** Применить тот же паттерн для остальных Use Cases

**Список (по приоритету):**
1. **GetPageWithBlocks** (1 час)
2. **GetAllPages** (1 час)
3. **PublishPage** (1 час)
4. **CreatePage** (1-2 часа)
5. **UpdatePage** (1 час)
6. Остальные (опционально)

**Для каждого:**
- Constructor injection интерфейсов
- DTO для входа/выхода (если применимо)
- Domain Exceptions
- Добавить в `bootstrap/container.php`

**Критерий завершения:**
- ✅ Минимум 4 критичных Use Cases рефакторены
- ✅ Все зарегистрированы в Container
- ✅ Базовые тесты написаны

**Время:** 4-6 часов

---

#### 2.4. Регрессионное тестирование (2 часа)

**Задача:** Убедиться что ничего не сломалось

**Действия:**
- Запустить все существующие тесты: `vendor/bin/phpunit`
- Проверить E2E тесты (если есть)
- Вручную протестировать критичные сценарии в UI

**Критерий завершения:**
- ✅ Все существующие тесты проходят
- ✅ Нет регрессий в функциональности

**Время:** 2 часа

---

**Итого Фаза 2:** 10-14 часов

**Критерий завершения фазы:**
- ✅ UpdatePageInline и 3-4 других Use Cases рефакторены
- ✅ Use Cases используют интерфейсы и DTO
- ✅ Domain Exceptions работают
- ✅ Интеграционные тесты написаны

---

### 🎯 Фаза 3: Refactoring Controllers (9-12 часов)

**Цель:** Controllers получают Use Cases через DI

#### 3.1. Рефакторинг PageController (3-4 часа)

**Задача:** Constructor injection Use Cases, обработка Domain Exceptions

**Файл:** `backend/src/Presentation/Controller/PageController.php`

**Код взять из:** `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.4, Этап 8)

**Ключевые изменения:**
1. **Constructor injection:**
   ```php
   public function __construct(
       private readonly UpdatePageInline $updatePageInline,
       private readonly GetPageWithBlocks $getPageWithBlocks,
       private readonly GetAllPages $getAllPages,
       // ...
   ) {}
   ```

2. **Обработка Domain Exceptions:**
   ```php
   try {
       $request = new UpdatePageInlineRequest(...);
       $response = $this->updatePageInline->execute($request);
       $this->jsonResponse($response->toArray(), 200);
   } catch (PageNotFoundException $e) {
       $this->jsonResponse(['error' => $e->getMessage(), 'context' => $e->getContext()], 404);
   } catch (BlockNotFoundException $e) {
       $this->jsonResponse(['error' => $e->getMessage(), 'context' => $e->getContext()], 404);
   } catch (InvalidArgumentException $e) {
       $this->jsonResponse(['error' => $e->getMessage()], 400);
   }
   ```

3. **Удалить все `new MySQLRepository()`**

**Критерий завершения:**
- ✅ PageController рефакторен
- ✅ Нет прямых инстанцирований репозиториев
- ✅ Domain Exceptions обрабатываются с правильными HTTP кодами
- ✅ Inline-редактирование теперь возвращает 404 вместо 500 при "Block not found"

**Время:** 3-4 часа

---

#### 3.2. Обновить index.php (2 часа)

**Задача:** Точка входа использует Container для создания контроллеров

**Файл:** `backend/public/index.php`

**Код взять из:** `CLEAN_ARCHITECTURE_VIOLATIONS_ANALYSIS.md` (раздел 2.4, Этап 9)

**Ключевые изменения:**
1. Загрузить Container:
   ```php
   $container = require __DIR__ . '/../bootstrap/container.php';
   ```

2. Создавать контроллеры через Container:
   ```php
   // Было:
   $controller = new PageController();
   
   // Стало:
   $controller = $container->make(PageController::class);
   ```

3. Глобальный exception handler:
   ```php
   } catch (DomainException $e) {
       http_response_code(404);
       echo json_encode(['error' => $e->getMessage(), 'context' => method_exists($e, 'getContext') ? $e->getContext() : []]);
   } catch (InvalidArgumentException $e) {
       http_response_code(400);
       echo json_encode(['error' => $e->getMessage()]);
   } catch (Exception $e) {
       http_response_code(500);
       echo json_encode(['error' => 'Internal server error', 'message' => $e->getMessage()]);
   }
   ```

**Критерий завершения:**
- ✅ index.php загружает Container
- ✅ Контроллеры создаются через `$container->make()`
- ✅ Глобальный exception handler работает

**Время:** 2 часа

---

#### 3.3. E2E тестирование PageController (2 часа)

**Задача:** Проверить все endpoints PageController

**Тест-кейсы:**
1. **GET /api/pages** → 200, список страниц
2. **GET /api/pages/{id}** → 200, страница с блоками
3. **PATCH /api/pages/{id}/inline** (блок существует) → 200, успех
4. **PATCH /api/pages/{id}/inline** (блок НЕ существует) → **404 (не 500!)**, с контекстом
5. **POST /api/pages** → 201, создание
6. **PUT /api/pages/{id}** → 200, обновление
7. **DELETE /api/pages/{id}** → 200, удаление

**Особое внимание:**
- ✅ "Block not found" теперь возвращает **404** с контекстом: `{"error": "Block not found", "context": {"blockId": "...", "pageId": "..."}}`

**Критерий завершения:**
- ✅ Все 7 endpoint'ов работают
- ✅ Inline-редактирование возвращает корректные коды
- ✅ Нет регрессий

**Время:** 2 часа

---

#### 3.4. Рефакторинг AuthController (3 часа)

**Задача:** Применить тот же паттерн для AuthController

**Файл:** `backend/src/Presentation/Controller/AuthController.php`

**Действия:**
1. Constructor injection Use Cases (Login, Logout, GetCurrentUser)
2. Обработка Domain Exceptions (`UnauthorizedException`)
3. Удалить `new MySQLUserRepository()`, `new MySQLSessionRepository()`

**Критерий завершения:**
- ✅ AuthController рефакторен
- ✅ Аутентификация работает
- ✅ POST /api/auth/login, POST /api/auth/logout, GET /api/auth/me — протестированы

**Время:** 3 часа

---

**Итого Фаза 3:** 9-12 часов

**Критерий завершения фазы:**
- ✅ PageController и AuthController полностью на DI
- ✅ index.php использует Container
- ✅ Inline-редактирование работает с правильными HTTP кодами (404 вместо 500)
- ✅ E2E тесты проходят

---

### 🧪 Фаза 4: Testing & Documentation (14-18 часов)

**Опционально, но важно для долгосрочной поддержки**

#### 4.1. Unit-тесты для Use Cases (4 часа)
- UpdatePageInline с mock репозиториями
- GetPageWithBlocks
- PublishPage
- Цель: coverage >70%

#### 4.2. Integration тесты Controllers (4 часа)
- PageController через HTTP
- AuthController

#### 4.3. Coverage report (1 час)
- Запустить PHPUnit с coverage
- Анализ пробелов

#### 4.4. Документация ARCHITECTURE_GUIDELINES.md (4 часа)
- Диаграмма слоёв
- Правила зависимостей
- Примеры кода
- FAQ

#### 4.5. Обновить README.md (1 час)
- Описание архитектуры
- Инструкции по запуску

**Итого Фаза 4:** 14-18 часов

---

### 🛡️ Фаза 5: Protection (6-8 часов)

**Опционально, защита от будущих нарушений**

#### 5.1. Настроить Deptrac (3 часа)
- Установка
- Конфигурация deptrac.yaml
- Исправление нарушений

#### 5.2. Добавить в CI/CD (2 часа)
- GitHub Actions / GitLab CI
- Автозапуск Deptrac

#### 5.3. PHPStan custom rules (3 часа, опционально)
- Запретить `new MySQLRepository()` вне bootstrap
- Правила для Use Cases

**Итого Фаза 5:** 6-8 часов

---

## 📊 Итоговая таблица

| Фаза | Описание | Время | Приоритет | Результат |
|------|----------|-------|-----------|-----------|
| **0** | Диагностика | ✅ 20 мин | 🔥 Критично | Причина установлена |
| **Quick Fix** | Исправление данных | 30-60 мин | 🔥 Срочно | Inline-редактор работает |
| **1** | Infrastructure | 10-14 ч | 🔥 Критично | DI Container готов |
| **2** | Use Cases | 10-14 ч | 🔥 Критично | Domain Exceptions работают |
| **3** | Controllers | 9-12 ч | 🔥 Критично | Правильные HTTP коды |
| **4** | Testing & Docs | 14-18 ч | ⚠️ Важно | Coverage >70% |
| **5** | Protection | 6-8 ч | ℹ️ Желательно | Автоматические проверки |

**MVP (минимум для работы):** Фазы 0 + Quick Fix + 1 + 2 + 3 = **30-40 часов**  
**Полная реализация:** Все фазы = **56-74 часа**

---

## 🚀 Рекомендуемый порядок выполнения

### День 1 (сегодня, 1-2 часа)
1. ✅ Выполнить Quick Fix (Трек 1, Вариант A)
2. ✅ Убедиться что inline-редактор работает
3. ✅ Commit: "fix: resolve block not found data mismatch"

### Неделя 1 (Фаза 1)
1. День 1-2: Domain интерфейсы + Exceptions + DI Container
2. День 3: bootstrap/container.php + DTO
3. День 4: Smoke-тестирование инфраструктуры

### Неделя 2 (Фаза 2)
1. День 1-2: Рефакторинг UpdatePageInline + тесты
2. День 3-4: Рефакторинг других Use Cases
3. День 5: Регрессионное тестирование

### Неделя 3 (Фаза 3)
1. День 1-2: PageController + index.php
2. День 3: E2E тестирование
3. День 4-5: AuthController + остальные контроллеры (опционально)

### Неделя 4-5 (Фаза 4, опционально)
1. Testing
2. Documentation

### Неделя 6+ (Фаза 5, опционально)
1. Deptrac
2. CI/CD

---

## ✅ Критерии успеха

**Immediate (Quick Fix):**
- ✅ Inline-редактор работает
- ✅ Нет ошибок при редактировании блоков

**Short-term (Фазы 1-3):**
- ✅ "Block not found" возвращает **404** (не 500) с контекстом
- ✅ Можно легко добавить логирование через decorator
- ✅ Можно написать unit-тесты для Use Cases с mock репозиториями
- ✅ DI Container автоматически инжектит зависимости

**Long-term (Фазы 4-5):**
- ✅ Coverage >70%
- ✅ Документация для новых разработчиков
- ✅ Автоматические проверки архитектуры в CI/CD
- ✅ Невозможно случайно нарушить архитектуру

---

## 📞 Следующий шаг

**Что делать прямо сейчас:**

1. **Выбрать трек:**
   - **Трек A (рекомендуется):** Quick Fix → проверить frontend/данные (30-60 мин)
   - **Трек B:** Сразу начать Фазу 1 (долгосрочно, 10-14 часов)

2. **Если выбран Трек A:**
   - Начать с проверки frontend: `grep -r "data-block-id" frontend/`
   - Открыть страницу в браузере → Inspect → найти blockId
   - Если нужно — пересоздать блоки через UI (Templates → Import)

3. **После Quick Fix:**
   - Commit изменений
   - Начать Фазу 1.1 (проверка Domain интерфейсов)

---

**Готово к выполнению.** Скажите, какой трек выбираете, и я начну реализацию.

# Быстрая инструкция: Деплой исправления кнопки

**Дата:** 12 октября 2025

---

## ✅ Что исправлено

1. Кнопка теперь центрируется и имеет автоматическую ширину (зависит от текста)
2. Исправлена синтаксическая ошибка в `editor.js` (класс UploadAdapter)
3. Публичные страницы и preview редактора рендерятся одинаково

---

## 🚀 Как задеплоить

### Вариант 1: Закрыть браузер и скопировать файлы

```powershell
# 1. Закройте ВСЕ вкладки браузера с редактором

# 2. Скопируйте файлы
Copy-Item "frontend\editor.js" -Destination "C:\xampp\htdocs\visual-editor-standalone\editor.js" -Force

Copy-Item "frontend\editor-preview.css" -Destination "C:\xampp\htdocs\visual-editor-standalone\editor-preview.css" -Force

Copy-Item "backend\src\Presentation\Controller\PublicPageController.php" -Destination "C:\xampp\htdocs\healthcare-cms-backend\src\Presentation\Controller\PublicPageController.php" -Force
```

### Вариант 2: Если файлы заблокированы - перезагрузка Apache

```powershell
# 1. Откройте XAMPP Control Panel
# 2. Нажмите Stop напротив Apache
# 3. Подождите 2 секунды
# 4. Скопируйте файлы (команды выше)
# 5. Нажмите Start напротив Apache
```

### Вариант 3: Если ничего не помогло - переименуйте файлы

```powershell
# Переименуем файлы в XAMPP для разблокировки
Rename-Item "C:\xampp\htdocs\visual-editor-standalone\editor.js" -NewName "editor.js.old" -Force

# Теперь скопируем новый
Copy-Item "frontend\editor.js" -Destination "C:\xampp\htdocs\visual-editor-standalone\editor.js" -Force

# То же для CSS
Rename-Item "C:\xampp\htdocs\visual-editor-standalone\editor-preview.css" -NewName "editor-preview.css.old" -Force
Copy-Item "frontend\editor-preview.css" -Destination "C:\xampp\htdocs\visual-editor-standalone\editor-preview.css" -Force
```

---

## 🔍 Проверка

1. Откройте редактор: `http://localhost/visual-editor-standalone/editor.html?v=1.3`
2. Нажмите `Ctrl + F5` (жёсткое обновление)
3. Добавьте блок "Button"
4. Проверьте:
   - ✅ Кнопка по центру
   - ✅ Ширина зависит от текста
   - ✅ Нет ошибок в консоли браузера (F12)

---

## ❌ Если не работает

**Проблема:** Файлы заблокированы  
**Решение:** Перезагрузите компьютер (крайняя мера)

**Проблема:** Кнопка всё ещё растянута  
**Решение:** Очистите кэш браузера (`Ctrl + Shift + Delete`)

**Проблема:** Ошибки в консоли  
**Решение:** Проверьте синтаксис в `editor.js` - должен быть класс `UploadAdapter`, а не стрелочная функция

---

**Готово!** После деплоя кнопка будет работать правильно.

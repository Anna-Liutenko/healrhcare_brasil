# 🧪 Как протестировать изменения на XAMPP

## ✅ Код уже обновлен!

Все измененные файлы скопированы в:
```
C:\xampp\htdocs\healthcare-cms-backend\
```

---

## 🌐 Шаг 1: Откройте ваш проект в браузере

Откройте URL, который вы обычно используете:

```
https://localhost/healthcare-cms-backend/public/
```

или

```
http://localhost/healthcare-cms-backend/public/
```

---

## 🔍 Шаг 2: Проверьте API endpoints в браузере DevTools

### 1. Откройте админ-панель CMS

Залогиньтесь как обычно в вашу CMS.

### 2. Откройте DevTools (F12)

Перейдите на вкладку **Network**.

### 3. Выполните действия:

- **Откройте список страниц** → проверьте запрос к `GET /api/pages`
- **Создайте новую страницу** → проверьте запрос к `POST /api/pages`
- **Откройте страницу для редактирования** → проверьте запрос к `GET /api/pages/:id`

### 4. Проверьте ответы (Response):

✅ **Правильно (camelCase):**
```json
{
  "pages": [
    {
      "id": "...",
      "title": "...",
      "createdAt": "...",      ← camelCase ✅
      "updatedAt": "...",      ← camelCase ✅
      "createdBy": "...",      ← camelCase ✅
      "showInMenu": true       ← camelCase ✅
    }
  ]
}
```

❌ **Неправильно (snake_case) - НЕ должно быть:**
```json
{
  "pages": [
    {
      "created_at": "...",     ← snake_case ❌
      "updated_at": "...",     ← snake_case ❌
      "show_in_menu": true     ← snake_case ❌
    }
  ]
}
```

---

## 🎯 Что проверить:

### ✓ Список страниц (GET /api/pages)
- Все ключи должны быть в **camelCase**
- `createdAt`, `updatedAt`, `createdBy`, `showInMenu`

### ✓ Создание страницы (POST /api/pages)
- Response должен содержать `pageId` (НЕ `page_id`)

### ✓ Получение страницы (GET /api/pages/:id)
- Объект page: все ключи camelCase
- Массив blocks: `pageId`, `customName` (camelCase)

### ✓ Меню (GET /api/menu)
- `menuItemId`, `pageId`, `externalUrl`, `parentId`, `displayName`

### ✓ Авторизация (POST /api/auth/login)
- User object: все ключи camelCase

---

## 🐛 Если что-то не работает:

### Проблема: Вижу ошибки PHP

**Решение:** Проверьте логи:
```
C:\xampp\apache\logs\error.log
```

### Проблема: Вижу старые ответы (snake_case)

**Решение:** Очистите кэш браузера (Ctrl+Shift+Del) и перезагрузите страницу.

### Проблема: Страница не загружается

**Решение:** 
1. Проверьте что Apache запущен в XAMPP Control Panel
2. Проверьте что MySQL запущен
3. Проверьте .htaccess в `public/` директории

---

## 📊 Альтернатива: Тест через Postman

Если хотите протестировать API напрямую:

### 1. Установите Postman (если нет)

Скачайте: https://www.postman.com/downloads/

### 2. Импортируйте эти запросы:

**Login:**
```
POST https://localhost/healthcare-cms-backend/public/api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "ваш_пароль"
}
```

**Get Pages:**
```
GET https://localhost/healthcare-cms-backend/public/api/pages
Authorization: Bearer {token_из_login}
```

**Create Page:**
```
POST https://localhost/healthcare-cms-backend/public/api/pages
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "Test Page",
  "slug": "test-page-" + Date.now(),
  "type": "regular",
  "status": "draft",
  "createdBy": "ваш_user_id"
}
```

---

## ✅ Критерии успеха:

После тестирования вы должны видеть:

1. ✅ Все ключи в JSON ответах - **camelCase** (нет underscore `_`)
2. ✅ Frontend работает без ошибок
3. ✅ Создание/редактирование страниц работает
4. ✅ Меню отображается корректно
5. ✅ Авторизация работает

---

## 📝 Отчет о проблемах

Если найдете проблемы, сообщите:
- Какой endpoint
- Какой ключ в snake_case
- Скриншот из DevTools Network tab

**Готово к тестированию!** 🚀
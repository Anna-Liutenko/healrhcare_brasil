# 🔍 ОТЧЕТ О ГЛУБОКОМ ИССЛЕДОВАНИИ ПРОБЛЕМ ПОСЛЕ ВОССТАНОВЛЕНИЯ БД

**Дата:** 10 ноября 2025
**Ветка:** `claude/investigate-repository-issue-011CV174teFMnz9VagW1GucA`
**Статус:** Исследование завершено, готов план исправления

---

## 📋 EXECUTIVE SUMMARY

После восстановления базы данных обнаружены **три критические группы проблем**:

1. ✅ **Рендеринг HTML на публичных страницах** — поле `rendered_html` пустое для всех страниц
2. ⚠️ **Страница-коллекция** — работает, но имеет логические ошибки
3. 🔴 **Медиатека** — таблица `media` пуста, файлы в неправильной директории

**Общий вывод:** Проблема не в одной точке. Требуется комплексное исправление.

---

## 🔴 ПРОБЛЕМА #1: Рендеринг HTML на публичных страницах

### Описание проблемы

После восстановления базы данных поле `pages.rendered_html` **пустое для всех опубликованных страниц**.

**Как это работает в норме:**
1. Пользователь редактирует страницу в визуальном редакторе (frontend)
2. Нажимает "Опубликовать" → вызывается API `POST /api/pages/{id}/publish`
3. Backend генерирует полный HTML из блоков страницы
4. Сохраняет HTML в `pages.rendered_html`
5. При запросе публичной страницы отдает готовый HTML напрямую (fast path)

**Что происходит сейчас:**
1. Поле `rendered_html` = `NULL` для всех страниц
2. Публичный сайт использует **runtime рендеринг** (медленно, генерирует HTML каждый раз)
3. Runtime рендеринг может давать **другой результат**, чем pre-rendered HTML

**Диагностика (как проверить):**
```bash
# Открыть любую публичную страницу
# Посмотреть исходный код (Ctrl+U)
# Найти комментарий в начале:

<!-- SERVED=runtime | length=... -->  # ❌ ПРОБЛЕМА: используется runtime
<!-- SERVED=pre-rendered | length=... -->  # ✅ OK: используется кэш
```

### Root Cause (корень проблемы)

При восстановлении БД:
- Таблица `pages` была восстановлена из старого бэкапа
- Колонка `rendered_html` существует (добавлена 30 октября), но для всех строк значение `NULL`
- Скрипты регенерации HTML не были запущены

### Затронутые файлы

**Backend (код рендеринга):**
- `backend/src/Presentation/Controller/PublicPageController.php` — контроллер публичных страниц
- `backend/src/Application/UseCase/PublishPage.php` — генерация HTML при публикации
- `backend/src/Application/UseCase/RenderPageHtml.php` — генератор HTML из блоков

**Утилиты восстановления:**
- `backend/tools/regenerate_rendered.php` — регенерация HTML для одной страницы (существует)
- **ОТСУТСТВУЕТ:** скрипт массовой регенерации для всех страниц

### План исправления

#### Шаг 1: Создать скрипт массовой регенерации HTML

**Создать файл:** `/home/user/healrhcare_brasil/backend/scripts/regenerate_all_rendered_html.php`

**Логика:**
1. Загрузить все страницы со `status = 'published'`
2. Для каждой страницы:
   - Вызвать `RenderPageHtml::execute()`
   - Сохранить результат в `pages.rendered_html`
   - Логировать процесс
3. Вывести итоговую статистику

#### Шаг 2: Запустить регенерацию

```bash
cd /home/user/healrhcare_brasil/backend
php scripts/regenerate_all_rendered_html.php
```

#### Шаг 3: Проверить результат

```bash
# Проверить, что rendered_html больше не NULL
# Открыть публичную страницу
# Проверить исходный код — должно быть: <!-- SERVED=pre-rendered -->
```

#### Шаг 4: Добавить автоматическую регенерацию при публикации

**Проверить файл:** `backend/src/Application/UseCase/PublishPage.php`

**Убедиться, что строки 40-53 выполняются:**
```php
try {
    $html = $this->renderPageHtml->execute($page);
    $page->setRenderedHtml($html);
} catch (\Throwable $e) {
    throw new \DomainException('Failed to render page HTML: ' . $e->getMessage());
}
```

---

## ⚠️ ПРОБЛЕМА #2: Страница-коллекция

### Описание проблемы

Страница-коллекция **функционально работает**, но имеет несколько логических ошибок:

1. **Дефолтная секция = 'guides'** вместо 'all materials'
2. **Hardcoded секции** не читаются из `collectionConfig`
3. **Игнорирование `rendered_html`** — коллекции всегда рендерятся динамически
4. **Нет обработки пустой коллекции** — UX проблема

**CSS:** ✅ Работает корректно, проблема НЕ в CSS

### Найденные баги

#### БАГ #2.1: Дефолтная секция 'guides'

**Файл:** `backend/src/Presentation/Controller/PublicPageController.php:609`

**Текущий код:**
```php
$section = $_GET['section'] ?? 'guides'; // default to guides
```

**Проблема:**
- При открытии `/all-materials` пользователь видит только **ГАЙДЫ**
- Ожидается показ **ВСЕХ материалов**

**Исправление:**
```php
$section = $_GET['section'] ?? null; // default to all materials
```

#### БАГ #2.2: Hardcoded секции

**Файл:** `backend/src/Application/UseCase/GetCollectionItems.php:54-61`

**Текущий код:**
```php
$sectionTypeMap = [
    'guides' => ['guide'],
    'articles' => ['article'],
    null => ['guide', 'article']
];
```

**Проблема:**
- Секции жестко заданы в коде
- Невозможно добавить новую секцию через `collectionConfig`

**Исправление:**
Читать секции из `$config['sections']` вместо hardcoded map (требует рефакторинга)

#### БАГ #2.3: Игнорирование rendered_html для коллекций

**Файл:** `backend/src/Presentation/Controller/PublicPageController.php:601-659`

**Проблема:**
- Метод `renderCollectionPage()` не проверяет `$page['renderedHtml']`
- Всегда вызывает `GetCollectionItems` use case
- Увеличивает нагрузку на БД при каждом запросе

**Исправление (опционально):**
Добавить проверку `rendered_html` для коллекций и использовать как кэш

#### БАГ #2.4: Нет обработки пустой коллекции

**Файл:** `backend/src/Application/UseCase/GetCollectionItems.php:130-138`

**Проблема:**
- Если нет статей/гайдов → возвращается `items: []`
- Нет сообщения "Элементов не найдено"
- UX: пользователь видит пустую страницу без объяснений

**Исправление:**
Добавить флаг `isEmpty` и сообщение в ответе

### План исправления

#### Шаг 1: Исправить дефолтную секцию (PRIORITY: HIGH)

**Файл:** `backend/src/Presentation/Controller/PublicPageController.php`

**Строка 609:** Изменить `'guides'` на `null`

#### Шаг 2: Исправить валидацию секции (PRIORITY: HIGH)

**Файл:** `backend/src/Presentation/Controller/PublicPageController.php`

**Строки 611-613:** Обновить валидацию
```php
// Было:
if (!in_array($section, ['guides', 'articles'], true)) {
    $section = 'guides';
}

// Стало:
$allowedSections = ['guides', 'articles', null];
if ($section !== null && !in_array($section, ['guides', 'articles'], true)) {
    $section = null; // Fallback to all
}
```

#### Шаг 3: Добавить обработку пустой коллекции (PRIORITY: MEDIUM)

**Файл:** `backend/src/Application/UseCase/GetCollectionItems.php`

**Добавить после строки 135:**
```php
$result['isEmpty'] = empty($cards);
$result['emptyMessage'] = empty($cards) ? 'Элементов не найдено' : null;
```

#### Шаг 4: Документировать поведение rendered_html (PRIORITY: LOW)

Добавить комментарий в `PublicPageController::renderCollectionPage()` о том, что коллекции всегда рендерятся динамически

---

## 🔴 ПРОБЛЕМА #3: Медиатека

### Описание проблемы

После восстановления БД:
1. ✅ Физические файлы остались на диске
2. 🔴 Таблица `media` в БД **пуста** (записи не восстановились)
3. 🔴 Папка `backend/public/uploads` **не существует**
4. 🔴 Файлы находятся в неправильных директориях

**Результат:**
- Frontend запрашивает `GET /api/media` → получает пустой массив
- Медиатека отображается как пустая
- Загрузка новых файлов **не работает** (папка uploads отсутствует)

### Текущее состояние файловой системы

**Ожидаемая структура:**
```
backend/public/uploads/  ← должны быть файлы
```

**Фактическая структура:**
```
backend/uploads/         ← 3 файла (UUID формат)
  - 1c134f11-9bfc-4186-91c0-819b89bc2b31.jpg
  - 32c79ed4-2ab1-4edc-b260-5d7cecb82230.png
  - e3dfe46c-b12c-48ce-98f1-6276b266bd47.jpg

frontend/uploads/        ← 18 файлов (старый формат)

backend/public/uploads/  ← НЕ СУЩЕСТВУЕТ ❌
```

### Затронутые файлы

**Backend (загрузка):**
- `backend/src/Presentation/Controller/MediaController.php` — контроллер API
- `backend/src/Application/UseCase/UploadMedia.php` — загрузка файлов
- `backend/src/Infrastructure/Repository/MySQLMediaRepository.php` — БД операции

**Frontend (UI):**
- `frontend/media-library.html` — интерфейс медиатеки
- `frontend/media-library.js` — логика отображения

**Утилиты:**
- `backend/scripts/cleanup-missing-media.php` — проверка расхождений БД↔файлы (существует)
- **ОТСУТСТВУЕТ:** скрипт восстановления записей в таблице `media`

### План исправления

#### Шаг 1: Создать правильную папку uploads

```bash
mkdir -p /home/user/healrhcare_brasil/backend/public/uploads
chmod 755 /home/user/healrhcare_brasil/backend/public/uploads
```

#### Шаг 2: Переместить существующие файлы

```bash
# Переместить файлы из backend/uploads
cp -r /home/user/healrhcare_brasil/backend/uploads/* \
      /home/user/healrhcare_brasil/backend/public/uploads/

# Опционально: переместить старые файлы из frontend/uploads
cp -r /home/user/healrhcare_brasil/frontend/uploads/* \
      /home/user/healrhcare_brasil/backend/public/uploads/
```

#### Шаг 3: Создать скрипт восстановления записей в БД

**Создать файл:** `/home/user/healrhcare_brasil/backend/scripts/restore_media_records.php`

**Логика:**
1. Сканировать папку `backend/public/uploads`
2. Для каждого файла:
   - Извлечь метаданные (размер, MIME, размеры для изображений)
   - Создать запись в таблице `media`
   - Использовать дефолтный `uploaded_by` (первый admin user)
3. Логировать процесс
4. Вывести статистику

#### Шаг 4: Запустить восстановление

```bash
cd /home/user/healrhcare_brasil/backend
php scripts/restore_media_records.php
```

#### Шаг 5: Проверить результат

```bash
# Проверить, что записи появились в таблице media
# Открыть медиатеку в браузере
# Попробовать загрузить новый файл
```

---

## 📊 ПРИОРИТИЗАЦИЯ ПРОБЛЕМ

| # | Проблема | Приоритет | Сложность | Время |
|---|----------|-----------|-----------|-------|
| 1 | Рендеринг HTML (пустой `rendered_html`) | 🔴 КРИТИЧЕСКИЙ | Средняя | 2-3 часа |
| 2 | Медиатека (таблица пуста) | 🔴 КРИТИЧЕСКИЙ | Средняя | 2-3 часа |
| 3 | Коллекция: дефолтная секция | 🟡 ВЫСОКИЙ | Низкая | 15 минут |
| 4 | Коллекция: обработка пустой | 🟢 СРЕДНИЙ | Низкая | 30 минут |
| 5 | Коллекция: hardcoded секции | 🟢 СРЕДНИЙ | Высокая | 3-4 часа |
| 6 | Коллекция: rendered_html | 🟢 НИЗКИЙ | Средняя | 1-2 часа |

---

## 📁 ПОЛНЫЙ СПИСОК ФАЙЛОВ ДЛЯ ИЗМЕНЕНИЯ

### Новые файлы (создать)

1. **`/home/user/healrhcare_brasil/backend/scripts/regenerate_all_rendered_html.php`**
   - Массовая регенерация HTML для всех опубликованных страниц

2. **`/home/user/healrhcare_brasil/backend/scripts/restore_media_records.php`**
   - Восстановление записей в таблице `media` из физических файлов

### Существующие файлы (изменить)

#### КРИТИЧЕСКИЕ (исправить сразу)

3. **`backend/src/Presentation/Controller/PublicPageController.php`**
   - Строка 609: изменить `'guides'` → `null`
   - Строки 611-613: обновить валидацию секции

#### СРЕДНИЙ ПРИОРИТЕТ

4. **`backend/src/Application/UseCase/GetCollectionItems.php`**
   - После строки 135: добавить `isEmpty` и `emptyMessage`

#### НИЗКИЙ ПРИОРИТЕТ (опционально)

5. **`backend/src/Application/UseCase/GetCollectionItems.php`**
   - Строки 54-61: рефакторинг hardcoded секций (читать из config)

6. **`backend/src/Presentation/Controller/PublicPageController.php`**
   - Метод `renderCollectionPage()`: добавить проверку `rendered_html`

---

## 🛠️ ПОШАГОВЫЙ ПЛАН ВОССТАНОВЛЕНИЯ

### Фаза 1: Диагностика (30 минут)

**Цель:** Подтвердить проблемы

```bash
# 1. Проверить состояние rendered_html в БД
cd /home/user/healrhcare_brasil/backend
php scripts/inspect_page_renderedhtml.php [slug]

# 2. Проверить таблицу media
# (нужен доступ к MySQL)

# 3. Проверить файловую систему
ls -la /home/user/healrhcare_brasil/backend/public/uploads
ls -la /home/user/healrhcare_brasil/backend/uploads
ls -la /home/user/healrhcare_brasil/frontend/uploads

# 4. Проверить публичную страницу в браузере
# Открыть исходный код → найти комментарий SERVED=...
```

### Фаза 2: Критические исправления (4-6 часов)

#### Задача 1: Восстановить rendered_html

```bash
# 1. Создать скрипт регенерации (см. раздел "Проблема #1")
# 2. Запустить скрипт
cd /home/user/healrhcare_brasil/backend
php scripts/regenerate_all_rendered_html.php

# 3. Проверить результат
# Открыть публичную страницу → должно быть: SERVED=pre-rendered
```

#### Задача 2: Восстановить медиатеку

```bash
# 1. Создать папку uploads
mkdir -p /home/user/healrhcare_brasil/backend/public/uploads
chmod 755 /home/user/healrhcare_brasil/backend/public/uploads

# 2. Переместить файлы
cp -r /home/user/healrhcare_brasil/backend/uploads/* \
      /home/user/healrhcare_brasil/backend/public/uploads/

# 3. Создать скрипт восстановления БД (см. раздел "Проблема #3")
# 4. Запустить скрипт
cd /home/user/healrhcare_brasil/backend
php scripts/restore_media_records.php

# 5. Проверить результат
# Открыть медиатеку в браузере → должны быть файлы
```

#### Задача 3: Исправить дефолтную секцию коллекции

```bash
# 1. Отредактировать файл
# backend/src/Presentation/Controller/PublicPageController.php
# Строка 609: 'guides' → null

# 2. Синхронизировать с XAMPP (если используется)
# 3. Проверить результат
# Открыть /all-materials → должны быть все материалы (гайды + статьи)
```

### Фаза 3: Средний приоритет (1-2 часа)

#### Задача 4: Обработка пустой коллекции

```bash
# 1. Отредактировать файл
# backend/src/Application/UseCase/GetCollectionItems.php
# После строки 135 добавить isEmpty flag

# 2. Синхронизировать с XAMPP
# 3. Проверить результат (создать тестовую пустую коллекцию)
```

### Фаза 4: Валидация (1 час)

**Чеклист проверки:**

- [ ] Все опубликованные страницы отдают `SERVED=pre-rendered`
- [ ] Медиатека показывает все файлы
- [ ] Загрузка новых файлов работает
- [ ] Страница-коллекция `/all-materials` показывает все материалы (не только гайды)
- [ ] Пустая коллекция отображает сообщение "Элементов не найдено"
- [ ] Кодировка UTF-8 работает корректно (кириллица отображается)

---

## 🚨 РИСКИ И ПРЕДУПРЕЖДЕНИЯ

### Риск #1: Потеря данных при регенерации HTML

**Описание:** При регенерации `rendered_html` существующий HTML будет перезаписан

**Митигация:**
- Сделать бэкап таблицы `pages` перед запуском скрипта
- Проверить работу скрипта на одной странице перед массовой обработкой

### Риск #2: Проблемы с правами доступа к папке uploads

**Описание:** Веб-сервер может не иметь прав на запись в `backend/public/uploads`

**Митигация:**
- Установить правильные права: `chmod 755` для папки
- Проверить владельца папки (должен быть пользователь веб-сервера)

### Риск #3: Несоответствие recorded media и физических файлов

**Описание:** После восстановления записей в БД могут быть расхождения

**Митигация:**
- Запустить `backend/scripts/cleanup-missing-media.php --dry-run` для диагностики
- Проверить результат вручную перед удалением несоответствующих записей

---

## 📚 РЕКОМЕНДАЦИИ НА БУДУЩЕЕ

### 1. Автоматическое резервное копирование

**Проблема:** После восстановления БД из бэкапа данные не полные

**Решение:**
- Настроить ежедневный бэкап БД (mysqldump)
- Сохранять бэкапы за последние 30 дней
- Включить в бэкап папку `uploads`

### 2. Мониторинг целостности данных

**Проблема:** Расхождения между БД и файловой системой не обнаруживаются автоматически

**Решение:**
- Запускать `cleanup-missing-media.php --dry-run` в cron (ежедневно)
- Отправлять уведомления при обнаружении расхождений

### 3. Документация процессов

**Проблема:** Нет документации по восстановлению после сбоя БД

**Решение:**
- Создать файл `docs/RECOVERY_PROCEDURES.md`
- Описать процедуры восстановления для каждого типа сбоя
- Включить чеклисты валидации

### 4. Улучшение логирования

**Проблема:** Недостаточно логов для диагностики проблем

**Решение:**
- Добавить логирование в критические операции (публикация, загрузка медиа)
- Использовать уровни логов (DEBUG, INFO, ERROR)
- Сохранять логи за последние 7 дней

---

## ✅ ИТОГОВЫЙ ВЫВОД

**Проблема не в одной точке.** Требуется комплексное восстановление:

1. **Рендеринг HTML:** Регенерировать `rendered_html` для всех страниц
2. **Медиатека:** Восстановить записи в таблице `media`, переместить файлы
3. **Коллекция:** Исправить логические ошибки в коде

**Общее время восстановления:** 6-8 часов

**Приоритет выполнения:**
1. Сначала: rendered_html + медиатека (критические проблемы)
2. Затем: коллекция (логические ошибки)

**Следующий шаг:** Создать скрипты восстановления и начать Фазу 2 плана.

---

**Готово к реализации.**
